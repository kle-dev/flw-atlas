# Flowable App Overview — `miniproject`

_Scanned 12 model files, 0 archives, 2 Java files, 2 REST endpoints. Generated by Flowable Atlas — regenerate with `atlas <project-dir>`. Diagrams of the models that carry layout: `miniproject.diagrams/*.svg`._

## 1. Apps

### Demo App (`demoApp`) — `apps/demo.app`
> Miniature fixture app for the atlas test suite
- variables: 1 · models: 5
    - bpmn `orderProcess` → `processes/order.bpmn`
    - cmmn `reviewCase` → `cases/review.cmmn`
    - form `orderForm` → `forms/order-form.form`
    - service `customerService` → `services/customer.service`
    - dataObject `customerDO` → `data/customer.data`

## 2. Processes (BPMN)

### Order Process (`orderProcess`) — `processes/order.bpmn`
> Handles an order from entry to approval.
- starter groups: `sales`
- 🔔 Start event `start` · next: `calcTask`
- ⚙️ Service task `calcTask` Calculate total → ${demoBean.run(execution)} → var `total` · next: `approveTask` [`${total > 100}`]
- 👤 User task `approveTask` Approve order form=`orderForm` · next: `notifyTask` [`${vars:bogus(}`]
- ⚙️ Service task `notifyTask` Notify → ${notifierBean} · next: `callSub`
    - 🎧 Execution listener [end] → (script)
- 📞 Call activity `callSub` → process `fulfilmentProcess` in(orderId→subOrderId) out(subTotal→total) · next: `decideTask`
- ⚙️ Service task `decideTask` Decide approval → dmn · next: `waitShipped`
- 📊 Decision task `decideTask` → decision table `orderDecision`
- ▫ Receive task `waitShipped` Wait for shipment · next: `stampTask`
- 🧾 Script task `stampTask` Stamp order (groovy) · next: `end`
- 🔔 End event `end`
- 🧾 Script task `badStamp` Broken stamp (groovy) — ⚠ 1 syntax finding · _(no sequence flow — not reachable)_
- 🧾 Script task `badApi` Broken API call (groovy) — ⚠ 1 syntax finding · _(no sequence flow — not reachable)_

## 3. Cases (CMMN)

### Review Case (`reviewCase`) — `cases/review.cmmn`
    - 📁 **Case plan model** Review plan
        - Human task Review order formKey=`orderForm`
        - Process task Start order processRef=`orderProcess`
        - Task Lookup customer serviceModelKey=`customerService`

## 4. Decision tables (DMN)

- `orderDecision` (FIRST, 1 rules) — in: `total` → out: `approved` — `decisions/order-decision.dmn`
    - when > 100 → true _(Large orders need approval.)_

## 5. Forms & pages

### Order Form (`orderForm`) — `forms/order-form.form`
- `customer` [select] Customer
- `amount` [number] Amount *(req)*
- `totalDisplay` [text] Total ← `{{amount}}`
- `lookupCustomer` [button] Lookup customer
- `customerRecord` [select] Customer record
- `canEditButton` [restButton] Check if editable
- `notifyButton` [workAction] Notify customer
- outcomes: `approve`
- 🔌 data source: rest `/api/customers`
- 🔌 data source: service `customerService.findAll`
- 🔌 data source: dataObject `customerDO.findAll`

## 5b. Data objects

- `priorityMD` (masterData) [2 fields: level, color] — `data/priority.data`
- `customerDO` (lookup) (table `cust_customer`) [3 fields: id, customerName, priority] — `data/customer.data`

## 7. Integration: Services, Channels, Events & REST

### Service models (.service)
- **Customer Service** (`customerService`, db) · table: `cust_customer` — `services/customer.service`
    - `GET` /api/customers (op `findAll`)

### Data layer — service ↔ Liquibase table ↔ data object
- **Customer Service** (`customerService`) → table `cust_customer` · changelog `001-customer` · 2/3 columns mapped end-to-end · data objects `customerDO`
    - ⚠ `orphan_col_` (varchar(255)) — in Liquibase only — the service does not map it

### Events (.event)
- `orderShipped` — payload: `orderId`, `carrier` · correlation: `orderId`

### REST endpoints CALLED by the app (→ matched code)
- `GET` /api/customers  _(from form-query `orderForm/customer`)_
    - ✅ served by GET /api/customers -> CustomerController#customers (src/main/java/com/example/CustomerController.java:10)
- `GET` {{endpoints.baseUrl}}/api/customers/{{customer}}/canEdit  _(from form-button `orderForm/canEditButton`)_
    - ✅ served by GET /api/customers/{id}/canEdit -> CustomerController#canEdit (src/main/java/com/example/CustomerController.java:15)
    - ≈ possibly GET /api/customers -> CustomerController#customers (src/main/java/com/example/CustomerController.java:10)
- `GET` /api/customers  _(from service-op `customerService/findAll`)_
    - ✅ served by GET /api/customers -> CustomerController#customers (src/main/java/com/example/CustomerController.java:10)

## 8. Java glue code

### REST controllers & endpoints
- **CustomerController** — `src/main/java/com/example/CustomerController.java`
    - `GET` /api/customers → customers() (line 10)
    - `GET` /api/customers/{id}/canEdit → canEdit() (line 15)

## 9. Resolved references (model → code / model)

### Model → Java code
- `orderProcess` [serviceTask-delegate] `demoBean` → src/main/java/com/example/DemoBean.java:6 (com.example.DemoBean)
- `orderProcess` [calls run()] `demoBean` → src/main/java/com/example/DemoBean.java:6 (com.example.DemoBean)

### Model → Model
- `orderForm` —field-service→ service `customerService` (`services/customer.service`)
- `orderForm` —field-dataObject→ dataObject `customerDO` (`data/customer.data`)
- `orderForm` —triggers-action→ action `notifyCustomerAction` (`actions/notify-customer.action`)
- `orderProcess` —start-form→ form `orderForm` (`forms/order-form.form`)
- `orderProcess` —userTask-form→ form `orderForm` (`forms/order-form.form`)
- `orderProcess` —ruleTask-decision→ decision `orderDecision` (`decisions/order-decision.dmn`)
- `orderProcess` —receives-event→ event `orderShipped` (`events/order-shipped.event`)
- `reviewCase` —humanTask-form→ form `orderForm` (`forms/order-form.form`)
- `reviewCase` —processTask→ process `orderProcess` (`processes/order.bpmn`)
- `reviewCase` —serviceMapping→ service `customerService` (`services/customer.service`)
- `customerDO` —backed-by-service→ service `customerService` (`services/customer.service`)
- `customerDO` —relates-to→ dataObject `priorityMD` (`data/priority.data`)
- `demoApp` —contains→ model:bpmn `orderProcess` (`processes/order.bpmn`)
- `demoApp` —contains→ model:cmmn `reviewCase` (`cases/review.cmmn`)
- `demoApp` —contains→ model:form `orderForm` (`forms/order-form.form`)
- `demoApp` —contains→ model:service `customerService` (`services/customer.service`)
- `demoApp` —contains→ model:dataObject `customerDO` (`data/customer.data`)

## 10. Unresolved references (external / library / missing)

### Other unresolved — review (external dependency, typo, or missing)
- [bean] `notifierBean` — from `orderProcess`
- [process] `fulfilmentProcess` — from `orderProcess`

## 11. Access & user groups (who can do what)

**All groups/roles referenced (3):** `auditors`, `backoffice`, `sales`

### App / page access
- app `demoApp`: sales, backoffice

### Who can start (processes & cases)
- process `orderProcess` ← sales
- case `reviewCase` ← auditors

### Task assignment (candidate groups / assignees per model)
- `orderProcess`: backoffice
- `reviewCase`: auditors

### Data object access (who may query/create/update/delete)
- `customerDO`: query=sales

### Security policies
- `orderPolicy` — `policies/case.policy`
    - read: sales
    - update: backoffice

## 12. Code map (where things live)

**Service models (.service):**
- `customerService` (db) · table: `cust_customer` — `services/customer.service`

**Models by folder:**
- `actions/` — 1 action
- `cases/` — 1 cmmn
- `data/` — 2 data
- `db/changelog/` — 1 liquibase
- `decisions/` — 1 dmn
- `events/` — 1 event
- `forms/` — 1 form
- `policies/` — 1 policy
- `processes/` — 1 bpmn
- `services/` — 1 service

**Java code by role:**

_controller (1):_
- `com.example.CustomerController` — `src/main/java/com/example/CustomerController.java:8`

_component (1):_
- `com.example.DemoBean` — `src/main/java/com/example/DemoBean.java:6`

## 13. Variables, beans & expressions

**Variables (17)** — where each one lives, is set and is read:

- `amount` [form]
    - used in: orderForm (form field), orderForm {{amount}}
- `appVar` [app]
    - used in: demoApp (app variable)
- `approved` [decision]
    - used in: orderDecision (declared / mapped)
- `color` [dataObject]
    - used in: priorityMD (declared / mapped)
- `courierCode` [process] ⚠ _inferred from a script_
    - touched by scripts: `Stamp order`
- `customer` [form]
    - used in: orderForm (form field), orderForm {{customer.email}}, orderForm {{customer}}
- `customerEmail` [action]
    - used in: notifyCustomerAction (script) · passed as: in on `script-evaluation-bot`: customerEmail
- `customerRecord` [form]
    - used in: orderForm (form field)
- `level` [dataObject]
    - used in: priorityMD (declared / mapped)
- `notified` [action, process]
    - passed as: in on `script-evaluation-bot`: notified · touched by scripts: `Notify`
- `orderId` [process]
    - used in: orderProcess (declared / mapped) · passed as: in on `callSub`: orderId→subOrderId · touched by scripts: `Stamp order`, `Broken stamp`, `Broken API call`
- `orderTotal` [action]
    - used in: notifyCustomerAction (script) · passed as: in on `script-evaluation-bot`: orderTotal
- `sent` [action]
    - used in: notifyCustomerAction (script) · passed as: out on `script-evaluation-bot`: sent
- `shippingStamp` [process]
    - touched by scripts: `Stamp order`, `Broken stamp`
- `subOrderId` [process]
    - used in: orderProcess (declared / mapped) · passed as: in on `callSub`: orderId→subOrderId
- `subTotal` [process]
    - used in: orderProcess (declared / mapped) · passed as: out on `callSub`: subTotal→total
- `total` [decision, java, process]
    - used in: orderProcess ${total > 100}, orderProcess (declared / mapped), orderDecision (declared / mapped), com.example.DemoBean (Java: set/getVariable) · passed as: out on `calcTask`: total, out on `callSub`: subTotal→total

**Beans/objects with method calls:** `demoBean`

**Expressions (10)** — grouped by what they call:

- `$response` (2)
    - `{{$response.canEdit}}` — in `orderForm`
    - `{{$response.executionPayload.sent}}` — in `orderForm`
- `customer` (2)
    - `{{customer}}` — in `orderForm`
    - `{{customer.email}}` — in `orderForm`
- `amount` (1)
    - `{{amount}}` — in `orderForm`
- `demoBean` (1)
    - `${demoBean.run(execution)}` — in `orderProcess`
- `endpoints` (1)
    - `{{endpoints.baseUrl}}` — in `orderForm`
- `notifierBean` (1)
    - `${notifierBean}` — in `orderProcess`
- `total` (1)
    - `${total > 100}` — in `orderProcess`
- `vars:bogus` (1) — invalid: 1 ⚠
    - ⚠ `${vars:bogus(}` — in `orderProcess`

**Also in the graph** (queryable in `miniproject.graph.json`, no section here): 1 serviceOperation · 1 method · 1 bot.

## 14. Findings — 8 open

unparseable files: 1 · invalid expressions: 2 · script syntax: 2 · missing models: 1 · schema gaps: 1 · script-inferred variables: 1

**unparseable files** (1)
- ⚠ parse: (form) Expecting property name enclosed in double quotes: line 2 column 1 (char 37) — in: `broken.form`

**invalid expressions** (2)
- · Unknown function 'vars:bogus' — in: `${vars:bogus(}`
    `bogus`
- ⚠ Unclosed '(' — in: `${vars:bogus(}`
    `(`

**script syntax** (2)
- ⚠ '(' is never closed — in: `Order Process` · at: `badStamp` · file: `processes/order.bpmn:1`
    `execution.setVariable('shippingStamp', "x-" + orderId`
- · 'execution' has no member 'setTransientVariabel' — did you mean 'setTransientVariable'? — in: `Order Process` · at: `badApi` · file: `processes/order.bpmn:1`
    `execution.setTransientVariabel('meta', orderId)`

**missing models** (1)
- ⚠ referenced model does not exist in this project — in: `fulfilmentProcess`

**schema gaps** (1)
- · column `orphan_col_` of table `cust_customer` is in Liquibase but not mapped by the service — in: `Customer Service` · file: `services/customer.service`

**script-inferred variables** (1)
- · only a script mentions this variable — the name is inferred, not declared — in: `courierCode`

