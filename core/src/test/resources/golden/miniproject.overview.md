# Flowable App Overview — `miniproject`

_Scanned 25 model files, 1 archives, 2 Java files, 2 REST endpoints. Generated by Flowable Atlas — regenerate with `atlas <project-dir>`. Diagrams of the models that carry layout: `miniproject.diagrams/*.svg`._

## 1. Apps

### Demo App (`demoApp`) — `apps/demo.app`
> Miniature fixture app for the atlas test suite
- variables: 1 · models: 5
    - bpmn `orderProcess` → `processes/order.bpmn`
    - cmmn `reviewCase` → `cases/review.cmmn`
    - form `orderForm` → `forms/order-form.form`
    - service `customerService` → `services/customer.service`
    - dataObject `customerDO` → `data/customer.data`

## 2. Processes (BPMN)

### Fulfilment Process (`fulfilmentProcess`) — `processes/fulfilment.bpmn`
> Ships an approved order and reports the shipping total back to its caller.
- 🔔 Start event `fStart` · next: `packTask`
- 🔔 Boundary event (timer=PT2H) `courierLate` Courier late · next: `fEnd`
- 🧾 Script task `packTask` Pack shipment (groovy) · next: `shipGw`
- ◆ Exclusive gateway `shipGw` Courier needed? · next: `callCourier` [`${subTotal > 5}`], `stockOk` (pickup)
- 📞 Call activity `callCourier` → process `courierProcess` · next: `stockOk`
- 🔔 Intermediate catch event (conditional=${stockLevel > 0}) `stockOk` Stock confirmed · next: `fEnd`
- 🔔 End event `fEnd`

### Order Process (`orderProcess`) — `processes/order.bpmn`
> Handles an order from entry to approval.
- starter groups: `sales`
- 🔔 Start event `start` · next: `calcTask`
- ⚙️ Service task `calcTask` Calculate total → ${demoBean.run(execution)} → var `total` · next: `approveTask` [`${total > 100}`]
- 👤 User task `approveTask` Approve order form=`orderForm` · next: `notifyTask` [`${vars:bogus(}`]
- ⚙️ Service task `notifyTask` Notify → ${notifierBean} · next: `callSub`
    - 🎧 Execution listener [end] → (script)
- 📞 Call activity `callSub` → process `fulfilmentProcess` in(orderId→subOrderId) in(orderId→orderId) out(subTotal→total) · next: `decideTask`
- ⚙️ Service task `decideTask` Decide approval → dmn · next: `waitShipped`
- 📊 Decision task `decideTask` → decision table `orderDecision`
- ▫ Receive task `waitShipped` Wait for shipment · next: `stampTask`
- 🧾 Script task `stampTask` Stamp order (groovy) · next: `end`
- 🔔 End event `end`
- 🧾 Script task `badStamp` Broken stamp (groovy) — ⚠ 1 syntax finding · _(no sequence flow — not reachable)_
- 🧾 Script task `badApi` Broken API call (groovy) — ⚠ 1 syntax finding · _(no sequence flow — not reachable)_

### Archived Process (`archivedProcess`) — `archives/demo-export.zip!apps/demo.bar!processes/archived.bpmn`
- 🔔 Start event `start` · next: `confirm`
- 👤 User task `confirm` Confirm order form=`orderForm` · next: `end`
- 🔔 End event `end`

## 3. Cases (CMMN)

### Review Case (`reviewCase`) — `cases/review.cmmn`
    - 📁 **Case plan model** Review plan
        - Human task Review order formKey=`orderForm`
        - Process task Start order processRef=`orderProcess`
        - Task Lookup customer serviceModelKey=`customerService`

## 4. Decision tables (DMN)

- `orderDecision` (FIRST, 1 rules) — in: `total` → out: `approved` — `decisions/order-decision.dmn`
    - when > 100 → true _(Large orders need approval.)_

## 5. Forms & pages

### Order Form (`orderForm`) — `forms/order-form.form`
- `customer` [select] Customer
- `amount` [number] Amount *(req)*
- `totalDisplay` [text] Total ← `{{amount}}`
- `lookupCustomer` [button] Lookup customer → service `customerService`
- `customerRecord` [select] Customer record
- `canEditButton` [restButton] Check if editable
- `notifyButton` [workAction] Notify customer → action `notifyCustomerAction` visible when `{{amount > 0}}`
- `orderTotal` [scriptButton] Recalculate ← `{{orderTotal}}` = `{{amount * 1.081}}`
- `escalate` [workAction] → action `notifyCustomerAction`
- `customerPortal` [linkButton] Open customer portal
- `creditScore` [restButton] Refresh score ← `{{$temp.score}}` *(hidden)* value dropped when `{{!customer}}`
- `internalNote` [textarea] Internal note enabled when `{{isSupervisor}}`
- outcomes: `approve`
- 🔌 data source: rest `/api/customers`
- 🔌 data source: service `customerService.findAll`
- 🔌 data source: dataObject `customerDO.findAll`

### Order Page (`orderPage`) — `pages/order-page.page`
- `welcome` [text] Open orders ← `{{openOrderCount}} orders waiting`
- `startOrder` [button] Start an order

## 5b. Data objects

- `customerDO` (lookup) (table `cust_customer`) [3 fields: id, customerName, priority] — `data/customer.data`
- `priorityMD` (masterData) [2 fields: level, color] — `data/priority.data`

## 5c. Data dictionaries

- `orderTypes` — types: `Address`, `Customer` — `dictionaries/order-types.dictionary`

## 5d. Other models (queries, templates, policies, sequences, …)

**document** (1):
- `contractDocument` Contract — `documents/contract.document`
**knowledgeBase** (1):
- `courierManuals` Courier manuals — `knowledgebase/courier-manuals.knowledgebase`
**palette** (1):
- `demoPalette` Demo palette — `palettes/demo.palette`
**query** (1):
- `openOrdersQuery` Open orders — `queries/open-orders.query`
**sequence** (1):
- `orderNumberSequence` Order number — `sequences/order-number.sequence`
**sla** (1):
- `approvalSla` Approval SLA — `slas/approval-sla.sla`
**template** (1):
- `orderConfirmationTemplate` Order confirmation — `templates/order-confirmation.tpl`
**variableExtractor** (1):
- `orderVarExtractor` Order variables — `extractors/order-vars.extractor`

## 6. AI agents & bots

### `orderAssistant` — Order assistant (conversational) — `agents/order-assistant.agent`
- model: openai/gpt-4o · temperature: 0.2 · apiEndpoint: yes
- 📚 knowledgeBase → `courierManuals`
- 🛠️ tool (service) → `customerService`

## 7. Integration: Services, Channels, Events & REST

### Service models (.service)
- **Customer Service** (`customerService`, db) · table: `cust_customer` — `services/customer.service`
    - `GET` /api/customers (op `findAll`)

### Data layer — service ↔ Liquibase table ↔ data object
- **Customer Service** (`customerService`) → table `cust_customer` · changelog `001-customer` · 2/3 columns mapped end-to-end · data objects `customerDO`
    - ⚠ `orphan_col_` (varchar(255)) — in Liquibase only — the service does not map it

### Channels (.channel)
- `orderEvents` inbound/jms · destination: `orders.shipped` — `channels/order-events.channel`

### Events (.event)
- `orderShipped` — payload: `orderId`, `carrier` · correlation: `orderId`

### REST endpoints CALLED by the app (→ matched code)
- `GET` /api/customers  _(from form-query `orderForm/customer`)_
    - ✅ served by GET /api/customers -> CustomerController#customers (src/main/java/com/example/CustomerController.java:10)
- `GET` {{endpoints.baseUrl}}/api/customers/{{customer}}/canEdit  _(from form-button `orderForm/canEditButton`)_
    - ✅ served by GET /api/customers/{id}/canEdit -> CustomerController#canEdit (src/main/java/com/example/CustomerController.java:15)
    - ≈ possibly GET /api/customers -> CustomerController#customers (src/main/java/com/example/CustomerController.java:10)
- `GET` https://portal.example.com/{{customer}}  _(from form-button `orderForm/customerPortal`)_
    - ⚠️ no matching controller in project (external or unimplemented)
- `GET` /api/customers/{{customer}}/score  _(from form-button `orderForm/creditScore`)_
    - ≈ possibly GET /api/customers -> CustomerController#customers (src/main/java/com/example/CustomerController.java:10)
    - ⚠️ no matching controller in project (external or unimplemented)
- `GET` /api/customers  _(from service-op `customerService/findAll`)_
    - ✅ served by GET /api/customers -> CustomerController#customers (src/main/java/com/example/CustomerController.java:10)

## 8. Java glue code

### REST controllers & endpoints
- **CustomerController** — `src/main/java/com/example/CustomerController.java`
    - `GET` /api/customers → customers() (line 10)
    - `GET` /api/customers/{id}/canEdit → canEdit() (line 15)

## 9. Resolved references (model → code / model)

### Model → Java code
- `orderProcess` [serviceTask-delegate] `demoBean` → src/main/java/com/example/DemoBean.java:6 (com.example.DemoBean)
- `orderProcess` [calls run()] `demoBean` → src/main/java/com/example/DemoBean.java:6 (com.example.DemoBean)

### Model → Model
- `orderAssistant` —tool→ service `customerService` (`services/customer.service`)
- `orderAssistant` —knowledgeBase→ knowledgeBase `courierManuals` (`knowledgebase/courier-manuals.knowledgebase`)
- `demoApp` —contains→ model:bpmn `orderProcess` (`processes/order.bpmn`)
- `demoApp` —contains→ model:cmmn `reviewCase` (`cases/review.cmmn`)
- `demoApp` —contains→ model:form `orderForm` (`forms/order-form.form`)
- `demoApp` —contains→ model:service `customerService` (`services/customer.service`)
- `demoApp` —contains→ model:dataObject `customerDO` (`data/customer.data`)
- `reviewCase` —humanTask-form→ form `orderForm` (`forms/order-form.form`)
- `reviewCase` —processTask→ process `orderProcess` (`processes/order.bpmn`)
- `reviewCase` —serviceMapping→ service `customerService` (`services/customer.service`)
- `orderEvents` —channel-event→ event `orderShipped` (`events/order-shipped.event`)
- `customerDO` —backed-by-service→ service `customerService` (`services/customer.service`)
- `customerDO` —relates-to→ dataObject `priorityMD` (`data/priority.data`)
- `contractDocument` —document-view-form→ form `orderForm` (`forms/order-form.form`)
- `orderVarExtractor` —extracts-from→ process `orderProcess` (`processes/order.bpmn`)
- `orderVarExtractor` —extracts-from→ process `reviewCase` (`cases/review.cmmn`)
- `orderForm` —field-service→ service `customerService` (`services/customer.service`)
- `orderForm` —field-dataObject→ dataObject `customerDO` (`data/customer.data`)
- `orderForm` —triggers-action→ action `notifyCustomerAction` (`actions/notify-customer.action`)
- `orderProcess` —start-form→ form `orderForm` (`forms/order-form.form`)
- `orderProcess` —userTask-form→ form `orderForm` (`forms/order-form.form`)
- `orderProcess` —callActivity→ process `fulfilmentProcess` (`processes/fulfilment.bpmn`)
- `orderProcess` —ruleTask-decision→ decision `orderDecision` (`decisions/order-decision.dmn`)
- `orderProcess` —receives-event→ event `orderShipped` (`events/order-shipped.event`)
- `approvalSla` —escalation-starts→ process `fulfilmentProcess` (`processes/fulfilment.bpmn`)
- `archivedProcess` —userTask-form→ form `orderForm` (`forms/order-form.form`)

## 10. Unresolved references (external / library / missing)

### Other unresolved — review (external dependency, typo, or missing)
- [bean] `notifierBean` — from `orderProcess`
- [process] `courierProcess` — from `fulfilmentProcess`

## 11. Access & user groups (who can do what)

**All groups/roles referenced (3):** `auditors`, `backoffice`, `sales`

### App / page access
- app `demoApp`: sales, backoffice

### Who can start (processes & cases)
- process `orderProcess` ← sales
- case `reviewCase` ← auditors

### Task assignment (candidate groups / assignees per model)
- `orderProcess`: backoffice
- `reviewCase`: auditors

### Data object access (who may query/create/update/delete)
- `customerDO`: query=sales

### Security policies
- `orderPolicy` — `policies/case.policy`
    - read: sales
    - update: backoffice

## 12. Code map (where things live)

**Agents / bots (models):**
- `orderAssistant` — `agents/order-assistant.agent`
**Service models (.service):**
- `customerService` (db) · table: `cust_customer` — `services/customer.service`

**Models by folder:**
- `actions/` — 1 action
- `agents/` — 1 agent
- `archives/` — 1 bpmn
- `cases/` — 1 cmmn
- `channels/` — 1 channel
- `data/` — 2 data
- `db/changelog/` — 1 liquibase
- `decisions/` — 1 dmn
- `dictionaries/` — 1 dict
- `documents/` — 1 other
- `events/` — 1 event
- `extractors/` — 1 other
- `forms/` — 1 form
- `knowledgebase/` — 1 other
- `pages/` — 1 form
- `palettes/` — 1 other
- `policies/` — 1 policy
- `processes/` — 2 bpmn
- `queries/` — 1 other
- `sequences/` — 1 other
- `services/` — 1 service
- `slas/` — 1 other
- `templates/` — 1 other

**Java code by role:**

_controller (1):_
- `com.example.CustomerController` — `src/main/java/com/example/CustomerController.java:8`

_component (1):_
- `com.example.DemoBean` — `src/main/java/com/example/DemoBean.java:6`

## 13. Variables, beans & expressions

**Variables (31)** — where each one lives, is set and is read:

- `amount` [form]
    - used in: orderForm (form field), orderForm {{amount * 1.081}}, orderForm {{amount > 0}}, orderForm {{amount}}
- `appVar` [app]
    - used in: demoApp (app variable)
- `approved` [decision]
    - used in: orderDecision (declared / mapped)
- `color` [dataObject]
    - used in: priorityMD (declared / mapped)
- `contractType` [document]
    - used in: contractDocument (declared / mapped)
- `courierCode` [process] ⚠ _inferred from a script_
    - touched by scripts: `Stamp order`
- `courierIndexed` [variableExtractor]
    - used in: orderVarExtractor (declared / mapped)
- `customer` [form]
    - used in: orderForm (form field), orderForm {{customer.email}}, orderForm {{customer}}
- `customerEmail` [action]
    - used in: notifyCustomerAction (script) · passed as: in on `script-evaluation-bot`: customerEmail
- `customerName` [agent, template]
    - used in: orderConfirmationTemplate ${customerName}, orderAssistant {{customerName}}
- `customerRecord` [form]
    - used in: orderForm (form field)
- `grandTotal` [template]
    - used in: orderConfirmationTemplate ${grandTotal}
- `internalNote` [form]
    - used in: orderForm (form field)
- `isSupervisor` [form]
    - used in: orderForm {{isSupervisor}}
- `level` [dataObject]
    - used in: priorityMD (declared / mapped)
- `manager` [sla]
    - used in: approvalSla ${manager}
- `managerApproved` [sla]
    - used in: approvalSla ${!managerApproved}
- `minTotal` [query]
    - used in: openOrdersQuery {{minTotal}}
- `notified` [action, process]
    - passed as: in on `script-evaluation-bot`: notified · touched by scripts: `Notify`
- `openOrderCount` [page]
    - used in: orderPage {{openOrderCount}}
- `orderId` [agent, process, query]
    - used in: orderAssistant {{orderId}}, openOrdersQuery {{orderId}}, orderProcess (declared / mapped) · passed as: in on `callSub`: orderId→subOrderId, in on `callSub`: orderId · touched by scripts: `Stamp order`, `Broken stamp`, `Broken API call`
- `orderNotes` [process]
    - used in: fulfilmentProcess (declared / mapped) · touched by scripts: `Pack shipment`
- `orderNumber` [template]
    - used in: orderConfirmationTemplate ${orderNumber}
- `orderTotal` [action, form]
    - used in: orderForm (form field), orderForm {{orderTotal}}, notifyCustomerAction (script) · passed as: in on `script-evaluation-bot`: orderTotal
- `orderTotalIndexed` [variableExtractor]
    - used in: orderVarExtractor (declared / mapped)
- `sent` [action]
    - used in: notifyCustomerAction (script) · passed as: out on `script-evaluation-bot`: sent
- `shippingStamp` [process]
    - touched by scripts: `Stamp order`, `Broken stamp`
- `stockLevel` [process]
    - used in: fulfilmentProcess ${stockLevel > 0}
- `subOrderId` [process]
    - used in: orderProcess (declared / mapped) · passed as: in on `callSub`: orderId→subOrderId
- `subTotal` [process]
    - used in: fulfilmentProcess ${subTotal > 5}, orderProcess (declared / mapped) · passed as: out on `callSub`: subTotal→total · touched by scripts: `Pack shipment`
- `total` [decision, java, process]
    - used in: orderProcess ${total > 100}, orderProcess (declared / mapped), orderDecision (declared / mapped), com.example.DemoBean (Java: set/getVariable) · passed as: out on `calcTask`: total, out on `callSub`: subTotal→total

**Beans/objects with method calls:** `demoBean`

**Expressions (29)** — grouped by what they call:

- `$response` (4)
    - `{{$response.canEdit}}` — in `orderForm`
    - `{{$response.executionPayload.sent}}` — in `orderForm`
    - `{{$response.scopeId}}` — in `orderForm`
    - `{{$response.score}}` — in `orderForm`
- `amount` (3)
    - `{{amount}}` — in `orderForm`
    - `{{amount > 0}}` — in `orderForm`
    - `{{amount * 1.081}}` — in `orderForm`
- `(other)` (2)
    - `${!managerApproved}` — in `approvalSla`
    - `{{!customer}}` — in `orderForm`
- `customer` (2)
    - `{{customer}}` — in `orderForm`
    - `{{customer.email}}` — in `orderForm`
- `customerName` (2)
    - `${customerName}` — in `orderConfirmationTemplate`
    - `{{customerName}}` — in `orderAssistant`
- `$temp` (1)
    - `{{$temp.score}}` — in `orderForm`
- `demoBean` (1)
    - `${demoBean.run(execution)}` — in `orderProcess`
- `endpoints` (1)
    - `{{endpoints.baseUrl}}` — in `orderForm`
- `grandTotal` (1)
    - `${grandTotal}` — in `orderConfirmationTemplate`
- `isSupervisor` (1)
    - `{{isSupervisor}}` — in `orderForm`
- `manager` (1)
    - `${manager}` — in `approvalSla`
- `minTotal` (1)
    - `{{minTotal}}` — in `openOrdersQuery`
- `notifierBean` (1)
    - `${notifierBean}` — in `orderProcess`
- `openOrderCount` (1)
    - `{{openOrderCount}}` — in `orderPage`
- `orderId` (1)
    - `{{orderId}}` — in `orderAssistant`, `openOrdersQuery`
- `orderNumber` (1)
    - `${orderNumber}` — in `orderConfirmationTemplate`
- `orderTotal` (1)
    - `{{orderTotal}}` — in `orderForm`
- `stockLevel` (1)
    - `${stockLevel > 0}` — in `fulfilmentProcess`
- `subTotal` (1)
    - `${subTotal > 5}` — in `fulfilmentProcess`
- `total` (1)
    - `${total > 100}` — in `orderProcess`
- `vars:bogus` (1) — invalid: 1 ⚠
    - ⚠ `${vars:bogus(}` — in `orderProcess`

**Also in the graph** (queryable in `miniproject.graph.json`, no section here): 1 document · 1 variableExtractor · 1 knowledgeBase · 1 palette · 1 query · 1 sequence · 1 sla · 1 template · 1 serviceOperation · 1 method · 1 bot.

## 14. Findings — 13 open

unparseable files: 2 · invalid expressions: 2 · script syntax: 2 · missing models: 1 · schema gaps: 1 · variables never read: 3 · unread call parameters: 1 · script-inferred variables: 1

**unparseable files** (2)
- · skip: JSON carries no model key — not a Design model wrapper — in: `archives/demo-export.zip!manifest.json`
- ⚠ parse: (form) Expecting property name enclosed in double quotes: line 2 column 1 (char 37) — in: `broken.form`

**invalid expressions** (2)
- · Unknown function 'vars:bogus' — in: `${vars:bogus(}`
    `bogus`
- ⚠ Unclosed '(' — in: `${vars:bogus(}`
    `(`

**script syntax** (2)
- ⚠ '(' is never closed — in: `Order Process` · at: `badStamp` · file: `processes/order.bpmn:1`
    `execution.setVariable('shippingStamp', "x-" + orderId`
- · 'execution' has no member 'setTransientVariabel' — did you mean 'setTransientVariable'? — in: `Order Process` · at: `badApi` · file: `processes/order.bpmn:1`
    `execution.setTransientVariabel('meta', orderId)`

**missing models** (1)
- ⚠ referenced model does not exist in this project — in: `courierProcess`

**schema gaps** (1)
- · column `orphan_col_` of table `cust_customer` is in Liquibase but not mapped by the service — in: `Customer Service` · file: `services/customer.service`

**variables never read** (3)
- · written but never read — written by a decision output of `approved` — in: `approved`
- · written but never read — written by a script on `Broken stamp`, a script on `Stamp order` — in: `shippingStamp`
- · written but never read — written by an in parameter on `Fulfil` — in: `subOrderId`

**unread call parameters** (1)
- · mapped into `process:fulfilmentProcess`, which never reads it — written by an in parameter on `Fulfil` — in: `orderId`

**script-inferred variables** (1)
- · only a script mentions this variable — the name is inferred, not declared — in: `courierCode`

