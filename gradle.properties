org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC

# The IntelliJ Platform provides the Kotlin stdlib; don't add our own.
kotlin.stdlib.default.dependency=false

# Compile toolchain is JDK 21 (see build.gradle.kts jvmToolchain(21)). Point Gradle at the
# locally installed JDK 21 so `./gradlew` resolves it without downloading a toolchain.
# Adjust the path if your JDK 21 lives elsewhere.
org.gradle.java.installations.paths=/Users/kevinklever/.sdkman/candidates/java/21.0.4-tem
