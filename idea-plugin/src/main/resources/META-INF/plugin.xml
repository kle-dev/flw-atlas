<!-- require-restart: the plugin registers languages / file types / parser definitions (the two
     expression dialects) which cannot be loaded dynamically, so force IntelliJ to prompt for a
     restart on install / update / uninstall instead of silently half-loading. -->
<idea-plugin require-restart="true">
    <id>com.flowable.atlas</id>
    <name>Flowable Atlas</name>
    <!-- No <version> here on purpose: patchPluginXml injects the Gradle project version at build
         time (root build.gradle.kts → allprojects { version }). A literal here is dead text that
         drifts — this one had been reading 0.10.4 since three releases ago. -->
    <vendor email="kevin.klever@flowable.com" url="https://www.flowable.com">Flowable</vendor>

    <description><![CDATA[
        <p><b>The complete Flowable companion for IntelliJ IDEA.</b> Flowable Atlas turns the model keys
        and expressions scattered across your Java code and Flowable models into first-class, IDE-aware
        references — completed, validated and navigable — and maps your whole project into a single
        interactive explorer. Everything is resolved against the models that <i>actually live in your
        repository</i>, so a wrong key or a broken expression is caught in the editor, long before
        deployment.</p>

        <p>Zero configuration: open a project that contains the Flowable models you exported from Design
        (the app <code>.zip</code> / deployment <code>-bar</code>, holding
        <code>.bpmn / .cmmn / .dmn / .form / .data / .service / …</code>) or the Flowable Design
        <code>*-models</code> workspace, and start typing. The plugin indexes every model and puts that
        knowledge to work across three areas.</p>

        <h3>1 &middot; Model-key intelligence</h3>
        <ul>
          <li><b>Context-aware key completion</b> at every Flowable API position that takes a model key —
              process, case, decision, form, event, channel, data object, master data, service, action,
              agent, knowledge base, template, security policy, query, variable extractor, sequence, SLA,
              dashboard component, data dictionary, page and unified work definitions. Both
              <code>org.flowable.*</code> and <code>com.flowable.*</code>. Search by <b>key, by name, or by
              any fragment</b> — type <code>0061</code> to get <code>DEMO-DO-0061</code>.</li>
          <li><b>Cascade completion</b> for fluent chains — <code>operation("…")</code> completes the
              operations of the data object / service resolved from the sibling
              <code>definitionKey(…)</code> / <code>serviceKey(…)</code> (constant references are
              resolved), and <code>value("…", …)</code> completes that operation's input fields. DMN
              <code>variable("…", …)</code> offers the decision's input/output variables.</li>
          <li><b>More than keys</b> — completion for message &amp; signal names, process variables,
              task-definition keys and activity IDs, <b>scoped</b> to the
              <code>processDefinitionKey</code> / <code>caseDefinitionKey</code> in the same query chain
              when present, so you see only that model's ids.</li>
          <li><b>Model &rarr; model references in BPMN/CMMN XML</b> — the keys that live inside the models
              themselves (<code>callActivity calledElement</code>, <code>flowable:formKey</code>,
              <code>decisionRef</code>, CMMN <code>caseRef</code> / <code>processRef</code>) get the same
              completion, Ctrl/Cmd-click navigation and broken-key inspection as Java call sites — so a
              <code>calledElement</code> pointing at a missing process is caught before deployment.</li>
          <li><b>Broken-key inspection</b> — an unknown key (in Java or model XML) is flagged inline with a
              <i>"did you mean …?"</i> quick-fix to the closest real key.</li>
          <li><b>Navigation &amp; Find Usages</b> — Ctrl/Cmd-click a key literal to jump straight to the
              model file; hover for its type, name and location. Find Usages on a delegate class or an
              <code>${bean.method()}</code> expression lists the model files that reference it.</li>
          <li><b>Code generation</b> — generate a typed Java constants class
              (<code>FlowableModelKeys.Process.ORDER_FULFILMENT</code>, model name in Javadoc) that stays
              in sync as models are added, renamed or removed; or, with Alt/Option-Enter on a data-object
              <code>definitionKey("…")</code>, a typed Java bean built from the model's field mappings.</li>
          <li><b>Liquibase aware</b> — inside a changelog it completes <code>&lt;column&gt;</code> names
              and <code>tableName</code> from the backing service model, and flags columns that no
              backing Flowable model defines.</li>
        </ul>

        <h3>2 &middot; Flowable expression language support</h3>
        <ul>
          <li><b>Both dialects, first-class</b> — the backend JUEL dialect (<code>${…}</code> /
              <code>#{…}</code>) and the frontend form dialect (<code>{{…}}</code>) each get syntax
              highlighting, rainbow parentheses and brace matching.</li>
          <li><b>Live validation</b> — structural syntax errors, and unknown functions, namespaces or
              <code>flw.*</code> members, are flagged against a catalog of the real, verified Flowable
              function set.</li>
          <li><b>Expression completion</b> — functions, engine root objects, and your project's own
              process/case variables and form fields.</li>
          <li><b>Recognised inline, everywhere expressions live</b> — BPMN/CMMN/DMN XML,
              <code>.form</code> / <code>.page</code> JSON, and (optionally) Java string literals — so the
              highlighting, validation and completion follow you into the models you edit.</li>
          <li><b>Real Groovy &amp; JavaScript in script bodies</b> — BPMN/CMMN script tasks, listener
              scripts and action-bot scripts get the IDE's own language support injected inline:
              compiler-grade syntax errors, highlighting and fragment editing (Alt-Enter). Groovy is
              bundled in every IDEA; JavaScript lights up on Ultimate. JUEL or unknown script formats
              keep the plain expression injection. Atlas additionally runs its own dependency-free
              structural script check (unterminated strings/comments, unbalanced brackets,
              <code>scriptFormat</code> typos) in the CLI and the explorer's Checks tab.</li>
          <li><b>Flowable environments</b> — define your DEV/QA/UAT/PROD servers once for the whole IDE
              instead of once per repository, and pick them from a list. Each environment holds a Flowable
              Design connection, a Flowable Work connection, or just one of the two; the Design pull and the
              Expression Playground point independently, so running against QA while models still come from
              DEV1 is an ordinary way to work. Mark an environment <i>Protected</i> and Atlas asks before
              pulling from it or evaluating against it. Passwords and tokens stay in the IDE password
              safe.</li>
          <li><b>Expression Playground</b> (the <i>Flowable Expressions</i> tool window) — type an
              expression, pick backend or frontend, and get instant validation and completion; scope
              completion to one model; evaluate a frontend expression live against a pasted JSON payload;
              or evaluate a backend expression against a running app through the Flowable Inspect REST API.
              Which app it runs against is one of your <b>environments</b>, and the fastest way to switch is
              to paste a Work URL: the environment, the scope and the instance id all fill in from the
              link.</li>
          <li><b>Script Playground</b> (the <i>Scripts</i> tab of the same tool window) — paste or
              write a Groovy/JavaScript/Python script: the IDE's real language editing (completion,
              coloring) where the language plugin is available, live structural validation with
              squiggles and clickable problem rows (the same checks the CLI and the explorer's Checks
              tab run), the scope variables the script touches as chips, a picker that loads any
              script task / listener / action-bot script straight from the project's models, and a
              library of worked examples — a complete, commented script for every context and
              language, to start from rather than from an empty editor.</li>
        </ul>

        <h3>3 &middot; Atlas explorer &amp; hub</h3>
        <ul>
          <li><b>Atlas Hub</b> — a tool window (right stripe) that is the plugin's control center:
              model-index status with per-type counts and one-click rebuild, every generated explorer
              page (double-click to open), and the Flowable Design sync state — kept up to date live.</li>
          <li><b>Map your whole project</b> — Tools → Flowable Atlas → <i>Generate Atlas Explorer…</i>
              turns your models + Java + Liquibase into a single self-contained, offline, interactive
              HTML page. Save it into the project and open it in your browser or right inside the IDE
              (embedded viewer), then click through every relationship in both directions. In
              Settings → Generation you pick exactly which artifacts are written — summary &amp;
              overview Markdown, the graph JSON, the explorer HTML, a CLAUDE.md primer for AI agents.
              Generation runs entirely in-process — no external tools required — and honours the
              project's expression allowlist.</li>
          <li><b>IDE-native viewing</b> — the embedded explorer follows the IDE light/dark theme
              (switching restyles live) and carries a toolbar with <i>Regenerate</i>, <i>Reload</i>
              and <i>Open in Browser</i>.</li>
          <li><b>Reopen without regenerating</b> — <i>Open Atlas Explorer</i> (Atlas Hub, or Tools →
              Flowable Atlas) opens an already-generated <code>*.explorer.html</code> — including
              one written by the standalone <code>atlas</code> CLI into the configured output folder — as a
              center editor tab (like a DB table), so you never have to leave the IDE for a browser.</li>
        </ul>

        <h3>How it feels</h3>
        <ul>
          <li><code>runtimeService.startProcessInstanceByKey("&lt;caret&gt;")</code> → your process keys.</li>
          <li><code>cmmnRuntimeService.createCaseInstanceBuilder().caseDefinitionKey("&lt;caret&gt;")</code> → your case keys.</li>
          <li><code>dataObjectRuntimeService.createDataObjectInstanceQuery().definitionKey(KEY).operation("&lt;caret&gt;")</code>
              → that data object's operations; <code>.value("&lt;caret&gt;", …)</code> → the operation's fields.</li>
          <li>Press completion twice to list <b>all</b> model keys of every type, regardless of the call site.</li>
        </ul>

        <p>Completion fires inside string literals and at bare arguments alike (inserting a quoted key),
        with Flowable keys ranked to the top — no setup beyond opening a project that contains your
        Flowable models.</p>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.21.0</h3>
        <ul>
          <li><b>Every node type has a face.</b> A process, a form, a service and forty other kinds of node
              were told apart by a coloured dot, and forty hues nobody can tell apart is no distinction at
              all. Every place a node appears — the sidebar, the browse list, the chips, the detail tabs,
              the breadcrumb, the search results — now carries a small stroke icon for its type
              ([Lucide](https://lucide.dev), ISC) in the type's colour, and in the collapsed sidebar rail
              the icons are the navigation. They scale with <code>A−</code> / <code>A+</code> like the text
              beside them. In the same pass every label that names a section or a column speaks in one voice
              — Geist, small, semibold, tracked capitals — and monospace is reserved for what a machine
              reads: keys, paths, code, expressions. Section headings, list heads, table headers, pills and
              filter chips used to pick their own face and tracking, and the sidebar footer read like a
              terminal.</li>
          <li><b>The chrome gets out of the way.</b> The top bar and the logo row are 48px instead of 64,
              the search field and the buttons sized to match, and there is one theme toggle — in the top
              bar — instead of one there and another in the sidebar footer. That footer is two rows on a
              grid now (project · parse-issue chip / <i>Atlas 0.21.0 · 3 days ago</i> · <code>A−</code>
              <code>A+</code>): the old single row overflowed the 240px sidebar as soon as the version stamp
              grew a date, squeezing the project name to one letter and pushing the buttons out over the
              page. A node's detail page opens with one sticky bar — its kind on the left, <i>back</i> /
              <i>expand all</i> / <i>copy link</i> on the right — instead of a row of buttons above a kind
              chip above the title. Smaller things: the attribute grid no longer shows a grey box where its
              last row runs out of cells, list rows no longer animate in on every re-render, and the last
              five hard-coded font sizes now follow <code>A−</code> / <code>A+</code> like everything
              else.</li>
          <li><b>The sidebar folds, and fits a narrow window.</b> Every group header — Models, Integration,
              Code, Expressions, Checks, Variables, Access, Other — is a button that folds its entries, and
              the fold is remembered, so a project with forty variable scopes need not show them on every
              visit. A folded group whose entry is the active one marks that on its header and stays folded;
              the sidebar never reopens itself behind your back. The keyboard walks it too: <code>↑</code>
              <code>↓</code> skip folded entries, <code>←</code> folds the group you are in, <code>→</code>
              unfolds a header. Below 800px — a split editor, a narrow tool window — the category list used
              to wrap into fourteen rows of chips above the content; it is a picker beside the search button
              now, one row. The UI test covers both: the desktop run folds and re-renders, a second run at
              800×600 picks a category.</li>
        </ul>
        <h3>0.20.0</h3>
        <ul>
          <li><b>A model's "Uses — variables &amp; expressions" section is back.</b> The generator strips
              the <code>_uses</code> map from the explorer payload — correctly, it is a byte-for-byte
              transpose of the <code>usedBy</code> lists every variable, expression, binding, string
              literal, custom function and service operation already carries — but the detail panel still
              read that key, so for a whole run of releases no process, case or form listed what it touches.
              The page now rebuilds the map from those <code>usedBy</code> lists on first use; the payload
              did not grow by a byte, and the release build fails if the section's builder ever goes
              missing.</li>
          <li><b>A reference lands on the model of its own type.</b> The graph looked every target up by key
              alone, first model registered wins — so with a process and a form both called
              <code>orderX</code>, a <code>formKey</code> of <code>orderX</code> drew a clean process →
              process edge, although the resolver had already worked out the right type. The resolved type
              now travels with the reference and the edge follows it; the key-only map is the fallback, not
              the rule. In the same pass, two models of different types sharing one key both survive — a
              form and a page, or a query and a template, live in one result bucket and the second used to
              be dropped as a "duplicate" without a word — and the shared key is reported once, as a
              <code>parseIssues</code> <i>warning</i> naming both files, because a key-only lookup (and the
              harvested variables and expressions) can still only go to one of them.</li>
          <li><b>A file with several processes credits each one with its own text.</b> The raw-text harvests
              — every <code>${…}</code> and <code>{{…}}</code>, every <code>${bean.method()}</code> call,
              every declared or mapped variable — worked on the whole file and attributed it to every model
              in it, so a deployment <code>.bpmn20.xml</code> holding two processes gave each the other's
              expressions and variables: <code>usedBy</code> inflated, a bean-call edge from a process that
              never calls the bean, and a variable written in one process and read in the next judged in the
              wrong scope. Each process, case or decision now gets the text inside its own element; only
              what stands outside all of them — the definitions header, its messages and signals — still
              belongs to every one.</li>
          <li><b>Nothing is dropped silently, and an archive inside an archive is read.</b> A Design export
              that packs one <code>.bar</code> per app produced a clean run with zero models: the inner
              archive matched no model extension and was skipped without a word. It is opened now, one level
              down, and its models carry a <code>export.zip!apps/inner.bar!processes/x.bpmn</code> label the
              diagram renderers resolve too. Everything else Atlas decides not to read leaves a
              <code>skip</code> diagnostic behind, at warning level because nothing failed — a file with a
              model extension that is not JSON at all (a Helm chart's <code>_helpers.tpl</code> used to be a
              parse <i>error</i>, and its <code>{{ }}</code> were harvested as frontend bindings), a JSON in
              an export that is no model wrapper, a legacy wrapper without a body or of a type Atlas has no
              parser for, a process in the old editor's JSON format whose XML twin never turned up, an
              archive nested two levels deep, a model file above a 32 MB limit — where each of those used to
              be indistinguishable from an empty project. An unreadable Java source costs that one file
              instead of aborting the run with a stack trace, and a model whose diagram could not be
              produced says so on its page (<code>diagramError</code>) and on the CLI, instead of looking
              like a model that simply has no layout.</li>
          <li><b>Test code is not the project.</b> Java under <code>src/test</code>,
              <code>src/integrationTest</code> and any other test source set is no longer scanned: a JUnit
              class calling <code>setVariable("foo", …)</code> registered a production write, so a variable
              only a test ever set could be reported as written-but-never-read, and a test
              <code>@RestController</code> became an endpoint the models could supposedly reach. Models
              under <code>src/test/resources</code> are still read — a test process is a model somebody has
              to keep in step.</li>
          <li><b><code>\${…}</code> is not an expression, and "not judged" is no longer silent.</b> A
              backslash before the dollar sign — in a Groovy string, a Java string, a JSON body — means
              <i>literal, do not evaluate</i>; Atlas harvested it like any other expression and validated
              it, which could only ever come out wrong. It is skipped now. And an expression whose harvested
              body still holds a <code>{</code> (the harvester may have cut it short at the first
              <code>}</code>) gets no verdict, as before — but it is counted
              (<code>stats.exprSkippedNested</code>) and its page says <i>not validated</i> and why, so an
              unjudged expression cannot pass for a clean one.</li>
          <li><b>The explorer fails loudly, and its links cannot freeze it.</b> Any error during the page's
              boot — a truncated data island, a malformed node — left the <i>Loading…</i> overlay up forever
              with no word; the overlay now shows the error and what to do about it. A malformed
              <code>%</code> in a hand-edited or truncated link threw out of the router on every navigation,
              freezing the page for good; an undecodable part resolves to the overview instead. Following a
              chip out of a filtered list into another category carried the filter text along and often read
              <i>Nothing here</i>; the filter is per category. When a followed link pushes the tab strip
              past twelve, the tab that made room is named — the other opening path always said so, this one
              destroyed the oldest tab silently. Recents are kept per report, so two reports on one machine
              no longer eat each other's eight slots. An agent page no longer reads <i>Vendor / model: /</i>
              or <i>API endpoint: undefined</i>, a custom function without a namespace no longer reads
              <code>namespace undefined.*</code>, an endpoint without a handler no longer shows a bare
              <code>#</code>. The Checks page's <i>open the list</i> for unused operations and unused custom
              functions opens a review list of exactly those, instead of the full category.</li>
          <li><b>Shortcuts, copying and the fullscreen diagram stop getting in each other's way.</b> Alt+←/→
              and Alt+W cycled and closed detail tabs even while the cursor sat in the list filter — on a
              Mac that is word-wise caret movement — and now leave text fields alone. <i>copy link</i> was
              the one copy button that bypassed the IDE's clipboard bridge and fell through to a prompt
              inside the embedded viewer. ⌘K opened the search palette <i>underneath</i> the fullscreen
              diagram, so you typed into an invisible input; searching leaves full screen first.</li>
          <li><b>Diagrams re-fit, remember the element in the link, and work from the keyboard.</b> The
              drawing kept its scale when the panel changed width — small in a tall box, or clipped in a
              narrower IDE tool window — and re-fits now unless you zoomed by hand; full screen fits the
              height too. Clicking an element puts it in the link
              (<code>#&lt;node&gt;&amp;e=&lt;element&gt;</code>), and following such a link locates the
              element on the canvas, not only in its rows. Every shape is a keyboard stop with a name for a
              screen reader, and Enter or Space opens its card. A cancelled drag no longer leaves the
              diagram panning with no button held, and each info card's size observer is disconnected with
              the card instead of piling up.</li>
          <li><b>The CLI can fail a build.</b> Every run exited 0 whatever it found, so Atlas could describe
              a broken project but never stop one — and none of the four reference projects had a model
              check in CI. <code>--fail-on error</code>, <code>--fail-on warning</code>, or a list of check
              ids (<code>--fail-on missingRefs,invalidExpr</code>) makes the run exit 1 when a finding
              matches, <i>after</i> writing every artifact, so a pipeline gets the report and the red build;
              an unknown value is a misuse. Three smaller truths on the way: <code>java -jar … --help</code>
              prints the usage instead of exit 2 (only the launcher had a help), <code>-v</code> lists the
              parse issues the status line has been counting all along instead of being parsed and never
              read, and <code>--all --slice</code> is the argument error every other flag conflict already
              was rather than a silent win for <code>--all</code>.</li>
          <li><b>From the explorer straight into the code.</b> Inside IntelliJ the embedded explorer offered
              a clipboard bridge and nothing else, so the source path it shows for every model and Java
              class, and the line it shows for every method and REST handler, could be copied but not
              followed. The plugin now injects a second bridge: <code>↗</code> beside a source path opens
              the file in an editor tab, a <code>:line</code> opens it at that line, a model inside a
              <code>.bar</code> opens read-only from the archive. Read the model here, edit the code there —
              the seam Atlas is built on, one click wide. The same page in a browser shows none of it,
              because it could not honour the click.</li>
          <li><b>A reload brings the list back as you left it.</b> The link carried the view, the category,
              the node and the search term — not the filter you had typed nor the sort you had picked, so
              coming back to a report mid-investigation cost you the whole setup. Both travel in the link
              now (<code>&amp;f=</code>, <code>&amp;s=</code>), written as you type or pick without a
              history entry, and a category change resets them instead of carrying "Most referenced"
              silently into the next list, where it had been overriding the relevance ranking of every later
              filter.</li>
          <li><b>Text size, and a list you can resize.</b> <code>A−</code> / <code>A+</code> in the sidebar
              footer step every font size on the page between 85 % and 150 % — the IDE's embedded browser
              applies none of the IDE's font scaling, so the 10–11 px metadata stayed 10–11 px on a dense
              monitor. The split between the list and the detail panel, fixed at 330 px, is a drag handle
              now (arrow keys nudge it, Home resets it) and is remembered.</li>
          <li><b>A report says when it was made.</b> The explorer's payload carried the project name, the
              graph and the counts — not the moment it was generated, so a page mailed to a reviewer could
              be a day or six months old and could not say. The page now carries <code>generatedAt</code>
              and <code>atlasVersion</code>; the sidebar footer reads <i>Atlas 0.20.0 · generated 3 days
              ago</i> with the exact time on hover, and <code>graph.json</code> gains a
              <code>_generated</code> block beside <code>_schema</code> for whatever reads it by
              machine.</li>
          <li><b><code>@Bean</code> factory methods are beans, and Kotlin sources are read.</b> Bean
              resolution knew only the stereotype annotations on a class — <code>@Component</code>,
              <code>@Service</code>, <code>@Repository</code>, <code>@Named</code> — so a delegate
              registered the standard way for code you do not own, a <code>@Bean</code> method in a
              <code>@Configuration</code> class, was invisible: every model naming it resolved to nothing.
              Such a bean now resolves to <i>its method's line</i>, named after the method unless the
              annotation says otherwise. And <code>.kt</code> files go through the same pass as
              <code>.java</code> — package, <code>class</code>/<code>object</code>/<code>enum class</code>,
              the supertype list after the primary constructor, <code>fun</code>,
              <code>val</code>/<code>var</code> properties and <code>const val</code> constants are all read
              — so a Kotlin <code>JavaDelegate</code> or <code>@RestController</code> is a real node with
              real edges instead of an unresolved external. A bean name two classes both claim resolves, but
              the edge is flagged suspect, as an ambiguous class name always was.</li>
          <li><b>The model index has one build policy, and it no longer freezes the editor.</b> The index
              was built by whoever asked first — and when that was the unused-declaration inspection, a
              reference resolve or Find Usages, the whole model scan ran under the daemon's read lock, which
              on a large repository is "the IDE freezes while I type". Five other consumers each launched
              their own background build on a cold index, two never asked for one at all, and nothing built
              it when a project opened, so the Atlas Hub greeted you with <i>Not scanned yet</i> until you
              clicked Rebuild or happened to open a Java file. Now: the index starts building when the
              project opens; every read-context consumer reads the cache and calls
              <code>ensureBuilding()</code> — one background build for any number of askers — and answers
              "nothing yet" until it lands, at which point the editor's markers, hints and inspections are
              re-run. Only completion and the explicit actions may wait for a build. The Hub says
              <i>Scanning the project…</i> and resolves itself.</li>
          <li><b>Inspections stop re-reading the model files per literal.</b> The key inspections rebuilt
              the set of known keys for every literal they looked at, the value-field inspection re-read and
              re-parsed the backing <code>.service</code> model for every <code>value("…")</code> in a file
              — a DAO with twenty query builders parsed the same JSON twenty times per highlighting pass —
              and the Liquibase coverage inspection parsed every <code>.service</code> model in the project
              for every changelog. All of that is now computed once per index snapshot and dropped with
              it.</li>
          <li><b>The settings pages say what they do.</b> Unticking every artifact on the Generation page
              left an empty selection — the checkboxes mutated the set in place and skipped the setter where
              "an empty selection falls back to the explorer HTML" lives — so <i>Generate Atlas
              Explorer…</i> wrote nothing and reported success; the page writes through the setter now. The
              four folder fields documented as project-relative wrote an absolute path whenever the browse
              button was used, which a pull then resolved outside the repository; the button writes the
              folder relative to the active project. The constants class name is validated as you type, and
              renaming it no longer silently ends auto-refresh — the file generated under the old name is
              named, with a <i>Generate now</i> action for the new one. The inlay-hint, key-recognition and
              Java-expression toggles re-run highlighting when applied instead of waiting for you to type
              into every open file, and the "invalid class name" balloon opens the page the class name is
              actually on.</li>
          <li><b>A stale explorer says so — after any change, not only a Design pull.</b> The Hub's "models
              changed since the last generation" row compared the page against the last pull alone, and only
              with a Design connection configured, so a team that gets its models through git never saw it,
              and the explorer tab itself never said anything. The index now carries the newest modification
              time of the models and archives it scanned; the Hub row compares against that (or the pull,
              whichever is newer) with no connection required, and an open explorer tab shows a banner with
              <i>Regenerate</i> on it. Both clear themselves when the page is regenerated.</li>
          <li><b>A pull says what it brought, not only what disappeared.</b> The post-pull balloon named the
              model keys that vanished project-wide — the code-impact signal — and nothing else, so "what
              did I just pull?" had no answer. Each pulled app now reports how many of its model files
              changed, were added or removed against the export that was on disk before (by content, entry
              by entry), naming them when there are few; a first export says so. The progress bar advances
              per app instead of sitting still for the whole download, and a pull refused with HTTP 401 —
              the expired token, the changed password — offers <i>Sign out &amp; retry</i>, which clears the
              stored credential, opens the connection to sign in, and pulls again.</li>
          <li><b>One shortcut, and a context menu on the models.</b> The reference page said it plainly: no
              action had a shortcut and there was no context menu, the Hub being the surface. That stays
              true for everything except the two that earn an exception: <b>Search Models…</b> has
              <code>Ctrl+Alt+Shift+M</code> (<code>⌥⇧⌘M</code>), because it is the one action that competes
              with Shift-Shift for the hand, and a right-click on a folder, a model file or an archive in
              the Project view offers <i>Generate Atlas Explorer…</i> and <i>Search Models…</i> — on nothing
              else, so the menu stays as short as it was on every other file.</li>
          <li><b>One scope for everything, and the Hub says what it could not read.</b> With a sub-project
              chosen in a monorepo, the model index narrowed itself to it — but the Search Everywhere tab's
              full-text half, Find Usages into models and the REST-endpoint gutter still walked the whole
              repository, so one query answered from two scopes. Every walk goes through one definition now.
              The Hub's index line names the scope when it is narrower than the repository, and lists the
              <code>.bar</code>/<code>.zip</code> archives in scope it could not open — which were logged at
              debug level, leaving a repository whose only archive is unreadable looking exactly like one
              with no models.</li>
          <li><b>Generating the explorer opens it.</b> The first generation ended in a balloon whose <i>Open
              in IDE</i> action expired with the balloon; the page you asked for now opens as a tab the
              moment it is written — from the menu action as well as from the editor's Regenerate. In the
              Hub, <i>Open in Browser</i> is hidden while there is nothing to open instead of quietly
              turning into the generate dialog, and a double-click on an artifact where neither an embedded
              nor an external browser exists (a Remote Dev backend) says so rather than doing nothing.</li>
          <li><b>Menu actions stay usable while the IDE indexes, and never freeze it.</b> Every Atlas action
              greyed out in dumb mode — right after opening a project, when <i>Pull from Design</i> and
              <i>Rebuild Model Index</i> are what you want and neither needs the IDE's indices; the actions
              that need no PSI are <code>DumbAware</code> now. <i>Open Atlas Explorer</i> walked the project
              six levels deep on the UI thread when the output folder was empty (a visible freeze on a cold
              monorepo) — it searches in the background. The generation-failure log opens as an in-memory
              tab instead of a temp file written on the UI thread, sub-project detection runs once at a time
              instead of once per Hub event, and two dead fields left the Hub.</li>
          <li><b>The error reporter is finally in the IDE.</b> 0.13.0 announced <i>Report a problem straight
              from the error dialog</i>, and the reporter was there — but never registered in the plugin
              descriptor, so the button never appeared and the reference page had to carry a "known gap"
              paragraph. It is registered now: <b>Report Flowable Atlas Problem…</b> copies the report to
              the clipboard and opens the issue tracker, transmitting nothing on its own. The reference page
              also stops claiming 2026.1 support: 2026.2 has been the floor since 0.17.</li>
          <li><b>Two <code>overview.md</code> lines stop leaking the generator's internals.</b> An AI
              agent's heading read <code>(None)</code> when the model states no type, and its model line
              <code>model: None/None (temp None)</code> when the settings are absent — both omit what is
              unset now. A data dictionary's line printed its types as a Python list literal, <code>types:
              ['Address', 'Customer']</code>; it is a code list. Both surfaced the moment the test fixture
              gained an agent and a dictionary — it now carries every model type the parsers know, plus a
              Design export with a <code>.bar</code> nested inside it.</li>
          <li><b>An expanded sub-process no longer wears the collapsed <code>[+]</code> marker.</b> The
              diagram painted it on every sub-process, over the children an expanded one lays out inside
              itself. It is read from the diagram interchange now (<code>isExpanded="false"</code>, or
              Design's collapsed stencil) and drawn only there.</li>
        </ul>
        <h3>0.19.0</h3>
        <ul>
          <li><b>Ten model types stop being name-only stubs</b> — queries, sequences, SLAs, templates,
              knowledge bases, variable extractors and document (content) models are parsed structurally: a
              query's parameters, sort keys and search-template body; a sequence's number format and
              counters; an SLA's due-date targets, escalations and lifecycle actions (a
              start-process/start-case escalation is a real model reference now); a template's variations
              and their actual text; a knowledge base's retrieval settings (credentials never leave the
              model — only their <i>type</i> is kept); a variable extractor's extracted variables (each an
              honest "write whose readers are out of reach"); a document model's per-action forms,
              permissions and variables. Palettes keep their <code>Palette-Id</code>/<code>title</code>
              identity instead of coming out as <code>key: None</code>. Master data and dashboard components
              stay generic for now — no corpus in reach contains a body to design against.</li>
          <li><b>What the parser knows, the page shows — structurally guaranteed.</b> Every parsed attribute
              now either has a renderer or lands in a collapsed <b>Other attributes</b> key/value tree on
              the detail page, tracked at render time, so a future parser field is visible by default. The
              Kotlin mirror: <code>PayloadCompletenessTest</code> fails the build when a parser emits a
              container key the explorer payload would silently drop — each key must be allowlisted (visible
              + searchable) or consciously stripped with a reason. Six parsed-but-invisible keys render now:
              a process's full <b>sequence-flow topology</b> (default flows marked), its declared <b>data
              objects</b>, its model-level <b>references</b> (SLA, security policy, event, channel,
              dictionary, sequence), an app's <b>pages</b>, a decision service's <b>decisions</b>, a form's
              <b>subforms</b>.</li>
          <li><b>BPMN/CMMN extraction closes its attribute gaps</b> — boundary events name the activity they
              hang on (and whether they interrupt it), conditional events keep their condition, gateways and
              flows keep the default-flow marker, <code>flowable:async</code>/<code>skipExpression</code>
              surface when set, lanes are extracted (and searchable via
              <code>label:</code>/<code>id:</code>), event payloads carry type/required/correlation instead
              of bare names, DMN columns keep their declared types and allowed values, service-operation
              parameters keep <code>required</code> and defaults, data-dictionary types keep their
              properties, form selects keep their options and every localised caption (each searchable as a
              <code>label:</code>), and agent prompts are no longer cut off at 200 characters.</li>
          <li><b>Search: a facet hit lights up what it matched</b> — the bound value of <code>label:</code>
              / <code>desc:</code> / <code>key:</code> / <code>id:</code> highlights in the result rows like
              any term, and the row's hint leads with the matched text itself (<code>label · Recalculate
              @orderTotal</code>). The detail page highlights faceted and multi-word queries too — it used
              to look for the raw query as one substring, so exactly the queries the engine is best at
              highlighted nothing there. "Did you mean" matches each word on its own and reaches captions,
              not just names and keys.</li>
          <li><b>The browse list explains its hits</b> — a row matched through a script body or a mapping
              says why (<code>script · stampTask</code>), exactly like the palette, and clicking it opens
              the detail panel with that element revealed and highlighted.</li>
          <li><b>The search dialog is keyboard-complete</b> — Tab cycles the dialog's own controls (facet
              chips, their ×, "Show more", "Did you mean") instead of being swallowed, the page behind the
              open dialog is inert to focus and screen readers, arrows move the selection without rebuilding
              the whole list (noticeable on 3000-node reports), and the <code>/</code> shortcut is finally
              written down — on the search button, in the zero-result tip and on the empty detail
              panel.</li>
        </ul>
        <h3>0.18.1</h3>
        <ul>
          <li><b>Labels and descriptions are searchable, and ranked as what they are</b> — a form field's
              label, a data object column's label, an outcome button's caption, a permission's label, a
              BPMN/CMMN element's name and a decision table's column headers are one ranked field
              (<code>label:</code>), just under the node's own name; the prose somebody wrote <i>about</i> a
              thing is another (<code>desc:</code>, also spelled <code>description:</code> /
              <code>doc:</code>) — Design's model <b>Description</b>, <code>documentation</code> on a
              process/case <i>and</i> on each of its elements, a form component's description, a DMN rule's
              annotation. Both were reachable only through the free-text walk before, at the same weight as
              a script body, so searching for a caption a user reads on screen ranked below any script that
              happened to mention the word. Both are collected by field <i>name</i> during the walk, so a
              label a parser starts emitting somewhere new is searchable without a change to the
              engine.</li>
          <li><b>A facet is a filter: it answers with all of them, and with nothing else</b> — you have
              named the field, so <code>label:</code> matches captions only. The display name of a variable,
              an expression, a binding, a string literal, a Java class, a method, a REST endpoint, a
              changelog, a worker topic or a group is an identifier Atlas synthesised out of something else,
              not a caption anybody wrote; those stay findable by name, key and free text, but they are not
              labels. A space after the colon is fine — <code>desc: approval</code> is the same query as
              <code>desc:approval</code> — and nothing about a query is case-sensitive: not the terms, not
              the facet name, not its value. The row says <i>why</i> it matched with a field of the kind you
              asked for, and <code>label:save</code> opens the field that reads "Save" rather than leaving
              you on the form.</li>
          <li><b>Design's model Description is no longer thrown away</b> — every parser built its own record
              and only the app parser kept the <code>description</code> the modeller wrote, so for a
              process, case, form, page, service, data object, action, agent, channel, event, dictionary,
              policy or decision it never reached the report: not shown, not searchable. It is carried
              through now and shown at the top of the detail panel for every model type that has one.</li>
          <li><b>A form in a Design workspace export has its fields</b> — Design persists a model's body as
              an escaped JSON <i>string</i> (<code>editorJson</code>), and a form's components are reached
              by walking maps, so the string was never opened: every form and page exported that way came
              out with an empty field list. No ids, no labels, no descriptions, no outcomes. A
              <code>.form</code> from an app zip or a deployment bar was never affected, and neither was an
              export that happens to nest <code>editorJson</code> as an object, which is why it survived
              this long. The model's own metadata header — including its description — is kept rather than
              overwritten.</li>
          <li><b>The result page is shared out across the sections</b> — searching for a form field's
              caption also matches whatever else carries the word, and a page cut off purely by score could
              fill itself with one kind and leave the group holding the answer undrawn. Every section with
              hits now gets a share of the page, and a section that runs out leaves its share to the others,
              so a result that really is all one kind still fills the page with it.</li>
          <li><b><code>scripts/search-diagnose.mjs</code></b> — point it at an existing report and a query
              and it separates the three things a search failure can be: the string never reached the
              report, it is there but does not match, or it matches and the page does not draw it. It uses
              the engine embedded in <i>that</i> report, so it diagnoses the version in the file rather than
              the checkout's. Developer tool; not shipped in the plugin or the CLI.</li>
          <li><b>A facet takes a quoted value</b> — <code>label: "Customer name"</code> is the only way to
              ask a facet for a multi-word caption, and it fell apart: the phrase pass stripped the quotes
              before the facet pass ran, the colon was left with nothing to bind, and the word
              <code>label</code> degraded into a free-text term — the query answered with whichever nodes
              happened to <i>mention</i> the word "label" instead of everything that carries the caption.
              The quoted value now binds to its facet first, stays contiguous like any phrase, and works
              spaced, unspaced and in any casing. (<code>"label: thing"</code> entirely inside quotes is
              still a literal phrase.)</li>
          <li><b>The palette teaches its own filters</b> — before you type, the bar under the input offers
              one chip per facet (<code>label:</code>, <code>desc:</code>, <code>key:</code>,
              <code>id:</code>, <code>type:</code>, <code>file:</code>, <code>in:</code>), each glossed with
              what it searches; clicking one types the prefix for you. A facet typed through the colon but
              not given a value yet says what it is waiting for instead of searching for the word
              <code>label</code>. And every facet that binds shows as a lit chip beside the result count —
              proof the filter took effect, a reminder it is still on, and, when clicked, the way to remove
              it from the query.</li>
        </ul>
        <p>Older releases: see <code>CHANGELOG.md</code> in the repository.</p>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>
    <!-- XML PSI for the Liquibase-changelog coverage inspection. -->
    <depends>com.intellij.modules.xml</depends>
    <!-- JSON PSI for form-model {{…}} expression injection and the .form → JSON association. -->
    <depends>com.intellij.modules.json</depends>
    <!-- JCEF (Atlas Hub, the explorer editor, the Inspect sign-in browser). Up to 2026.1 JCEF sat in the
         platform core; 2026.2 (build 262, the floor since the 2026.2 move) has it in the bundled
         "Web Browser (JCEF)" plugin (id com.intellij.modules.jcef), which is also why :idea-plugin has to
         put that plugin on its compile classpath. Still declared *optional*: someone who disables the
         bundled plugin, or an IDE without JCEF at all (remote dev), should lose the browser panels rather
         than the whole plugin — every call site is guarded by JBCefApp.isSupported() for exactly that. -->
    <depends optional="true" config-file="flowable-atlas-jcef.xml">com.intellij.modules.jcef</depends>

    <resource-bundle>messages.FlowableAtlasBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Services (FlowableModelIndexService, ModelConstantsService/Settings) are @Service — no registration needed. -->
        <!-- Settings tree: an app-level root page (core toggles) with three project-level children.
             parentId chaining across levels is supported — the settings tree merges them by ID. -->
        <applicationConfigurable
                parentId="tools"
                id="com.flowable.atlas.settings"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas"
                instance="com.flowable.atlas.settings.FlowableAtlasConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.expressions"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.expressions"
                instance="com.flowable.atlas.settings.ExpressionsConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.generation"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation"
                instance="com.flowable.atlas.settings.GenerationConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings.generation"
                id="com.flowable.atlas.settings.generation.constants"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation.constants"
                instance="com.flowable.atlas.settings.GenerationConstantsConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings.generation"
                id="com.flowable.atlas.settings.generation.liquibase"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation.liquibase"
                instance="com.flowable.atlas.settings.GenerationLiquibaseConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings.generation"
                id="com.flowable.atlas.settings.generation.dto"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation.dto"
                instance="com.flowable.atlas.settings.GenerationDtoConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.design"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.design"
                instance="com.flowable.atlas.settings.DesignConfigurable"/>

        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.environments"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.environments"
                instance="com.flowable.atlas.settings.EnvironmentsConfigurable"/>
        <completion.contributor
                language="JAVA"
                implementationClass="com.flowable.atlas.completion.FlowableJavaCompletionContributor"/>
        <!-- Liquibase changelog: complete <column name="…"> / tableName from the backing service model. -->
        <completion.contributor
                language="XML"
                implementationClass="com.flowable.atlas.liquibase.LiquibaseColumnCompletionContributor"/>
        <!-- BPMN/CMMN model XML: complete model keys at cross-reference attributes
             (calledElement, flowable:formKey, decisionRef, caseRef, processRef, …). -->
        <completion.contributor
                language="XML"
                implementationClass="com.flowable.atlas.completion.FlowableXmlKeyCompletionContributor"/>
        <localInspection
                language="JAVA"
                shortName="FlowableValueField"
                displayName="Invalid Flowable data-object value field"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableValueFieldInspection"/>
        <localInspection
                language="JAVA"
                shortName="FlowableBrokenKey"
                displayName="Unknown Flowable model key"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableBrokenKeyInspection"/>
        <!-- BPMN/CMMN model XML: an unknown model key at a cross-reference attribute (broken deployment). -->
        <localInspection
                language="XML"
                shortName="FlowableXmlBrokenKey"
                displayName="Unknown Flowable model key (model XML)"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableXmlBrokenKeyInspection"/>
        <!-- Ctrl-click a key literal → jump to the model file; Find Usages works too. -->
        <psi.referenceContributor
                language="JAVA"
                implementation="com.flowable.atlas.navigation.FlowableKeyReferenceContributor"/>
        <!-- Ctrl-click a model key in BPMN/CMMN XML (calledElement, formKey, decisionRef, …) → the model file. -->
        <psi.referenceContributor
                language="XML"
                implementation="com.flowable.atlas.navigation.FlowableXmlKeyReferenceContributor"/>
        <!-- Hover / Ctrl-Q on a key literal → model type, name and file. -->
        <lang.documentationProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.navigation.FlowableKeyDocumentationProvider"/>
        <!-- Search Everywhere / Go to Symbol: every model key (→ model file) and every bot key
             (→ the BotService class and the actions that invoke it). -->
        <gotoSymbolContributor
                implementation="com.flowable.atlas.navigation.FlowableKeyGotoSymbolContributor"/>
        <!-- A "Flowable Model" tab of its own in Search Everywhere: model keys and the file paths
             inside a .bar/.zip (invisible to the Files tab), plus a live full-text search over model
             content (invisible to Find in Files). The contributor above stays — it feeds the
             platform's Symbols tab, this feeds its own. -->
        <searchEverywhereContributor
                implementation="com.flowable.atlas.navigation.se.FlowableModelSeContributor$Factory"/>
        <!-- Liquibase changelog column not mapped in the backing Flowable service/data-object model. -->
        <localInspection
                language="XML"
                shortName="FlowableLiquibaseCoverage"
                displayName="Liquibase column not defined in Flowable model"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.liquibase.LiquibaseCoverageInspection"/>
        <!-- Alt-Enter on a data-object definitionKey → generate a typed Java bean from its fields. -->
        <intentionAction>
            <language>JAVA</language>
            <className>com.flowable.atlas.intention.GenerateDataObjectBeanIntention</className>
            <category>Flowable</category>
        </intentionAction>
        <!-- Don't report Java referenced from models (delegates, ${bean.method()}, listeners) as unused. -->
        <implicitUsageProvider implementation="com.flowable.atlas.usage.FlowableImplicitUsageProvider"/>
        <!-- Find Usages / Ctrl-B on a delegate class or ${bean.method()} shows the model files that use it. -->
        <customUsageSearcher implementation="com.flowable.atlas.usage.FlowableModelUsageSearcher"/>
        <!-- Gutter icon on Java methods/beans referenced from Flowable models → navigate to the model usages. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableModelReferenceLineMarkerProvider"/>
        <!-- Gutter icon on a BotService class → navigate to the .action models that invoke the bot. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableBotActionLineMarkerProvider"/>
        <!-- Gutter icon on a @GetMapping/@PostMapping/… handler → navigate to the models calling its endpoint. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableEndpointUsageLineMarkerProvider"/>
        <!-- Gutter icon on a model-key expression — a literal or a constant/local reference at a key
             call site — whose model has a diagram (bundled Design .svg, or rendered from its DI layout)
             → open it in the IDE's built-in image/SVG viewer. No diagram (e.g. a form BAR) → no marker. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableDiagramLineMarkerProvider"/>
        <!-- Inline hint: the physical table name after a string literal that is a data-object key. -->
        <codeInsight.declarativeInlayProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.hint.FlowableDataObjectTableInlayProvider"
                isEnabledByDefault="true"
                group="VALUES_GROUP"
                providerId="com.flowable.atlas.dataObjectTable"
                bundle="messages.FlowableAtlasBundle"
                nameKey="inlay.dataObjectTable.name"
                descriptionKey="inlay.dataObjectTable.description"/>
        <!-- Inline hint: the action model's name after a string literal that is an .action key. -->
        <codeInsight.declarativeInlayProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.hint.FlowableActionNameInlayProvider"
                isEnabledByDefault="true"
                group="VALUES_GROUP"
                providerId="com.flowable.atlas.actionName"
                bundle="messages.FlowableAtlasBundle"
                nameKey="inlay.actionName.name"
                descriptionKey="inlay.actionName.description"/>
        <!-- Warn when renaming a Java symbol referenced by Flowable model expressions (text, not auto-updated). -->
        <refactoring.elementListenerProvider
                implementation="com.flowable.atlas.usage.FlowableRenameWarningProvider"/>
        <!-- Balloon shown after generating the Atlas explorer (open in browser / open in IDE). -->
        <notificationGroup id="Flowable Atlas" displayType="BALLOON"/>
        <!-- Render a *.explorer.html as an embedded (JCEF) "Atlas Explorer" tab in the center editor
             area (like a DB table); hides the raw-HTML "Text" tab for it. -->
        <fileEditorProvider implementation="com.flowable.atlas.explorer.AtlasFileEditorProvider"/>
        <!-- "Models changed since this explorer was generated" banner above the embedded page. -->
        <editorNotificationProvider implementation="com.flowable.atlas.explorer.AtlasExplorerStaleNotificationProvider"/>
        <!-- Second tab on the same *.explorer.html: the Flowable Expressions playground, so the map and
             the playground share one window (no separate tool window needed). Placed after Atlas Explorer. -->
        <fileEditorProvider implementation="com.flowable.atlas.explorer.AtlasExpressionEditorProvider"/>

        <!-- ==================== Flowable Expression support ==================== -->
        <!-- Two dialects of one expression language: backend ${…}/#{…} (JUEL) and frontend {{…}} (flw.*). -->
        <fileType name="Flowable Backend Expression"
                  language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableBackendExprFileType"
                  fieldName="INSTANCE"
                  extensions="flowable-be"/>
        <fileType name="Flowable Frontend Expression"
                  language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableFrontendExprFileType"
                  fieldName="INSTANCE"
                  extensions="flowable-fe"/>
        <lang.parserDefinition language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableBackendExprParserDefinition"/>
        <lang.parserDefinition language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableFrontendExprParserDefinition"/>
        <!-- Token coloring (parentheses, brackets, strings, operators, …) for both dialects. -->
        <lang.syntaxHighlighterFactory language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprSyntaxHighlighterFactory"/>
        <lang.syntaxHighlighterFactory language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprSyntaxHighlighterFactory"/>
        <!-- Match () and [] — caret on a brace highlights its partner, unmatched braces are flagged. -->
        <lang.braceMatcher language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprBraceMatcher"/>
        <lang.braceMatcher language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprBraceMatcher"/>
        <!-- Structural syntax squiggles for both dialects (playground + injected fragments). -->
        <annotator language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.annotator.FlowableExpressionAnnotator"/>
        <annotator language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.annotator.FlowableExpressionAnnotator"/>
        <!-- Semantic expression findings as real inspections: severity/enablement per profile, project
             allowlist via quick fix. No language attribute — the two dialects share no registered base
             language; the implementations bail out immediately on any non-expression file. -->
        <localInspection
                shortName="FlowableExprUnknownFunction"
                displayName="Unknown Flowable expression function or namespace"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.expr.inspection.FlowableExprUnknownFunctionInspection"/>
        <localInspection
                shortName="FlowableExprGrounding"
                displayName="Expression root is not a known variable, bean, or root object"
                groupName="Flowable"
                enabledByDefault="false"
                level="WEAK WARNING"
                implementationClass="com.flowable.atlas.expr.inspection.FlowableExprGroundingInspection"/>
        <!-- Rainbow parentheses are done in the syntax highlighter (FlowableExprSyntaxHighlighter):
             the highlighting lexer tags each round paren with its nesting level and each level gets
             a distinct colour. This paints in every surface, including the playground's embedded
             editor field where an annotator would not. Per-theme default colours + the Color Scheme
             page that makes them customizable: -->
        <additionalTextAttributes scheme="Default" file="colorSchemes/FlowableExprDefault.xml"/>
        <additionalTextAttributes scheme="Darcula" file="colorSchemes/FlowableExprDarcula.xml"/>
        <colorSettingsPage implementation="com.flowable.atlas.expr.lang.FlowableExprColorSettingsPage"/>
        <!-- Alt-Enter on an injected ${…}/{{…}} fragment → rework it in the playground, pre-filled.
             Registered once (no <language> filter): the same class must not be registered twice or the
             platform rejects it as a duplicate. isAvailable() self-gates on FlowableExprFile, which
             covers both the backend and frontend dialects. -->
        <intentionAction>
            <className>com.flowable.atlas.expr.intention.OpenInExpressionPlaygroundIntention</className>
            <category>Flowable</category>
        </intentionAction>
        <!-- Ctrl-Q / hover on a catalog function or root inside an expression → call head + doc line. -->
        <lang.documentationProvider language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.documentation.FlowableExprDocumentationProvider"/>
        <lang.documentationProvider language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.documentation.FlowableExprDocumentationProvider"/>
        <!-- Completion of functions, root objects and the project's variables/form-fields. -->
        <completion.contributor language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.completion.FlowableExpressionCompletionContributor"/>
        <completion.contributor language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.completion.FlowableExpressionCompletionContributor"/>
        <!-- Inject the expression languages inline wherever expressions live in the repo. -->
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableXmlExpressionInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableJsonExpressionInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableJavaExpressionInjector"/>
        <!-- Inject the IDE's real Groovy / JavaScript languages into Flowable script bodies (BPMN
             scriptTask, CMMN script tasks, execution/task listener scripts, action-bot scriptInfo):
             compiler-grade syntax errors right in the model file. Languages are resolved by ID at
             runtime (Language.findLanguageByID), so there is NO hard or optional plugin dependency —
             no Groovy/JavaScript plugin simply means no injection, never an error. -->
        <multiHostInjector implementation="com.flowable.atlas.script.inject.FlowableXmlScriptInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.script.inject.FlowableJsonScriptInjector"/>
        <!-- Completion for the Flowable script bindings (execution.…, task.…, flw.time.…): the real
             language completion knows nothing about these dynamic bindings, so the bindings catalog
             offers them — in the Script Playground and inside injected script bodies. language="any"
             because the host language varies (Groovy/JS/plain-text fallback) and this plugin has no
             dependency on the language plugins; the contributor gates itself cheaply. -->
        <completion.contributor language="any"
                  implementationClass="com.flowable.atlas.script.completion.ScriptBindingsCompletionContributor"/>
        <!-- Silence the Groovy/JS "unresolved" noise on the engine bindings (execution, flw, …)
             inside Flowable script bodies — they are dynamic bindings the language PSI can never
             resolve. Scoped to the playground + injected script fragments; real syntax errors and
             the bindings-catalog validation are unaffected. Registered per language (the suppressor
             lookup does not honor "any"); an entry for an absent language plugin is simply dormant,
             so this keeps the no-Groovy/JS-dependency rule intact. -->
        <lang.inspectionSuppressor language="Groovy"
                  implementationClass="com.flowable.atlas.script.inspection.FlowableScriptInspectionSuppressor"/>
        <lang.inspectionSuppressor language="JavaScript"
                  implementationClass="com.flowable.atlas.script.inspection.FlowableScriptInspectionSuppressor"/>
        <!-- Hover / Ctrl-Q on a binding shows catalog documentation instead of the platform's
             "No candidates found for method call" fallback for unresolvable dynamic bindings. -->
        <lang.documentationProvider language="Groovy"
                  implementationClass="com.flowable.atlas.script.documentation.ScriptBindingsDocumentationProvider"/>
        <lang.documentationProvider language="JavaScript"
                  implementationClass="com.flowable.atlas.script.documentation.ScriptBindingsDocumentationProvider"/>
        <lang.documentationProvider language="TEXT"
                  implementationClass="com.flowable.atlas.script.documentation.ScriptBindingsDocumentationProvider"/>
        <!-- The Script Playground's scratch file resolves imports against the whole project —
             without a module, `import org.slf4j.LoggerFactory` would never resolve there. -->
        <resolveScopeProvider implementation="com.flowable.atlas.script.toolwindow.ScriptPlaygroundResolveScopeProvider"/>
        <!-- Treat Flowable form models (.form) as JSON so their {{…}} strings become injection hosts,
             and action models (.action) so their scriptInfo.script strings can host Groovy/JS. The
             remaining JSON-serialized model extensions are here so a model OPENS with its content:
             an unregistered extension resolves to UnknownFileType, which is binary, so the editor
             shows the "file type not associated" panel instead — most visibly for an entry opened
             out of a .bar/.zip. A <fileType> with no implementationClass only adds matchers to the
             existing type (the same idiom the bundled javaFX and Maven plugins use). The list mirrors
             ModelType.EXTENSIONS in :core. -->
        <fileType name="JSON"
                  extensions="form;action;page;data;dictionary;query;sequence;sla;event;channel;agent;service;tpl;policy;extractor;knowledgebase;masterdata;dashboardcomponent;document;palette;app"/>
        <!-- The XML-serialized models. .bpmn20.xml / .cmmn.xml / .dmn.xml already end in .xml. -->
        <fileType name="XML" extensions="bpmn;cmmn;dmn"/>
        <!-- Reader Mode's visual formatting layer crashes on minified single-line JSON, which is what a
             Design model export is; order="last" is load-bearing, because the platform enables the layer
             from this same extension point. See FlowableModelVisualFormattingGuard. -->
        <readerModeProvider implementation="com.flowable.atlas.model.FlowableModelVisualFormattingGuard"
                            order="last"/>
        <!-- The Expression Playground: type an expression, get validation + completion. -->
        <toolWindow id="Flowable Expressions"
                    anchor="bottom"
                    secondary="true"
                    icon="/META-INF/expr-toolwindow.svg"
                    factoryClass="com.flowable.atlas.expr.toolwindow.FlowableExpressionToolWindowFactory"/>
        <!-- The Atlas Hub: the plugin's control center (model-index status, generated explorers,
             Flowable Design sync). Right anchor — the home of project control-center panels
             (Gradle, Maven, Database). -->
        <toolWindow id="Atlas Hub"
                    anchor="right"
                    icon="/META-INF/atlas-hub.svg"
                    factoryClass="com.flowable.atlas.hub.AtlasHubToolWindowFactory"/>
        <!-- "Report Flowable Atlas Problem…" in the IDE's error dialog: builds the report, copies it to
             the clipboard and opens the issue tracker — nothing is transmitted by the plugin itself. -->
        <errorHandler implementation="com.flowable.atlas.AtlasErrorReportSubmitter"/>
        <!-- Starts the model-index build when a project opens, so the Hub, the markers and the
             inspections do not wait for the first completion or Rebuild click to ask for one. -->
        <postStartupActivity implementation="com.flowable.atlas.index.FlowableIndexWarmUp"/>
    </extensions>

    <projectListeners>
        <!-- Regenerates the model-constants class when models are added/removed/edited. -->
        <listener class="com.flowable.atlas.generate.ModelConstantsAutoRefresher"
                  topic="com.intellij.openapi.vfs.newvfs.BulkFileListener"/>
    </projectListeners>

    <!-- Action/group texts and descriptions come from messages/FlowableAtlasBundle.properties
         (action.<id>.text / .description keys). Ellipsis convention: only actions that always need
         further input before they can complete carry one. -->
    <actions resource-bundle="messages.FlowableAtlasBundle">
        <group id="Flowable.Menu" popup="true">
            <add-to-group group-id="ToolsMenu" anchor="last"/>

            <!-- Open -->
            <action id="Flowable.OpenAtlasHub"
                    class="com.flowable.atlas.action.OpenAtlasHubAction"/>
            <!-- Platform icons (AllIcons) on the actions the Atlas Hub toolbar reuses; no entry in
                 the main toolbar — the hub is the plugin's visible surface, everything else stays
                 in the menu. -->
            <action id="Flowable.OpenAtlasExplorer"
                    class="com.flowable.atlas.action.OpenAtlasExplorerAction"
                    icon="AllIcons.Nodes.PpWeb"/>
            <action id="Flowable.OpenExpressionPlayground"
                    class="com.flowable.atlas.action.OpenExpressionPlaygroundAction"
                    icon="/META-INF/expr-toolwindow.svg"/>
            <!-- Opens Search Everywhere on the "Flowable Model" tab — otherwise reachable only by
                 pressing Shift twice and tabbing across to it, which nobody discovers unaided. -->
            <action id="Flowable.SearchModels"
                    class="com.flowable.atlas.action.SearchFlowableModelsAction"
                    icon="AllIcons.Actions.Search">
                <!-- The one action that competes with Shift-Shift for the hand: worth a chord. -->
                <keyboard-shortcut keymap="$default" first-keystroke="control alt shift M"/>
            </action>
            <separator/>

            <!-- Generate (submenu, with a nested Liquibase submenu) -->
            <group id="Flowable.Generate" popup="true">
                <action id="Flowable.GenerateAtlasExplorer"
                        class="com.flowable.atlas.action.GenerateAtlasExplorerAction"
                        icon="AllIcons.Actions.Compile"/>
                <action id="Flowable.GenerateModelConstants"
                        class="com.flowable.atlas.action.GenerateModelConstantsAction"/>
                <group id="Flowable.GenerateLiquibase" popup="true">
                    <action id="Flowable.GenerateLiquibaseFromDataObject"
                            class="com.flowable.atlas.action.GenerateLiquibaseFromDataObjectAction"/>
                    <action id="Flowable.GenerateLiquibaseFromApps"
                            class="com.flowable.atlas.action.GenerateLiquibaseFromAppsAction"/>
                </group>
                <group id="Flowable.GenerateDto" popup="true">
                    <action id="Flowable.GenerateDtoFromDataObject"
                            class="com.flowable.atlas.action.GenerateDtoFromDataObjectAction"/>
                    <action id="Flowable.GenerateDtoFromApps"
                            class="com.flowable.atlas.action.GenerateDtoFromAppsAction"/>
                </group>
            </group>
            <separator/>

            <!-- Flowable Design -->
            <action id="Flowable.SwitchDesignEnvironment"
                    class="com.flowable.atlas.action.SwitchDesignEnvironmentAction"/>
            <action id="Flowable.PullFromDesign"
                    class="com.flowable.atlas.action.PullFromDesignAction"
                    icon="AllIcons.Actions.Download"/>
            <action id="Flowable.SwitchWorkEnvironment"
                    class="com.flowable.atlas.action.SwitchWorkEnvironmentAction"/>
            <action id="Flowable.ManageEnvironments"
                    class="com.flowable.atlas.action.ManageEnvironmentsAction"/>
            <separator/>

            <!-- Maintenance -->
            <action id="Flowable.RebuildModelIndex"
                    class="com.flowable.atlas.action.RebuildModelIndexAction"/>
            <!-- Debug helper; visible only with idea.is.internal=true. -->
            <action id="Flowable.DumpKeyIndex" internal="true"
                    class="com.flowable.atlas.action.DumpFlowableIndexAction"/>
        </group>

        <!-- Right-click on a folder, a model file or an archive in the Project view: the two actions
             that are about the thing under the cursor. Hidden elsewhere (FlowableProjectViewGroup). -->
        <group id="Flowable.ProjectViewPopup" class="com.flowable.atlas.action.FlowableProjectViewGroup" popup="false">
            <separator/>
            <reference ref="Flowable.GenerateAtlasExplorer"/>
            <reference ref="Flowable.SearchModels"/>
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
        </group>
    </actions>
</idea-plugin>
