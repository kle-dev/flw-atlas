<!-- require-restart: the plugin registers languages / file types / parser definitions (the two
     expression dialects) which cannot be loaded dynamically, so force IntelliJ to prompt for a
     restart on install / update / uninstall instead of silently half-loading. -->
<idea-plugin require-restart="true">
    <id>com.flowable.atlas</id>
    <name>Flowable Atlas</name>
    <version>0.10.0</version>
    <vendor email="kevin.klever@flowable.com" url="https://www.flowable.com">Flowable</vendor>

    <description><![CDATA[
        <p><b>The complete Flowable companion for IntelliJ IDEA.</b> Flowable Atlas turns the model keys
        and expressions scattered across your Java code and Flowable models into first-class, IDE-aware
        references — completed, validated and navigable — and maps your whole project into a single
        interactive explorer. Everything is resolved against the models that <i>actually live in your
        repository</i>, so a wrong key or a broken expression is caught in the editor, long before
        deployment.</p>

        <p>Zero configuration: open a project that contains the Flowable models you exported from Design
        (the app <code>.zip</code> / deployment <code>-bar</code>, holding
        <code>.bpmn / .cmmn / .dmn / .form / .data / .service / …</code>) or the Flowable Design
        <code>*-models</code> workspace, and start typing. The plugin indexes every model and puts that
        knowledge to work across three areas.</p>

        <h3>1 &middot; Model-key intelligence</h3>
        <ul>
          <li><b>Context-aware key completion</b> at every Flowable API position that takes a model key —
              process, case, decision, form, event, channel, data object, master data, service, action,
              agent, knowledge base, template, security policy, query, variable extractor, sequence, SLA,
              dashboard component, data dictionary, page and unified work definitions. Both
              <code>org.flowable.*</code> and <code>com.flowable.*</code>. Search by <b>key, by name, or by
              any fragment</b> — type <code>0061</code> to get <code>KYC-DO-0061</code>.</li>
          <li><b>Cascade completion</b> for fluent chains — <code>operation("…")</code> completes the
              operations of the data object / service resolved from the sibling
              <code>definitionKey(…)</code> / <code>serviceKey(…)</code> (constant references are
              resolved), and <code>value("…", …)</code> completes that operation's input fields. DMN
              <code>variable("…", …)</code> offers the decision's input/output variables.</li>
          <li><b>More than keys</b> — completion for message &amp; signal names, process variables,
              task-definition keys and activity IDs, <b>scoped</b> to the
              <code>processDefinitionKey</code> / <code>caseDefinitionKey</code> in the same query chain
              when present, so you see only that model's ids.</li>
          <li><b>Model &rarr; model references in BPMN/CMMN XML</b> — the keys that live inside the models
              themselves (<code>callActivity calledElement</code>, <code>flowable:formKey</code>,
              <code>decisionRef</code>, CMMN <code>caseRef</code> / <code>processRef</code>) get the same
              completion, Ctrl/Cmd-click navigation and broken-key inspection as Java call sites — so a
              <code>calledElement</code> pointing at a missing process is caught before deployment.</li>
          <li><b>Broken-key inspection</b> — an unknown key (in Java or model XML) is flagged inline with a
              <i>"did you mean …?"</i> quick-fix to the closest real key.</li>
          <li><b>Navigation &amp; Find Usages</b> — Ctrl/Cmd-click a key literal to jump straight to the
              model file; hover for its type, name and location. Find Usages on a delegate class or an
              <code>${bean.method()}</code> expression lists the model files that reference it.</li>
          <li><b>Code generation</b> — generate a typed Java constants class
              (<code>FlowableModelKeys.Process.ORDER_FULFILMENT</code>, model name in Javadoc) that stays
              in sync as models are added, renamed or removed; or, with Alt/Option-Enter on a data-object
              <code>definitionKey("…")</code>, a typed Java bean built from the model's field mappings.</li>
          <li><b>Liquibase aware</b> — inside a changelog it completes <code>&lt;column&gt;</code> names
              and <code>tableName</code> from the backing service model, and flags columns that no
              backing Flowable model defines.</li>
        </ul>

        <h3>2 &middot; Flowable expression language support</h3>
        <ul>
          <li><b>Both dialects, first-class</b> — the backend JUEL dialect (<code>${…}</code> /
              <code>#{…}</code>) and the frontend form dialect (<code>{{…}}</code>) each get syntax
              highlighting, rainbow parentheses and brace matching.</li>
          <li><b>Live validation</b> — structural syntax errors, and unknown functions, namespaces or
              <code>flw.*</code> members, are flagged against a catalog of the real, verified Flowable
              function set.</li>
          <li><b>Expression completion</b> — functions, engine root objects, and your project's own
              process/case variables and form fields.</li>
          <li><b>Recognised inline, everywhere expressions live</b> — BPMN/CMMN/DMN XML,
              <code>.form</code> / <code>.page</code> JSON, and (optionally) Java string literals — so the
              highlighting, validation and completion follow you into the models you edit.</li>
          <li><b>Expression Playground</b> (the <i>Flowable Expressions</i> tool window) — type an
              expression, pick backend or frontend, and get instant validation and completion; scope
              completion to one model; evaluate a frontend expression live against a pasted JSON payload;
              or evaluate a backend expression against a running app through the Flowable Inspect REST API
              (connection auto-detected from the project's Spring configuration).</li>
        </ul>

        <h3>3 &middot; Atlas explorer &amp; hub</h3>
        <ul>
          <li><b>Atlas Hub</b> — a tool window (right stripe) that is the plugin's control center:
              model-index status with per-type counts and one-click rebuild, every generated explorer
              page (double-click to open), and the Flowable Design sync state — kept up to date live.</li>
          <li><b>Map your whole project</b> — Tools → Flowable Atlas → <i>Generate Atlas Explorer…</i>
              turns your models + Java + Liquibase into a single self-contained, offline, interactive
              HTML page. Save it into the project and open it in your browser or right inside the IDE
              (embedded viewer), then click through every relationship in both directions. In
              Settings → Generation you pick exactly which artifacts are written — summary &amp;
              overview Markdown, the graph JSON, the explorer HTML, a CLAUDE.md primer for AI agents.
              Generation runs entirely in-process — no external tools required — and honours the
              project's expression allowlist.</li>
          <li><b>IDE-native viewing</b> — the embedded explorer follows the IDE light/dark theme
              (switching restyles live) and carries a toolbar with <i>Regenerate</i>, <i>Reload</i>
              and <i>Open in Browser</i>.</li>
          <li><b>Reopen without regenerating</b> — <i>Open Atlas Explorer</i> (Atlas Hub, or Tools →
              Flowable Atlas) opens an already-generated <code>*.explorer.html</code> — including
              one written by the standalone <code>atlas</code> CLI into the configured output folder — as a
              center editor tab (like a DB table), so you never have to leave the IDE for a browser.</li>
        </ul>

        <h3>How it feels</h3>
        <ul>
          <li><code>runtimeService.startProcessInstanceByKey("&lt;caret&gt;")</code> → your process keys.</li>
          <li><code>cmmnRuntimeService.createCaseInstanceBuilder().caseDefinitionKey("&lt;caret&gt;")</code> → your case keys.</li>
          <li><code>dataObjectRuntimeService.createDataObjectInstanceQuery().definitionKey(KEY).operation("&lt;caret&gt;")</code>
              → that data object's operations; <code>.value("&lt;caret&gt;", …)</code> → the operation's fields.</li>
          <li>Press completion twice to list <b>all</b> model keys of every type, regardless of the call site.</li>
        </ul>

        <p>Completion fires inside string literals and at bare arguments alike (inserting a quoted key),
        with Flowable keys ranked to the top — no setup beyond opening a project that contains your
        Flowable models.</p>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.8.8</h3>
        <ul>
          <li><b>Payload scope in the Expression Playground</b> — evaluate a frontend expression as a
              component <i>inside a subform or list</i> would see it: enter a payload node path
              (e.g. <code>orders[2].items[0]</code>) or place the caret on the node and hit
              <i>From Caret</i>. <code>$item</code>, <code>$index</code> and the chained
              <code>$itemParent</code> are bound exactly like the form runtime binds them;
              <code>root</code> and <code>$payload</code> stay absolute. The scoped node is
              highlighted in the payload editor, and a path that no longer resolves is flagged on
              the field and reported as the evaluation result.</li>
          <li><b>Resizable playground panels</b> — the divider between the expression editor and the
              payload/scope card is now grabbable (drag it to trade width when docked side-by-side, or
              height when stacked), and the frontend evaluation result sits under the payload behind
              its own drag handle so a large payload can be given more room; it still scrolls.</li>
        </ul>

        <h3>0.8.7</h3>
        <ul>
          <li><b>“Last scanned” time on the Model Index</b> — the Atlas Hub now shows when the model
              index was last built, next to the model count, so you can tell how fresh it is.</li>
        </ul>

        <h3>0.8.6</h3>
        <ul>
          <li><b>Works when the IDE backend runs remotely</b> (e.g. JetBrains Remote Dev in Kubernetes).
              <i>Generate Model Constants</i> no longer depends on a modal dialog that could silently
              fail on the thin client — it uses the class name from <i>Settings → Flowable Atlas →
              Generation</i>, reports any problem as a notification, and falls back to a folder picker
              when the project exposes no Java source root. <i>Open in Browser</i> now uses the IDE's own
              browser mechanism (like the built-in HTML action) and is shown only where a browser can
              actually be launched.</li>
          <li><b>“Generate Model Constants” in the Atlas Hub</b> — right in the Hub's <i>Model Index</i>
              section, next to <i>Rebuild</i>.</li>
          <li><b>Warning when you rename Java a model uses</b> — renaming a method or bean/delegate class
              referenced from a model expression (<code>${bean.method()}</code>, delegate expressions, …)
              warns that the model's text references are not updated by the rename, with a
              <i>Show affected models</i> action. A gutter icon also marks such methods/beans and
              navigates to the models that use them.</li>
          <li><b>Stale-explorer hint after a Design pull</b> — pulling models from Flowable Design now
              offers a <i>Regenerate Atlas Explorer</i> action (and a hint in the Atlas Hub): the model
              index is rebuilt automatically, but the generated explorer is not.</li>
          <li><b>Clearer settings wording</b> — the “index raw Design workspace sources” and
              “expressions in Java string literals” options now explain what they actually do.</li>
        </ul>

        <h3>0.8.5</h3>
        <ul>
          <li><b>"Paste session from browser" for "Evaluate Against App"</b> — the reliable way to
              evaluate backend expressions against an SSO/OAuth2-fronted app whose identity provider
              blocks the embedded browser login (e.g. Microsoft Entra Conditional Access). Log in to the
              app in your normal browser, copy any authenticated request (DevTools → Network →
              <i>Copy as cURL</i>, or just its <code>Cookie</code> header) and paste it: the plugin
              extracts the <code>Cookie</code>, <code>Authorization</code> and CSRF-token
              (<code>X-XSRF-TOKEN</code>) headers and replays them, so the Inspect request rides your
              already-authenticated session — CSRF-protected POSTs included. Captured headers live only
              for the current IDE session (nothing written to disk).</li>
          <li><b>Embedded sign-in sends a desktop User-Agent</b> — the <i>Sign in via browser (SSO)</i>
              login now presents a normal desktop-Chrome User-Agent, which lets some IdPs accept the
              embedded login; where policy still blocks it, use <i>Paste session from browser</i>.</li>
        </ul>

        <h3>0.8.4</h3>
        <ul>
          <li><b>Sign in via browser (SSO) for “Evaluate Against App”</b> — the Expression Playground's
              backend evaluation now works against apps fronted by SSO/OAuth2, not just local
              basic-auth dev instances. A new <i>Sign in via browser (SSO)…</i> button opens the app in
              an embedded browser; you complete the real identity-provider login (Microsoft, Keycloak,
              …), and the resulting session cookie is reused for this IDE session so the Inspect request
              rides your authenticated session. Basic auth and the SSO cookie can be combined — for an
              OAuth2 gateway in front of a Flowable that still wants basic auth. A login redirect now
              surfaces a clear “app is behind SSO/OAuth2 — sign in” message instead of a raw HTTP 302.</li>
        </ul>

        <h3>0.8.3</h3>
        <ul>
          <li><b>Monorepo detection refined</b> — a root-level build file
              (<code>pom.xml</code>/<code>settings.gradle</code>/…) that wraps a single app is now
              treated as one whole project and is never split into its modules; only a true
              reactor bundling two or more independent apps (each carrying its own <code>.app</code>)
              prompts you to pick a project. The Atlas Hub's <i>Change…</i> link now appears whenever
              a sub-project is detected, so you can always switch scope (previously it could lock you
              onto “Whole project”).</li>
          <li><b>Fixes</b> — Atlas Explorer generation no longer fails with a
              <code>NullPointerException</code> on a service-model operation that declares an HTTP
              method but no URL. Resolved a plugin-load error (missing intention description) for the
              “Generate Java bean for this Flowable data object” intention.</li>
        </ul>

        <h3>0.8.2</h3>
        <ul>
          <li><b>Resizable Atlas Explorer sidebar</b> — grab the sidebar's right edge and drag it
              wider or narrower like an IntelliJ tool window; the width is remembered. Dragging it
              very narrow snaps it to the icon rail, and while collapsed, hovering the rail flies
              the full labelled menu out over the content — so a narrow window (or a narrow Atlas
              Hub tool window) no longer leaves you with unlabelled dots.</li>
        </ul>

        <h3>0.8.1</h3>
        <ul>
          <li><b>Multi-project (monorepo) support</b> — Atlas detects the distinct Flowable
              sub-projects under the project root; the Atlas Hub lets you switch the active one and
              the model index then scans just that subtree.</li>
          <li><b>Flowable Inspect connection editor</b> — Settings → Tools → Flowable Atlas →
              <i>Connections</i> gains an embedded Inspect connection (base URL + credentials, with
              <i>Detect from project</i>) that the Expression Playground evaluates backend
              expressions against.</li>
          <li><b>Fixes</b> — resolve a duplicate registration of the “Open in Expression Playground”
              intention that could abort the highlighting pass, and clear all remaining compiler
              warnings across the codebase.</li>
        </ul>

        <h3>0.8.0</h3>
        <ul>
          <li><b>Atlas Hub — one control center</b> — a new tool window (right stripe, or Tools →
              Flowable Atlas → <i>Atlas Hub</i>) shows the model-index status with per-type counts
              and a background <i>Rebuild</i>, every generated <code>*.explorer.html</code>
              (double-click opens the embedded viewer, with a browser fallback when JCEF is
              unavailable), and the Flowable Design sync state with last-pull time — refreshing
              live as models change, artifacts are generated or a pull finishes.</li>
          <li><b>The explorer follows your IDE theme</b> — the embedded Atlas Explorer now opens in
              the IntelliJ light/dark theme and restyles live when you switch the LAF, without a
              reload. The in-page toggle still wins for explicit overrides; <i>auto</i> follows the
              IDE. In a plain browser nothing changes.</li>
          <li><b>Explorer editor toolbar</b> — the embedded viewer gained a thin toolbar:
              <i>Regenerate</i> (re-runs the generator for exactly this file and reloads, no dialogs,
              no balloon), <i>Reload</i> and <i>Open in Browser</i>.</li>
          <li><b>Settings, reorganized</b> — Settings → Tools → Flowable Atlas is now a small tree:
              the root page keeps the core toggles, and three project-level sub-pages hold the rest —
              <i>Expressions</i> (validation toggles and, for the first time, the expression
              allowlist as an editable table plus custom-function discovery), <i>Generation</i>
              (per-artifact selection instead of the old two-way switch, a configurable output
              folder, and the model-constants class options — now project-level and VCS-shared) and
              <i>Connections</i> (the full Flowable Design connection editor — replacing the old
              dialog — and the Inspect connection with <i>Detect from Project</i>).</li>
          <li><b>Allowlist flows into generation</b> — the project's expression allowlist and the
              custom-function settings are now passed to the Atlas generator (as with the CLI's
              <code>--expr-allowlist</code> / <code>--custom-functions</code>), so the explorer's
              <i>Suspect</i> findings respect them.</li>
          <li><b>Tools menu, tidied</b> — grouped into Open / Generate / Flowable Design /
              Maintenance; dialog-opening actions carry an ellipsis; the debug <i>Dump Key Index</i>
              became the user-facing <i>Rebuild Model Index</i> (background, balloon with counts)
              and the raw dump is internal-mode only. Consistent naming across dialogs and
              notifications; project settings consolidated into
              <code>.idea/flowable-atlas.xml</code>. The main-toolbar compass icon is gone — the
              Atlas Hub is the plugin's one visible surface (tool-window icons follow the native
              monochrome style).</li>
        </ul>
        <h3>0.7.6</h3>
        <ul>
          <li><b>Open an existing Atlas explorer from the menu</b> — Tools → Flowable Atlas →
              <i>Open Atlas Explorer</i> opens an already-generated <code>*.explorer.html</code> in the
              embedded in-IDE viewer, without regenerating it and without switching to a browser. It
              looks under <code>atlas-output/</code> first (where both the generator and the standalone
              <code>atlas</code> CLI write by default), so a page produced from the terminal opens
              straight in the IDE; if several exist you pick one, and if none is found it offers to
              generate. Opening now brings the rendered <i>Atlas Explorer</i> tab to the front
              immediately (instead of the HTML source), from the menu, the post-generation balloon and
              double-clicking the file alike.</li>
          <li><b>One-click toolbar icon — opens in the center</b> — <i>Open Atlas Explorer</i> also sits
              as a compass icon in the main toolbar; clicking it opens the explorer as a <b>center editor
              tab</b> (like opening a database table), rendered right away, so it fills the main area and
              can be maximized like any editor.</li>
          <li><b>One unified view — no “Text” tab, playground included</b> — the center Atlas Explorer no
              longer shows the raw-HTML <i>Text</i> sub-tab, and a second <b>Flowable Expressions</b> tab
              sits right next to <i>Atlas Explorer</i>, so the whole-project map and the expression
              playground share a single window instead of two separate entry points. (The standalone
              <i>Flowable Expressions</i> tool window still works too.)</li>
        </ul>
        <h3>0.7.5</h3>
        <ul>
          <li><b>Service-operation “Used by” now also finds Java callers</b> — Java code that invokes an
              operation through the data-object runtime builder
              (<code>…createDataObjectInstanceQuery().definitionKey(key).operation("op")</code>) is now
              detected and listed alongside the model consumers. The <code>definitionKey</code> is
              resolved even when it is a <code>static final String</code> constant reference (a generated
              model-keys class) rather than a string literal, and data objects resolve through their
              backing service — so the operation, the constant and the Java class all line up on the same
              node. Each class node in turn lists the operations it calls.</li>
        </ul>
        <h3>0.7.4</h3>
        <ul>
          <li><b>Service-operation “Used by” now finds usages hidden in data-source URLs</b> — forms and
              pages most often invoke an operation through a REST data source, lookup or navigation URL
              (e.g. <code>{{endpoints.dataobject}}/dataobject-runtime/data-object-instances?dataObjectDefinitionKey=…&amp;dataObjectOperationKey=…</code>)
              rather than a structured field. The operation and target keys embedded as literal query
              params are now detected, so these usages appear in the operation's <b>“Used by”</b> list.
              Data-object references still resolve through their backing service. (Verified against a real
              project: operations with recorded usages went from none to dozens.)</li>
        </ul>
        <h3>0.7.3</h3>
        <ul>
          <li><b>Service operations are now first-class in the Explorer</b> — every service-registry
              operation (e.g. <code>findByPodId</code>) gets its own node under a new
              <b>Service operations</b> category, with a <b>“Used by”</b> list of every form service
              button, data-object field and CMMN service mapping that calls it. Data-object references
              are resolved through their backing service, so usages reached via a data object and via the
              service aggregate onto the same operation. Operation keys are searchable, and each operation
              in a service's Operations list links to its own where-used page.</li>
          <li><b>Bot name links to its Java bot class</b> — in an action's detail, the Bot field is now a
              clickable chip that navigates to the backing Java bot class node (platform bots stay plain
              text).</li>
        </ul>
        <h3>0.7.2</h3>
        <ul>
          <li><b>Rainbow parentheses now work in the Expression Playground field too</b> — the paren
              colouring moved from an annotator into the syntax highlighter (the highlighting lexer tags
              each round paren with a colour). Annotator highlights do not reliably paint in the
              playground's embedded editor field; syntax-highlighter colours do, so parentheses are now
              coloured in the playground and in inline <code>${…}</code> / <code>{{…}}</code> fragments
              alike. Colouring is <b>per pair</b>: each opening <code>(</code> takes the next colour and
              its matching <code>)</code> reuses it, so neighbouring pairs are visually distinct while a
              pair's <code>(</code>/<code>)</code> always match.</li>
          <li><b>Playground field is a proper scrollable editor</b> — the expression field is now a
              multi-line code editor with vertical and horizontal scrollbars, so long expressions can be
              scrolled and edited comfortably instead of being clipped.</li>
          <li><b>Syntax errors point at the exact spot</b> — the playground now shows the first
              structural error with a caret under the offending offset (e.g. the unclosed <code>(</code>
              for a missing <code>)</code>), so you see where to fix without hunting. (Annotator squiggles
              don't reliably paint in the embedded field, so the position is surfaced directly.)</li>
          <li><b>Autocomplete for project custom functions, with parameters</b> — the extracted
              <code>externals.additionalData</code> functions are offered in completion: custom namespaces
              and top-level helpers at the root, namespace members after <code>ns.</code>, and custom
              <code>flw.*</code> members after <code>flw.</code>. Each lists its parameter names and, on
              selection, inserts <code>(params)</code> with the parameters selected so they're easy to fill
              in. Real names are read from the source — including from a compiled bundle's
              <b>sourcemap</b> (<code>*.js.map</code> <code>sourcesContent</code>), so even a minified
              <code>custom.js</code> yields <code>findCommonAttribute(allItems, path, identifierPath?)</code>
              instead of <code>(e, t, r)</code>.</li>
        </ul>
        <h3>0.7.1</h3>
        <ul>
          <li><b>Custom functions show their arguments</b> — each custom-function node in the Atlas
              Explorer carries its signature (e.g. <code>flowkyc.findCommon(customer, docs)</code>), read
              from the source: inline arrows / method shorthands / function expressions, and — in a
              compiled bundle — an identifier member resolved to its <code>function name(…)</code>
              declaration.</li>
          <li><b>Frontend bindings link to the custom functions they call</b> — a <code>{{…}}</code>
              binding now references not only its form/model but also each custom function it calls
              (<i>Calls custom functions</i>), and every custom function lists the exact bindings that
              call it (<i>Called in bindings</i>), in addition to the forms under <i>Used by</i>.</li>
        </ul>
        <h3>0.7.0</h3>
        <ul>
          <li><b>Rainbow parentheses render again in frontend <code>{{…}}</code> (and backend
              <code>${…}</code>) expressions</b> — each nesting level of <code>( )</code> is coloured
              so a matching pair shares a colour and a missing one stands out. The colours are now
              forced onto the annotation instead of relying on a colour-scheme default that silently
              stopped rendering after a platform upgrade, so they show reliably in the playground and
              inline in <code>.form</code> / model fragments.</li>
          <li><b>Custom functions are found in compiled frontend bundles</b> — a project that ships only
              the built <code>static/ext/custom.js</code> (Rollup UMD: <code>var additionalData = {…}</code>)
              or a nested <code>externals: { additionalData: {…} }</code> config now has its
              <code>externals.additionalData</code> functions read too, not just uncompiled source. A React
              <code>&lt;Form additionalData={…}&gt;</code> prop is not mistaken for a registration.</li>
          <li><b>Custom functions are cross-referenced in the Atlas Explorer</b> — each project custom
              function (a <code>flowkyc.*</code> namespace member, an extra <code>flw.*</code> member, or a
              top-level helper) is a first-class node: it lists the forms/models whose <code>{{…}}</code>
              bindings call it, and each form lists the custom functions it uses (navigable both ways).</li>
        </ul>
        <h3>0.6.0</h3>
        <ul>
          <li><b>Project custom functions are read from source and validated precisely</b> — functions a
              project registers via <code>flowable.externals.additionalData</code> (e.g. a
              <code>flowkyc.*</code> namespace, or extra <code>flw.*</code> members) are extracted from the
              frontend-customization source in the project. Calls to them now validate exactly: a known
              member is valid, a close typo is flagged (<i>did you mean …?</i>), and unknown names are left
              alone. Compiled bundles that can't be read are skipped; unresolved constructs (spreads,
              computed keys) are noted, never guessed.</li>
          <li><b>Payload preview no longer marks valid expressions invalid</b> — an expression that is
              correct but can't be evaluated statically (a running-form/locale member such as
              <code>flw.getUser</code>, or a custom <code>externals.additionalData</code> function) now shows
              a neutral <i>“not available in the payload preview”</i> note instead of a red error.</li>
          <li><b>Leniency for custom <code>flw.*</code></b> — an unknown <code>flw.&lt;member&gt;</code> with no
              close match to a built-in is treated as a project-injected custom function and not flagged;
              only a plausible typo is surfaced.</li>
          <li><b>Atlas Explorer</b> shows a <i>custom functions</i> badge/panel listing the project's
              <code>externals.additionalData</code> functions and where they were read from.</li>
        </ul>
        <h3>0.5.0</h3>
        <ul>
          <li><b>Expression warnings are now real inspections</b> — unknown function / namespace /
              <code>flw.*</code> findings and the opt-in codebase grounding moved from a fixed
              annotator to <i>Settings → Editor → Inspections → Flowable</i>: severity is adjustable,
              they can be disabled per profile/scope, and they appear in <i>Inspect Code</i>.
              Structural syntax errors are still flagged directly.</li>
          <li><b>Project allowlist for custom functions</b> — Alt-Enter on a finding offers
              <i>Add … to Flowable expression allowlist</i>: functions/namespaces your project
              registers itself (which the built-in catalog cannot know) are silenced project-wide.
              Stored in <code>.idea/flowable-atlas.xml</code>, shareable via VCS. The
              "ground backend expressions" checkbox moved into the (default-off) grounding inspection.</li>
          <li><b>Expression Playground</b> shows semantic findings (allowlist-aware) in a status line.</li>
          <li><b>Modernized Atlas Explorer</b> (bundled generator) — light/dark theme with toggle,
              responsive layout, keyboard &amp; screen-reader support, no more 600-item list cap
              (incremental scrolling), working browser back/forward + a copy-link button, an SVG
              neighborhood graph per node, and a <i>⚠ parse issues</i> badge that lists files the
              generator could not fully analyze. Structural syntax errors and "unknown function"
              findings are now separate <i>Invalid</i> / <i>Suspect</i> categories, and
              <code>--expr-allowlist</code> suppresses findings for project-provided functions.</li>
          <li>Internals: diagnostics logging at previously silent failure points (model indexing,
              generator, Inspect client), consolidated JSON/wrapper helpers.</li>
        </ul>
        <h3>0.4.1</h3>
        <ul>
          <li><b>Requires an IDE restart on install / update</b> — the plugin registers languages,
              file types and parser definitions (the two expression dialects) that cannot be loaded
              dynamically, so IntelliJ now prompts for a restart instead of silently half-loading
              (which left the Flowable Expressions tool window and its stripe icon missing).</li>
          <li><b>Open the Expression Playground from the menu</b> — Tools → Flowable Atlas →
              <i>Open Expression Playground</i> shows the Flowable Expressions tool window directly,
              independent of the tool-window stripe button.</li>
          <li><b>Fixed the tool-window icon</b> — a correctly sized (16&times;16) icon that renders
              reliably across display scalings, instead of the oversized plugin icon.</li>
        </ul>
        <h3>0.4.0</h3>
        <ul>
          <li><b>Flowable expression language support</b> — first-class editor support for both the
              backend JUEL dialect (<code>${…}</code> / <code>#{…}</code>) and the frontend form dialect
              (<code>{{…}}</code>): syntax highlighting, rainbow parentheses, brace matching, live
              validation (syntax + unknown functions/namespaces/<code>flw.*</code> members against the
              verified Flowable catalog) and completion of functions, root objects and the project's
              variables/form-fields. Expressions are recognised inline in BPMN/CMMN/DMN XML,
              <code>.form</code>/<code>.page</code> JSON and (optionally) Java strings.</li>
          <li><b>Expression Playground</b> — a <i>Flowable Expressions</i> tool window to try an
              expression with live validation and completion, evaluate a frontend expression against a
              pasted JSON payload, or evaluate a backend expression against a running app via the
              Flowable Inspect REST API (connection auto-detected from the project's Spring config).</li>
        </ul>
        <h3>0.3.0</h3>
        <ul>
          <li><b>Renamed to Flowable Atlas</b> (formerly Flowable Keys) — the plugin now also
              produces the Atlas explorer, not just model-key tooling.</li>
          <li><b>Generate Atlas Explorer</b> — Tools → Flowable Atlas → <i>Generate Atlas Explorer</i>
              runs the bundled Atlas generator over your project and writes a single self-contained,
              interactive HTML page. Choose where to save it in the project, then open it in the
              external browser or in an embedded in-IDE viewer. Any <code>*.explorer.html</code> in the
              project also opens in the embedded viewer.</li>
          <li><b>All artifacts option</b> — a <i>Generate</i> scope in Settings → Tools → Flowable Atlas
              switches the action from "explorer HTML only" to writing the full Atlas set (summary.md,
              overview.md, graph.json, explorer.html, CLAUDE.md) into a folder you pick.</li>
        </ul>
        <h3>0.2.0</h3>
        <ul>
          <li><b>Infix key search</b> — completion now matches any fragment of a key, so typing
              <code>0061</code> at <code>definitionKey("…")</code> proposes <code>KYC-DO-0061</code>
              (start / word-boundary matches still rank first).</li>
          <li><b>Scoped task/activity completion</b> — <code>taskDefinitionKey</code> /
              <code>activityId</code> are narrowed to the sibling
              <code>processDefinitionKey</code> / <code>caseDefinitionKey</code> in the same query chain,
              falling back to the project-wide union otherwise.</li>
          <li><b>Model &rarr; model references in BPMN/CMMN XML</b> — completion, Ctrl/Cmd-click
              navigation and a broken-key inspection (with quick fix) for <code>calledElement</code>,
              <code>flowable:formKey</code>, <code>decisionRef</code>,
              <code>decisionTableReferenceKey</code>, CMMN <code>caseRef</code> / <code>processRef</code>.</li>
          <li><b>Generate a typed data-object bean</b> — Alt/Option-Enter on a data-object
              <code>definitionKey("…")</code> writes a Java POJO from the model's field mappings.</li>
          <li>Long project scans now honour cancellation (no UI stall while indexing / finding usages).</li>
        </ul>
        <h3>0.1.0</h3>
        <ul>
          <li>Initial release: context-aware Flowable model-key completion, cascade completion,
              broken-key inspection, key navigation &amp; Find Usages, model-constants generation, and
              Liquibase changelog awareness.</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>
    <!-- XML PSI for the Liquibase-changelog coverage inspection. -->
    <depends>com.intellij.modules.xml</depends>
    <!-- JSON PSI for form-model {{…}} expression injection and the .form → JSON association. -->
    <depends>com.intellij.modules.json</depends>

    <resource-bundle>messages.FlowableAtlasBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Services (FlowableModelIndexService, ModelConstantsService/Settings) are @Service — no registration needed. -->
        <!-- Settings tree: an app-level root page (core toggles) with three project-level children.
             parentId chaining across levels is supported — the settings tree merges them by ID. -->
        <applicationConfigurable
                parentId="tools"
                id="com.flowable.atlas.settings"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas"
                instance="com.flowable.atlas.settings.FlowableAtlasConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.expressions"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.expressions"
                instance="com.flowable.atlas.settings.ExpressionsConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.generation"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation"
                instance="com.flowable.atlas.settings.GenerationConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.connections"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.connections"
                instance="com.flowable.atlas.settings.ConnectionsConfigurable"/>
        <completion.contributor
                language="JAVA"
                implementationClass="com.flowable.atlas.completion.FlowableJavaCompletionContributor"/>
        <!-- Liquibase changelog: complete <column name="…"> / tableName from the backing service model. -->
        <completion.contributor
                language="XML"
                implementationClass="com.flowable.atlas.liquibase.LiquibaseColumnCompletionContributor"/>
        <!-- BPMN/CMMN model XML: complete model keys at cross-reference attributes
             (calledElement, flowable:formKey, decisionRef, caseRef, processRef, …). -->
        <completion.contributor
                language="XML"
                implementationClass="com.flowable.atlas.completion.FlowableXmlKeyCompletionContributor"/>
        <localInspection
                language="JAVA"
                shortName="FlowableValueField"
                displayName="Invalid Flowable data-object value field"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableValueFieldInspection"/>
        <localInspection
                language="JAVA"
                shortName="FlowableBrokenKey"
                displayName="Unknown Flowable model key"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableBrokenKeyInspection"/>
        <!-- BPMN/CMMN model XML: an unknown model key at a cross-reference attribute (broken deployment). -->
        <localInspection
                language="XML"
                shortName="FlowableXmlBrokenKey"
                displayName="Unknown Flowable model key (model XML)"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableXmlBrokenKeyInspection"/>
        <!-- Ctrl-click a key literal → jump to the model file; Find Usages works too. -->
        <psi.referenceContributor
                language="JAVA"
                implementation="com.flowable.atlas.navigation.FlowableKeyReferenceContributor"/>
        <!-- Ctrl-click a model key in BPMN/CMMN XML (calledElement, formKey, decisionRef, …) → the model file. -->
        <psi.referenceContributor
                language="XML"
                implementation="com.flowable.atlas.navigation.FlowableXmlKeyReferenceContributor"/>
        <!-- Hover / Ctrl-Q on a key literal → model type, name and file. -->
        <lang.documentationProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.navigation.FlowableKeyDocumentationProvider"/>
        <!-- Search Everywhere / Go to Symbol: every model key (→ model file) and every bot key
             (→ the BotService class and the actions that invoke it). -->
        <gotoSymbolContributor
                implementation="com.flowable.atlas.navigation.FlowableKeyGotoSymbolContributor"/>
        <!-- Liquibase changelog column not mapped in the backing Flowable service/data-object model. -->
        <localInspection
                language="XML"
                shortName="FlowableLiquibaseCoverage"
                displayName="Liquibase column not defined in Flowable model"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.liquibase.LiquibaseCoverageInspection"/>
        <!-- Alt-Enter on a data-object definitionKey → generate a typed Java bean from its fields. -->
        <intentionAction>
            <language>JAVA</language>
            <className>com.flowable.atlas.intention.GenerateDataObjectBeanIntention</className>
            <category>Flowable</category>
        </intentionAction>
        <!-- Don't report Java referenced from models (delegates, ${bean.method()}, listeners) as unused. -->
        <implicitUsageProvider implementation="com.flowable.atlas.usage.FlowableImplicitUsageProvider"/>
        <!-- Find Usages / Ctrl-B on a delegate class or ${bean.method()} shows the model files that use it. -->
        <customUsageSearcher implementation="com.flowable.atlas.usage.FlowableModelUsageSearcher"/>
        <!-- Gutter icon on Java methods/beans referenced from Flowable models → navigate to the model usages. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableModelReferenceLineMarkerProvider"/>
        <!-- Gutter icon on a BotService class → navigate to the .action models that invoke the bot. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableBotActionLineMarkerProvider"/>
        <!-- Warn when renaming a Java symbol referenced by Flowable model expressions (text, not auto-updated). -->
        <refactoring.elementListenerProvider
                implementation="com.flowable.atlas.usage.FlowableRenameWarningProvider"/>
        <!-- Balloon shown after generating the Atlas explorer (open in browser / open in IDE). -->
        <notificationGroup id="Flowable Atlas" displayType="BALLOON"/>
        <!-- Render a *.explorer.html as an embedded (JCEF) "Atlas Explorer" tab in the center editor
             area (like a DB table); hides the raw-HTML "Text" tab for it. -->
        <fileEditorProvider implementation="com.flowable.atlas.explorer.AtlasFileEditorProvider"/>
        <!-- Second tab on the same *.explorer.html: the Flowable Expressions playground, so the map and
             the playground share one window (no separate tool window needed). Placed after Atlas Explorer. -->
        <fileEditorProvider implementation="com.flowable.atlas.explorer.AtlasExpressionEditorProvider"/>

        <!-- ==================== Flowable Expression support ==================== -->
        <!-- Two dialects of one expression language: backend ${…}/#{…} (JUEL) and frontend {{…}} (flw.*). -->
        <fileType name="Flowable Backend Expression"
                  language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableBackendExprFileType"
                  fieldName="INSTANCE"
                  extensions="flowable-be"/>
        <fileType name="Flowable Frontend Expression"
                  language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableFrontendExprFileType"
                  fieldName="INSTANCE"
                  extensions="flowable-fe"/>
        <lang.parserDefinition language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableBackendExprParserDefinition"/>
        <lang.parserDefinition language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableFrontendExprParserDefinition"/>
        <!-- Token coloring (parentheses, brackets, strings, operators, …) for both dialects. -->
        <lang.syntaxHighlighterFactory language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprSyntaxHighlighterFactory"/>
        <lang.syntaxHighlighterFactory language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprSyntaxHighlighterFactory"/>
        <!-- Match () and [] — caret on a brace highlights its partner, unmatched braces are flagged. -->
        <lang.braceMatcher language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprBraceMatcher"/>
        <lang.braceMatcher language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprBraceMatcher"/>
        <!-- Structural syntax squiggles for both dialects (playground + injected fragments). -->
        <annotator language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.annotator.FlowableExpressionAnnotator"/>
        <annotator language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.annotator.FlowableExpressionAnnotator"/>
        <!-- Semantic expression findings as real inspections: severity/enablement per profile, project
             allowlist via quick fix. No language attribute — the two dialects share no registered base
             language; the implementations bail out immediately on any non-expression file. -->
        <localInspection
                shortName="FlowableExprUnknownFunction"
                displayName="Unknown Flowable expression function or namespace"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.expr.inspection.FlowableExprUnknownFunctionInspection"/>
        <localInspection
                shortName="FlowableExprGrounding"
                displayName="Expression root is not a known variable, bean, or root object"
                groupName="Flowable"
                enabledByDefault="false"
                level="WEAK WARNING"
                implementationClass="com.flowable.atlas.expr.inspection.FlowableExprGroundingInspection"/>
        <!-- Rainbow parentheses are done in the syntax highlighter (FlowableExprSyntaxHighlighter):
             the highlighting lexer tags each round paren with its nesting level and each level gets
             a distinct colour. This paints in every surface, including the playground's embedded
             editor field where an annotator would not. Per-theme default colours + the Color Scheme
             page that makes them customizable: -->
        <additionalTextAttributes scheme="Default" file="colorSchemes/FlowableExprDefault.xml"/>
        <additionalTextAttributes scheme="Darcula" file="colorSchemes/FlowableExprDarcula.xml"/>
        <colorSettingsPage implementation="com.flowable.atlas.expr.lang.FlowableExprColorSettingsPage"/>
        <!-- Alt-Enter on an injected ${…}/{{…}} fragment → rework it in the playground, pre-filled.
             Registered once (no <language> filter): the same class must not be registered twice or the
             platform rejects it as a duplicate. isAvailable() self-gates on FlowableExprFile, which
             covers both the backend and frontend dialects. -->
        <intentionAction>
            <className>com.flowable.atlas.expr.intention.OpenInExpressionPlaygroundIntention</className>
            <category>Flowable</category>
        </intentionAction>
        <!-- Ctrl-Q / hover on a catalog function or root inside an expression → call head + doc line. -->
        <lang.documentationProvider language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.documentation.FlowableExprDocumentationProvider"/>
        <lang.documentationProvider language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.documentation.FlowableExprDocumentationProvider"/>
        <!-- Completion of functions, root objects and the project's variables/form-fields. -->
        <completion.contributor language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.completion.FlowableExpressionCompletionContributor"/>
        <completion.contributor language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.completion.FlowableExpressionCompletionContributor"/>
        <!-- Inject the expression languages inline wherever expressions live in the repo. -->
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableXmlExpressionInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableJsonExpressionInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableJavaExpressionInjector"/>
        <!-- Treat Flowable form models (.form) as JSON so their {{…}} strings become injection hosts. -->
        <fileType name="JSON" extensions="form"/>
        <!-- The Expression Playground: type an expression, get validation + completion. -->
        <toolWindow id="Flowable Expressions"
                    anchor="bottom"
                    secondary="true"
                    icon="/META-INF/expr-toolwindow.svg"
                    factoryClass="com.flowable.atlas.expr.toolwindow.FlowableExpressionToolWindowFactory"/>
        <!-- The Atlas Hub: the plugin's control center (model-index status, generated explorers,
             Flowable Design sync). Right anchor — the home of project control-center panels
             (Gradle, Maven, Database). -->
        <toolWindow id="Atlas Hub"
                    anchor="right"
                    icon="/META-INF/atlas-hub.svg"
                    factoryClass="com.flowable.atlas.hub.AtlasHubToolWindowFactory"/>
    </extensions>

    <projectListeners>
        <!-- Regenerates the model-constants class when models are added/removed/edited. -->
        <listener class="com.flowable.atlas.generate.ModelConstantsAutoRefresher"
                  topic="com.intellij.openapi.vfs.newvfs.BulkFileListener"/>
    </projectListeners>

    <!-- Action/group texts and descriptions come from messages/FlowableAtlasBundle.properties
         (action.<id>.text / .description keys). Ellipsis convention: only actions that always need
         further input before they can complete carry one. -->
    <actions resource-bundle="messages.FlowableAtlasBundle">
        <group id="Flowable.Menu" popup="true">
            <add-to-group group-id="ToolsMenu" anchor="last"/>

            <!-- Open -->
            <action id="Flowable.OpenAtlasHub"
                    class="com.flowable.atlas.action.OpenAtlasHubAction"/>
            <!-- Platform icons (AllIcons) on the actions the Atlas Hub toolbar reuses; no entry in
                 the main toolbar — the hub is the plugin's visible surface, everything else stays
                 in the menu. -->
            <action id="Flowable.OpenAtlasExplorer"
                    class="com.flowable.atlas.action.OpenAtlasExplorerAction"
                    icon="AllIcons.Nodes.PpWeb"/>
            <action id="Flowable.OpenExpressionPlayground"
                    class="com.flowable.atlas.action.OpenExpressionPlaygroundAction"/>
            <separator/>

            <!-- Generate -->
            <action id="Flowable.GenerateAtlasExplorer"
                    class="com.flowable.atlas.action.GenerateAtlasExplorerAction"
                    icon="AllIcons.Actions.Compile"/>
            <action id="Flowable.GenerateModelConstants"
                    class="com.flowable.atlas.action.GenerateModelConstantsAction"/>
            <separator/>

            <!-- Flowable Design -->
            <action id="Flowable.PullFromDesign"
                    class="com.flowable.atlas.action.PullFromDesignAction"
                    icon="AllIcons.Actions.Download"/>
            <action id="Flowable.ConfigureDesignConnection"
                    class="com.flowable.atlas.action.ConfigureDesignConnectionAction"/>
            <separator/>

            <!-- Maintenance -->
            <action id="Flowable.RebuildModelIndex"
                    class="com.flowable.atlas.action.RebuildModelIndexAction"/>
            <!-- Debug helper; visible only with idea.is.internal=true. -->
            <action id="Flowable.DumpKeyIndex" internal="true"
                    class="com.flowable.atlas.action.DumpFlowableIndexAction"/>
        </group>
    </actions>
</idea-plugin>
