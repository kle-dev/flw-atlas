<!-- require-restart: the plugin registers languages / file types / parser definitions (the two
     expression dialects) which cannot be loaded dynamically, so force IntelliJ to prompt for a
     restart on install / update / uninstall instead of silently half-loading. -->
<idea-plugin require-restart="true">
    <id>com.flowable.atlas</id>
    <name>Flowable Atlas</name>
    <!-- No <version> here on purpose: patchPluginXml injects the Gradle project version at build
         time (root build.gradle.kts → allprojects { version }). A literal here is dead text that
         drifts — this one had been reading 0.10.4 since three releases ago. -->
    <vendor email="kevin.klever@flowable.com" url="https://www.flowable.com">Flowable</vendor>

    <description><![CDATA[
        <p><b>The complete Flowable companion for IntelliJ IDEA.</b> Flowable Atlas turns the model keys
        and expressions scattered across your Java code and Flowable models into first-class, IDE-aware
        references — completed, validated and navigable — and maps your whole project into a single
        interactive explorer. Everything is resolved against the models that <i>actually live in your
        repository</i>, so a wrong key or a broken expression is caught in the editor, long before
        deployment.</p>

        <p>Zero configuration: open a project that contains the Flowable models you exported from Design
        (the app <code>.zip</code> / deployment <code>-bar</code>, holding
        <code>.bpmn / .cmmn / .dmn / .form / .data / .service / …</code>) or the Flowable Design
        <code>*-models</code> workspace, and start typing. The plugin indexes every model and puts that
        knowledge to work across three areas.</p>

        <h3>1 &middot; Model-key intelligence</h3>
        <ul>
          <li><b>Context-aware key completion</b> at every Flowable API position that takes a model key —
              process, case, decision, form, event, channel, data object, master data, service, action,
              agent, knowledge base, template, security policy, query, variable extractor, sequence, SLA,
              dashboard component, data dictionary, page and unified work definitions. Both
              <code>org.flowable.*</code> and <code>com.flowable.*</code>. Search by <b>key, by name, or by
              any fragment</b> — type <code>0061</code> to get <code>DEMO-DO-0061</code>.</li>
          <li><b>Cascade completion</b> for fluent chains — <code>operation("…")</code> completes the
              operations of the data object / service resolved from the sibling
              <code>definitionKey(…)</code> / <code>serviceKey(…)</code> (constant references are
              resolved), and <code>value("…", …)</code> completes that operation's input fields. DMN
              <code>variable("…", …)</code> offers the decision's input/output variables.</li>
          <li><b>More than keys</b> — completion for message &amp; signal names, process variables,
              task-definition keys and activity IDs, <b>scoped</b> to the
              <code>processDefinitionKey</code> / <code>caseDefinitionKey</code> in the same query chain
              when present, so you see only that model's ids.</li>
          <li><b>Model &rarr; model references in BPMN/CMMN XML</b> — the keys that live inside the models
              themselves (<code>callActivity calledElement</code>, <code>flowable:formKey</code>,
              <code>decisionRef</code>, CMMN <code>caseRef</code> / <code>processRef</code>) get the same
              completion, Ctrl/Cmd-click navigation and broken-key inspection as Java call sites — so a
              <code>calledElement</code> pointing at a missing process is caught before deployment.</li>
          <li><b>Broken-key inspection</b> — an unknown key (in Java or model XML) is flagged inline with a
              <i>"did you mean …?"</i> quick-fix to the closest real key.</li>
          <li><b>Navigation &amp; Find Usages</b> — Ctrl/Cmd-click a key literal to jump straight to the
              model file; hover for its type, name and location. Find Usages on a delegate class or an
              <code>${bean.method()}</code> expression lists the model files that reference it.</li>
          <li><b>Code generation</b> — generate a typed Java constants class
              (<code>FlowableModelKeys.Process.ORDER_FULFILMENT</code>, model name in Javadoc) that stays
              in sync as models are added, renamed or removed; or, with Alt/Option-Enter on a data-object
              <code>definitionKey("…")</code>, a typed Java bean built from the model's field mappings.</li>
          <li><b>Liquibase aware</b> — inside a changelog it completes <code>&lt;column&gt;</code> names
              and <code>tableName</code> from the backing service model, and flags columns that no
              backing Flowable model defines.</li>
        </ul>

        <h3>2 &middot; Flowable expression language support</h3>
        <ul>
          <li><b>Both dialects, first-class</b> — the backend JUEL dialect (<code>${…}</code> /
              <code>#{…}</code>) and the frontend form dialect (<code>{{…}}</code>) each get syntax
              highlighting, rainbow parentheses and brace matching.</li>
          <li><b>Live validation</b> — structural syntax errors, and unknown functions, namespaces or
              <code>flw.*</code> members, are flagged against a catalog of the real, verified Flowable
              function set.</li>
          <li><b>Expression completion</b> — functions, engine root objects, and your project's own
              process/case variables and form fields.</li>
          <li><b>Recognised inline, everywhere expressions live</b> — BPMN/CMMN/DMN XML,
              <code>.form</code> / <code>.page</code> JSON, and (optionally) Java string literals — so the
              highlighting, validation and completion follow you into the models you edit.</li>
          <li><b>Real Groovy &amp; JavaScript in script bodies</b> — BPMN/CMMN script tasks, listener
              scripts and action-bot scripts get the IDE's own language support injected inline:
              compiler-grade syntax errors, highlighting and fragment editing (Alt-Enter). Groovy is
              bundled in every IDEA; JavaScript lights up on Ultimate. JUEL or unknown script formats
              keep the plain expression injection. Atlas additionally runs its own dependency-free
              structural script check (unterminated strings/comments, unbalanced brackets,
              <code>scriptFormat</code> typos) in the CLI and the explorer's Checks tab.</li>
          <li><b>Flowable environments</b> — define your DEV/QA/UAT/PROD servers once for the whole IDE
              instead of once per repository, and pick them from a list. Each environment holds a Flowable
              Design connection, a Flowable Work connection, or just one of the two; the Design pull and the
              Expression Playground point independently, so running against QA while models still come from
              DEV1 is an ordinary way to work. Mark an environment <i>Protected</i> and Atlas asks before
              pulling from it or evaluating against it. Passwords and tokens stay in the IDE password
              safe.</li>
          <li><b>Expression Playground</b> (the <i>Flowable Expressions</i> tool window) — type an
              expression, pick backend or frontend, and get instant validation and completion; scope
              completion to one model; evaluate a frontend expression live against a pasted JSON payload;
              or evaluate a backend expression against a running app through the Flowable Inspect REST API.
              Which app it runs against is one of your <b>environments</b>, and the fastest way to switch is
              to paste a Work URL: the environment, the scope and the instance id all fill in from the
              link.</li>
          <li><b>Script Playground</b> (the <i>Scripts</i> tab of the same tool window) — paste or
              write a Groovy/JavaScript/Python script: the IDE's real language editing (completion,
              coloring) where the language plugin is available, live structural validation with
              squiggles and clickable problem rows (the same checks the CLI and the explorer's Checks
              tab run), the scope variables the script touches as chips, a picker that loads any
              script task / listener / action-bot script straight from the project's models, and a
              library of worked examples — a complete, commented script for every context and
              language, to start from rather than from an empty editor.</li>
        </ul>

        <h3>3 &middot; Atlas explorer &amp; hub</h3>
        <ul>
          <li><b>Atlas Hub</b> — a tool window (right stripe) that is the plugin's control center:
              model-index status with per-type counts and one-click rebuild, every generated explorer
              page (double-click to open), and the Flowable Design sync state — kept up to date live.</li>
          <li><b>Map your whole project</b> — Tools → Flowable Atlas → <i>Generate Atlas Explorer…</i>
              turns your models + Java + Liquibase into a single self-contained, offline, interactive
              HTML page. Save it into the project and open it in your browser or right inside the IDE
              (embedded viewer), then click through every relationship in both directions. In
              Settings → Generation you pick exactly which artifacts are written — summary &amp;
              overview Markdown, the graph JSON, the explorer HTML, a CLAUDE.md primer for AI agents.
              Generation runs entirely in-process — no external tools required — and honours the
              project's expression allowlist.</li>
          <li><b>IDE-native viewing</b> — the embedded explorer follows the IDE light/dark theme
              (switching restyles live) and carries a toolbar with <i>Regenerate</i>, <i>Reload</i>
              and <i>Open in Browser</i>.</li>
          <li><b>Reopen without regenerating</b> — <i>Open Atlas Explorer</i> (Atlas Hub, or Tools →
              Flowable Atlas) opens an already-generated <code>*.explorer.html</code> — including
              one written by the standalone <code>atlas</code> CLI into the configured output folder — as a
              center editor tab (like a DB table), so you never have to leave the IDE for a browser.</li>
        </ul>

        <h3>How it feels</h3>
        <ul>
          <li><code>runtimeService.startProcessInstanceByKey("&lt;caret&gt;")</code> → your process keys.</li>
          <li><code>cmmnRuntimeService.createCaseInstanceBuilder().caseDefinitionKey("&lt;caret&gt;")</code> → your case keys.</li>
          <li><code>dataObjectRuntimeService.createDataObjectInstanceQuery().definitionKey(KEY).operation("&lt;caret&gt;")</code>
              → that data object's operations; <code>.value("&lt;caret&gt;", …)</code> → the operation's fields.</li>
          <li>Press completion twice to list <b>all</b> model keys of every type, regardless of the call site.</li>
        </ul>

        <p>Completion fires inside string literals and at bare arguments alike (inserting a quoted key),
        with Flowable keys ranked to the top — no setup beyond opening a project that contains your
        Flowable models.</p>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.15.0</h3>
        <ul>
          <li><b>Flowable environments, defined once for every project</b> — the plugin knew exactly one
              Design server and exactly one running app, so working against DEV1, DEV2, QA, UAT and PROD
              meant retyping a URL, an auth mode, a workspace and an app list every time you switched, and
              doing it again in the next repository. There is now an IDE-wide list of environments under
              <i>Settings → Tools → Flowable Atlas → Environments</i>: a tree of environments, each holding
              a <b>Flowable Design</b> connection, a <b>Flowable Work</b> connection, or one of the two — a
              QA stage with a running app and no Design server is an ordinary thing, shown without any
              warning. Copy an environment to clone it for the next stage; reorder them, because DEV → QA →
              UAT → PROD is a pipeline and alphabetical puts PROD second. Passwords and tokens stay in the
              IDE password safe, keyed by URL, so they are never in a shared file.</li>
          <li><b>Control and Hub addresses belong in the environment too</b> — a stage is not only the two
              servers Atlas talks to; it is also the Flowable <b>Control</b> and Flowable <b>Hub</b> pages
              you open by hand, and keeping those in bookmarks while the URLs beside them live in the IDE
              was the obvious gap. They are a URL and nothing else: no username, no password, no <i>Test
              Connection</i>, because Atlas never calls them — a form asking for a password nothing would
              read is worse than no form. Everything else about an environment applies unchanged, including
              copying it for the next stage.</li>
          <li><b>Open an environment in the browser from the Atlas Hub</b> — its toolbar's <i>Open
              Environment in Browser</i> lists every address in the catalog, grouped per stage, and hands
              the one you pick to the browser: Design, the app, Control, Hub. Not just the two new kinds — a
              Design base URL <i>is</i> the Design UI and a Work base URL <i>is</i> the app, so leaving them
              out would have meant keeping bookmarks for exactly the two addresses Atlas knows best. It
              follows neither the pull's environment nor the playground's: a third rule about which one it
              means is one more thing that can silently be wrong, so it asks, and speed search makes the
              asking a keystroke. A protected stage shows its lock in the list and nothing more — opening a
              page changes nothing, and a confirmation on a link would be theatre.</li>
          <li><b>Which environment a project uses is two choices, not one</b> — the Design pull and the
              Expression Playground point independently, and that is the point rather than an oversight:
              running against QA while models still come from DEV1 is a normal way to work. Every picker
              only offers environments that actually have a server of that kind, so choosing a runtime can
              never leave you unable to pull. With a single environment configured, nothing has to be chosen
              at all and no dropdown appears.</li>
          <li><b>Pasting a Work URL is now the fastest way to switch environment</b> — the playground's
              <i>App URL</i> field already filled in the scope and instance id from a pasted link; it now
              recognises which of your environments the link belongs to and switches to it, so one paste
              moves the connection, the scope and the instance together. A link matching no environment is
              used as-is, as a target that lives for this IDE session. What it no longer does is write that
              URL into the project's committed settings — a QA link pasted for one evaluation used to become
              the whole team's configured server.</li>
          <li><b>Every pasted link keeps its own entry</b> — the playground held exactly one pasted target,
              so the second link silently evicted the first, which is backwards: pasting two links is what
              you do when you are comparing two apps, and re-pasting the one you just looked at was the cost
              of a decision you never made. They all sit in the picker now, marked <i>(this session)</i>,
              and the button beside it is where they are managed: <b>Forget</b> one, forget all of them, or
              — for the one that turns out to be a target you keep coming back to — <b>Save as an
              Environment…</b>, which asks for the single thing that was missing. The name may be one that
              already exists: a stage with a Design server and no app joins it rather than making a second
              environment with the same label. Credentials typed in the paste dialog go with it, into the
              password safe, so nothing has to be typed twice. Forgetting a target takes whatever was
              captured for it along — a cookie left behind for a URL that is in no list any more is a
              credential nobody can see and nobody asked to keep.</li>
          <li><b>Protected environments</b> — tick <i>Ask before pulling from or evaluating against this
              environment</i> and Atlas asks first, and marks it with a lock wherever it can be picked. A
              pull asks modally, because it replaces archives in the working tree; an evaluation asks from a
              small confirmation with <i>Cancel</i> preselected, so declining is one keystroke. There is no
              "don't ask again": a guard you can switch off is not a guard. The check follows the
              <b>URL</b>, not the picked connection, so it cannot be walked around by pasting a link.</li>
          <li><b>Settings and the Atlas Hub cannot drift apart any more</b> — configuring something and
              seeing no effect in the Hub was a real defect, not a feeling. <i>Generation</i> had no
              notification at all, so changing the Atlas output folder left the Hub listing artifacts from
              the old one; every Atlas settings page now publishes from a <code>final</code> method, which
              makes forgetting a compile error rather than something a review has to catch. Switching
              connection now also drops the Hub's cached workspace and app lists, which used to keep showing
              the previous server's app names. The playground re-reads on a sub-project switch. And a
              personal app selection that the shared default has caught up with is dropped instead of
              masking it — that mask was most of what "editing Settings does nothing" was made of.</li>
          <li><b>Settings holds the environment list and nothing else</b> — no page for "which environment
              this project uses", because a second copy of that choice could not be told apart from the one
              in the Atlas Hub. There was a shared default in Settings and a personal override in the Hub,
              and the pair drifting is most of what "I configure something and it has no effect" was made
              of: the honest question — <i>is this the setting, or my copy of it?</i> — had no answer on
              screen. Now the environment, its workspace and its apps are picked in the Hub, beside the
              models they fetch; the runtime environment is picked in the playground, beside the expression
              it evaluates; and what you pick <b>is</b> the setting. The one project-level field left, the
              folder pulled archives are written to, moved to <i>Generation</i> next to the other output
              folders.</li>
          <li><b>The Atlas Hub's Design section is the whole pull, in order</b> — environment, workspace,
              apps, <i>Pull from DEV1</i>. Both pickers are ordinary drop-downs rather than a status line
              with a <i>Change…</i> link: the link cost the same clicks but did not look like a choice, and
              switching is the gesture the panel is organised around. They open instantly, since the
              environment list is in memory; the workspace list is fetched when you switch environment or
              open the drop-down, not every time the panel is drawn. Everything a drop-down offers can be
              selected — an entry that sends you to Settings to first create what it promised is not a
              choice. The runtime environment sits in its own section beside <i>Open Playground</i>.</li>
          <li><b>The workspace and app selection is per environment</b> — a workspace key belongs to one
              server, so a single project-wide value was right for at most one environment and silently
              wrong on the next. Switching environment lands in the two pickers below it: the new server's
              workspaces are read right then, and that environment's saved workspace and apps come back
              ticked. A workspace or app list that could not be read is no longer remembered as read, so the
              next refresh tries again instead of leaving an empty list looking like an answer.</li>
          <li><b>Environments a repository ships to its team</b> — an environment list was a thing every
              developer built by hand, from a wiki page or a colleague's screenshot, and four people typing
              four URLs get at least one of them wrong. A project can now define its own: <i>Settings →
              Environments → <b>Share with Project</b></i> writes the selected environment into
              <code>.idea/flowable-environments.xml</code>, and everyone who clones the repository has it in
              every picker, marked <i>(project)</i>, with no configuration at all. Committed like
              <code>.idea/flowable-atlas.xml</code> beside it, so a URL that moves is a commit rather than
              six settings dialogs. <b>No credentials, and not as a rule someone has to remember</b> — the
              file's schema has a name, the <i>Protected</i> flag and one URL per kind, and no field a
              username or a password could go in. Each developer signs in as themselves, from the IDE
              password safe as before; a shared login is one audit trail with everyone's name missing from
              it. Your own list still wins: define an environment of the same name and it shadows the
              project's entirely, which is how you point <i>QA</i> at your own instance without arguing with
              the repository. A shared entry is read-only here — <i>Copy Environment</i> turns it into one
              of yours in one click — and the file is re-read when a <code>git pull</code> changes it, so
              the Hub cannot go on offering a URL that has moved.</li>
          <li><b>The Environments page fits its dialog again</b> — username and password shared one row, and
              a text field with no column count reports its <i>text</i> as its minimum width, so a long
              Design URL made the page demand more room than the settings dialog has and pushed the password
              field off the right edge. A row per field, and every URL field bounded.</li>
          <li><b>The Hub stops spending its height on empty boxes</b> — the explorer list reserved eight
              rows whether or not anything had been generated, and the app list a fixed ~140px for a
              workspace that usually holds one app: between them, most of a panel that has five sections to
              fit into one narrow stripe. Both are sized to what they hold now, an empty section is a single
              grey line, and a workspace with twenty apps scrolls at eight rows instead of pushing <i>Pull
              from …</i> off the bottom.</li>
          <li><b>“Which Flowable project” is a drop-down</b> — it was a line of text with a <i>Change…</i>
              link underneath, and the link was hidden whenever detection had not turned anything up, so in
              a repository holding several apps the answer to "can I pick one?" was a blank space. Now it is
              the same kind of picker as the environment rows, always offering the whole repository, and it
              costs one line instead of two. The amber <i>"3 found — choose one"</i> stays until a choice is
              actually made — including a deliberate <i>whole repository</i>, which the plugin can tell
              apart from a default nobody looked at.</li>
          <li><b>“Not set” is now something the plugin can actually hold</b> — with a single environment
              defined, picking <i>not set</i> did nothing: it unset the pointer, and "nothing stored"
              already meant <i>"you have one environment, use it"</i>, so the rule answered with the same
              environment the user had just deselected. The picker read <i>not set</i> while the workspace
              and the ticked apps underneath it stayed, and a pull would have run against that environment.
              A deliberate <i>not set</i> is stored as such now — it beats the single-environment
              convenience, which still applies when no choice was ever made — and it empties the two pickers
              below, because a panel still showing the previous environment's apps reads as a selection that
              carried over, which is exactly what a pull must never do. Choosing the environment again
              brings its workspace and apps back; saying <i>not set</i> is not deleting anything.</li>
          <li><b>Two environments may point at the same server</b> — one Design server hosting a DEV
              workspace and a QA workspace is an ordinary setup, and the first cut refused to save it on the
              grounds that the two would share a saved password. They do share it, and that is correct: same
              server, same login. A rule derived from how credentials happen to be keyed had no business
              forbidding a real-world layout.</li>
          <li><b>A pasted link to a task inside a case evaluates</b> — it answered <i>"Internal server
              error"</i> before. Flowable's <code>subScopeId</code> is a <b>plan item instance</b> id, and
              no Work route exposes one, so putting the task id from <code>…/case/CAS-1/task/TSK-2</code>
              there made the engine look for a plan item that does not exist. A named task is the more
              specific scope and the engine evaluates it directly, so that is what a link now resolves to.
              The playground's <i>Sub-scope id</i> field says what it wants, too: it read "Optional", which
              was true and useless.</li>
          <li><b>The paste dialog can fix credentials for an app it already knows</b> — it hid the username
              and password once the app was recognised, which looked tidy and was a dead end: a saved
              password that was empty or wrong could only be corrected in Settings, and the only clue was a
              401 from the next evaluation. The fields are shown either way, prefilled from what is
              stored.</li>
          <li><b>An app or Design server bound to IPv6 only is reachable again</b> — and this was the most
              misleading error Atlas could give: <i>"nothing is listening on that host and port"</i>, about
              a server the user had open in a browser. The JDK's HTTP client connects to the <b>first</b>
              address a name resolves to and never falls back to the others; on macOS <code>localhost</code>
              resolves to <code>127.0.0.1</code> first, so a Vite/node dev server listening on
              <code>[::1]</code> was unreachable from the IDE while <code>curl</code> reached it happily.
              Both clients now retry the host's remaining addresses when the connection itself fails — an
              HTTP answer of any status is still the server's own answer and is never retried.</li>
          <li><b>A connection test says something a person can act on</b> — <i>Test Connection</i> on a
              Flowable Work connection probed the Inspect endpoint with a <code>GET</code>, which Flowable
              answers with a <i>500</i>: a perfectly healthy local app was reported as an internal server
              error, spelled out as a slab of truncated JSON. It now probes the app itself and says what
              happened — reachable, credentials rejected, wrong context path, or unreachable and why — and
              no status line anywhere prints a raw response body. A failed test never blocked saving and now
              says so, since an app that is simply not running yet is the most ordinary reason to see
              one.</li>
          <li><b>The connection fields are the width of the dialog</b> — a URL field rendered about as wide
              as the word <code>http:</code>, because a filled cell still sits at its minimum width unless
              its column may grow.</li>
          <li><b>The environment editor's Add button opens its menu at the button</b> — it opened in the
              bottom-left corner of the screen when the list was still empty, which is exactly when a
              first-time user needs it, and a popup stranded there holds the whole Settings dialog, so every
              other page looked as if it were loading forever.</li>
          <li><b>The two expression dialects keep their own expression</b> — switching the playground
              between Backend and Frontend carried the text across, which looked like "your work is
              preserved" and was the opposite: the expression already parked in the other dialect was
              replaced, with no way back to it. They are different languages against different scopes, and
              each now has its own scratch text.</li>
          <li><b>Pasting a Work URL is one dialog, and it checks the connection</b> — the playground's
              backend card was carrying the whole flow in the open: a URL field, a sentence explaining what
              the field does, a <i>Save as environment…</i> link that only sometimes applied, and an
              "unsaved" state in the environment picker. Four controls and three sentences for something
              that happens in one gesture. <b>Paste Work URL…</b> now opens a dialog that resolves the app,
              the scope and the instance from the link, and — when the app is not one of your environments
              yet — asks how to get in, and <i>tests the connection before it closes</i>. Getting in means
              the same two routes the environment editor offers: a username and password in the open, since
              that is the common case, and <i>Sign in via Browser…</i> / <i>Paste Session…</i> beside the
              test for an app behind an identity provider — or for a bearer token pasted from a cURL. They
              are links rather than a mode selector because they are not alternatives: an SSO-fronted
              Flowable can want the session <i>and</i> basic auth behind it, and the request sends whatever
              is there. A one-off target stays a listed choice in the picker for as long as it lives, marked
              <i>(this session)</i> — it is never added to the environment list, so if it vanished the
              moment you glanced at another environment there would be no way back to it short of pasting
              the link again. And it outranks the "there is only one environment, use it" convenience: with
              a single Work environment defined, that fallback used to quietly re-select it the instant a
              link was pasted, so the pasted app never became the target and the picker showed an
              environment nobody had chosen. An <i>explicit</i> pick still wins — that is the user changing
              their mind. It <b>creates nothing</b>. A link from a colleague, a one-off look at a stage you
              do not work against, an app you will never open again: none of those should leave an
              environment behind, and being made to name one before you can evaluate is a toll on the common
              case. An unknown app becomes a target for this IDE session — the picker says <i>"(this
              session)"</i> — and its credentials go to the same in-memory store a browser sign-in uses, so
              nothing typed there reaches the disk. An environment is something you decide to have, in
              <i>Settings → Environments</i>. The card is down to the environment, the scope and the
              instance id, with no explanatory text left to read. An app you have not registered is
              therefore usable, with authentication, in one pass; before, an unknown URL could only be
              evaluated against if its password happened to already be in the keychain.</li>
          <li><b>Generation is a page with three child pages</b> — Model Constants, Liquibase and
              Data-Object DTOs each get their own. On one page they were four screens of fields with no
              hierarchy, so finding the DTO class-name pattern meant scrolling past the Liquibase rename
              regex.</li>
          <li><b>Two new actions and one rename</b> — <i>Switch Design Environment…</i> and <i>Switch Work
              Environment…</i> put the switcher in the Tools menu and in Find Action; <i>Configure Design
              Connection…</i> became <i>Manage Environments…</i>.</li>
          <li><b>The Atlas Hub footer is just the version now</b> — it also read <i>"verified on 2026.2 —
              2026.1 is untested"</i>, which is our release process on display in a panel people keep open
              all day, and nothing a reader can act on. The verified range has not moved: it is still stated
              in the reference documentation, and every bug report submitted from Atlas carries the running
              IDE's branch and whether it is inside that range — which is the one place the distinction
              changes what happens next.</li>
          <li><b>A fresh Design connection no longer reads as "Not configured"</b> — the Hub called a
              connection unconfigured until a workspace <i>and</i> at least one app had been saved, and hid
              the whole section while it did. So a configured server whose default workspace holds no apps
              left nothing to tick and no way to tick it: the pickers that finish the setup sat behind the
              condition they exist to satisfy. <i>Not configured</i> now means <i>no server</i>; the
              workspace and app pickers appear as soon as there is one, reading <i>none selected</i> and
              <i>Choose a workspace</i> until something is picked. A pull that is missing a workspace or an
              app now says which of the two it is, instead of reopening Settings.</li>
          <li><b>Pick the Design workspace in the Hub</b> — the <i>Flowable Design</i> section let you
              choose which apps a pull fetches, but not the workspace they come from: that was fixed to
              whatever <i>Settings → Connections</i> held, so working against a second workspace for an
              afternoon meant editing the shared project settings, which is a VCS-tracked file the whole
              team reads. The workspace now has its own picker in the Hub, right above the app list, and it
              is part of the same <b>personal override</b> — kept workspace-locally, marked <i>(personal
              selection)</i>, undone by <i>Reset to configured</i>, and dropped by itself as soon as the
              pick lands back on the configured workspace. A switch starts with nothing ticked, because one
              workspace's apps do not exist in the next one.</li>
          <li><b>A Design server on a plain <code>http://</code> port no longer times out</b> — pointing the
              connection at, say, <code>http://localhost:10014</code> could fail with <i>"Cannot reach …
              request timeout"</i> while the very same URL answered a <code>curl</code> instantly. The JDK's
              HTTP client defaults to HTTP/2, which over cleartext is an h2c <i>upgrade</i> request, and a
              server that neither completes nor declines that upgrade leaves the request hanging until the
              timeout. The Design and Inspect clients now speak plain HTTP/1.1; <code>https://</code> still
              negotiates HTTP/2 through ALPN, so nothing is given up for it.</li>
          <li><b>One reload icon for the two Design lists</b> — the <i>Refresh apps</i> link sat under the
              app list next to <i>Reset to configured</i>, where it read as part of resetting rather than as
              a reload. It is now a refresh icon beside the workspace it re-reads, and it reloads both
              server lists: the workspaces and the apps.</li>
          <li><b>The Hub stops asking to be widened</b> — it is read in a side panel a few hundred pixels
              wide, and a lot of it was sized as if that were negotiable. The app list pinned 320px of width
              and now takes the width it is given; the Design status line spelled out every ticked app key
              and now says <i>N apps selected</i> past three; a long workspace name pushed <i>Change…</i>
              and the reload icon past the right edge and is now shortened, with the full name and key in
              its tooltip. An explorer row showed the full project-relative path and a timestamp behind the
              file name — it now shows the tail of the folder, with path and generation time in the row's
              tooltip. An app list with nothing in it is a single line of text instead of a tall empty box
              holding a centred label the panel then clipped.</li>
          <li><b>Configuring Inspect in Settings now reaches the Expression Playground</b> — the
              playground's backend card and <i>Settings → Connections</i> edit one and the same connection,
              but the card read the settings only when it was first built and then outlived every trip to
              Settings. So typing a base URL there changed nothing you could see, and the next <i>Evaluate
              Against App</i> wrote the card's stale value straight back over what you had just configured.
              Applying the settings now updates an open playground; a field you have typed into yourself
              keeps its text, because the settings only reclaim what they put there.</li>
          <li><b>The Frontend dialect no longer wears the Atlas Explorer's icon</b> — in the Expression
              Playground the <i>Frontend</i> toggle used <code>AllIcons.General.Web</code>, the same globe
              the <i>Open Atlas Explorer</i> button uses a few pixels away. It is now the form icon, which
              is also where a frontend expression actually sits.</li>
          <li><b>The Hub toolbar is openers only</b> — <i>Generate Atlas Explorer</i> and <i>Pull from
              Design</i> were there twice over: once as a toolbar icon, once as a link in the very section
              whose state they change. Doing something to the project belongs next to that thing's state, so
              the toolbar now holds only ways to get somewhere: <i>Open Atlas Explorer</i>, the new <i>Open
              Expression Playground</i> (the tool window's own icon — it had no button anywhere, only a link
              at the very bottom of the panel), and <i>Refresh</i>. Both removed actions stay where they
              were in the sections and under Tools → Flowable Atlas.</li>
          <li><b>The Script Playground has examples to start from</b> — the <i>Scripts</i> tab could load a
              script out of the project's own models, which is exactly the wrong direction when the question
              is <i>how does one write these</i>: it can only show you what somebody already wrote. <i>Load
              Example…</i> now offers a library of complete, working scripts, at least one for every script
              context and every language the tab edits, from reading and writing variables through transient
              and local scope, JSON, dates, the engine services, a multi-instance collection, raising a BPMN
              error rather than failing the job, a CMMN plan item and an action bot's inputs and outputs.
              Each one lands in the editor as ordinary text to edit, with the comments that explain the
              decision it demonstrates — why a transient variable, why a task listener binds
              <code>task</code> and never <code>execution</code>. They are held to the same standard as the
              code around them: the build runs every example through the script validator in its own context
              and fails on a single warning, so an example can never ship the squiggle it would draw.</li>
        </ul>
        <h3>0.14.0</h3>
        <ul>
          <li><b>Atlas can update itself</b> — with no Marketplace listing, nothing ever told you a new
              version existed; the ZIP you installed months ago just kept running, silently old. Every
              release now publishes a custom plugin repository, so adding one URL under <i>Settings →
              Plugins → ⚙ → Manage Plugin Repositories…</i> puts Atlas in the normal update flow — update
              badge, one click, no download. The URL always resolves to the newest release, and the
              compatibility range in it is read out of the built plugin rather than written by hand, so it
              cannot advertise a version the IDE would then refuse to install.</li>
          <li><b>Cancelling is no longer reported as a failure</b> — pressing <i>Cancel</i> on a running
              Atlas action told you it had broken. Generating the explorer or the artifact set said
              <i>"Failed to generate the Atlas explorer"</i>, a Design pull or connection test said
              <i>"Design request failed"</i>, and an Inspect evaluation reported an empty error. All three
              were the cancellation itself being caught and dressed up as an error. Eleven places that catch
              broadly now let a cancellation through untouched. The same bug had a quieter form in the
              editor: when a background scan was cancelled mid-inspection, the <i>implicit usage</i> check
              read that as "no index", and a class referenced only from a model could be greyed out as
              unused.</li>
          <li><b>Find Usages no longer blocks typing</b> — invoking <i>Find Usages</i> on a delegate class
              or bean while the model index was cold built the whole index while holding the read lock, so
              every keystroke and file refresh queued behind a full model scan. The lookup now reads the PSI
              under a short lock, builds the index outside it, and takes the lock again only to report
              results. The index service had always split itself this way internally; this one caller had
              been wrapping the whole thing back up again.</li>
          <li><b>Stricter XML parsing</b> — model files are treated as untrusted input, and the parser said
              so, but only <i>external</i> DTDs and entities were actually refused. An internal DTD subset
              still expanded, which is the shape a decompression-bomb document takes. A <code>DOCTYPE</code>
              is now rejected outright — real model files never carry one. The parser also gets one instance
              per thread instead of one shared across the IDE's index scan and the CLI's walk, where neither
              class is specified as thread-safe.</li>
          <li><b>Releases can be signed</b> — Atlas installs by side-loading a ZIP, with no Marketplace
              vouching for it, so nothing distinguished our build from a substituted one. Release artifacts
              are now signed when a key is configured, and the signature is verified before publishing.
              <code>SHA256SUMS.txt</code> alone could never establish this: whoever can replace the download
              can replace its checksum line in the same breath.</li>
          <li><b>Attribution for the embedded typeface</b> — the explorer HTML embeds the Geist font, which
              ships under the SIL Open Font License and obliges its notice to travel with the font. Because
              a generated <code>.explorer.html</code> is a redistribution with no repository attached, the
              notice now lives inside the generated stylesheet as well as in the new
              <code>THIRD-PARTY-NOTICES.md</code>. The repository <code>LICENSE</code> had also claimed that
              everything in it was Flowable AG's property, which stopped being true the day the font was
              embedded; it now carves out the two third-party components and says in plain words that the
              source is readable, not usable.</li>
          <li><b><code>SECURITY.md</code> and <code>CONTRIBUTING.md</code></b> — a security problem now has
              somewhere to go that is not a public issue, together with the design decisions worth knowing
              before reporting one (Atlas transmits nothing on its own, credentials live in the
              PasswordSafe, generated artifacts contain your project's data by construction).
              <code>CONTRIBUTING.md</code> is honest about what the licence permits and documents the build,
              the release gates and which files are generated rather than written.</li>
          <li><b>Placeholder keys everywhere</b> — every example model key, namespace and table name in the
              code, tests and documentation is a <code>DEMO-*</code> placeholder. Some had been carried over
              from a real project, and this repository is public.</li>
        </ul>
        <h3>0.13.0</h3>
        <ul>
          <li><b>Report a problem straight from the error dialog</b> — an Atlas exception used to reach the
              IDE's generic "Report to JetBrains" dialog, which discards third-party plugin reports, so the
              only way to hand one over was to dig <code>idea.log</code> out of <i>Help → Show Log</i>. The
              dialog now offers <b>Report Flowable Atlas Problem…</b>: it assembles the environment (Atlas
              and IDE version, verified platform range, OS, JRE) plus the stack traces, copies that to the
              clipboard and opens the issue tracker. Nothing is transmitted by the plugin itself — a trace
              can carry model keys and file paths from your project, so you see the text before it
              moves.</li>
          <li><b>Failures leave a trail</b> — around forty places swallowed their exception silently. The
              worst were the credential stores: typing a Design or Inspect password and hitting <i>Apply</i>
              could fail to write it to the PasswordSafe (locked keychain, "do not save passwords" mode) and
              say nothing, so the next pull asked again for no visible reason. Those now log, as do a failed
              Design pull, custom-function extraction (whose failure made the inspection flag your own
              <code>flw.*</code> functions as unknown), sub-project detection and explorer discovery. Hot
              paths log at debug or once, never per file, so <code>idea.log</code> stays readable.</li>
          <li><b>The Atlas Hub states what was actually verified</b> — its footer reads <i>"verified on
              2026.2"</i> and flags the running IDE whenever it falls outside that, in either direction.
              Atlas installs on 2026.1 and later on purpose: it ships as a ZIP with no update channel, so a
              tight <code>until-build</code> would make it vanish on the day you upgrade the IDE rather than
              prompt for an update. That makes the range it <i>installs</i> on wider than the range it is
              <i>verified</i> on, so a 2026.1 install is flagged as untested too — "it loads" is no longer
              confused with "it was tested".</li>
          <li><b>A CHANGELOG in the repository</b> — the release history only existed inside the plugin
              descriptor, where the IDE's plugin manager shows it, leaving CLI users and anyone reading the
              repo on GitHub with no way to see what changed. <code>CHANGELOG.md</code> is now generated
              from these very notes, so the two cannot drift.</li>
          <li><b>Removed a platform API scheduled for deletion</b> — three combo/list renderers used a
              <code>SimpleListCellRenderer.create</code> overload JetBrains has marked for removal, which
              would have broken the plugin on a future IDE. Replaced with the supported
              <code>textListCellRenderer</code>. JetBrains' Plugin Verifier now reports no
              scheduled-for-removal usage at all.</li>
          <li><b>Fixed two gates that were not gating</b> — the tests that keep
              <code>CLAUDE.template.md</code> and <code>CHANGELOG.md</code> in step with their generators
              compare files outside the module's source sets, which Gradle could not see: hand-editing
              either left <code>./gradlew build</code> green on exactly the drift those tests exist to
              catch. They are declared inputs now, verified by injecting a change and watching the build
              fail. Also added the repository's <code>LICENSE</code>.</li>
        </ul>
        <h3>0.12.2</h3>
        <ul>
          <li><b>Runs on IntelliJ IDEA 2026.2</b> — one ZIP for 2026.1 and 2026.2, both checked with
              JetBrains' Plugin Verifier. 2026.2 moved JCEF (the Atlas Hub, the explorer editor, the Inspect
              sign-in browser) out of the platform core into the bundled <i>Web Browser (JCEF)</i> plugin;
              Atlas now declares that plugin as an <i>optional</i> dependency, so the same build picks it up
              on 2026.2 and ignores it on 2026.1.</li>
        </ul>
        <h3>0.12.1</h3>
        <ul>
          <li><b>DTO class names come from a pattern</b> — <i>Generate → Data-Object DTOs</i> names its
              classes the way the Liquibase dialog names its changelog files: a token pattern (<code>{name}
              {shortName} {key} {app} {suffix}</code>) plus an optional regex rename, rendered live into the
              preview's <i>Class name</i> and <i>Target file</i> columns. The default
              <code>{name}{suffix}</code> is the name you got before, a class name typed into the table
              outranks the pattern for that row, and the Alt-Enter intention proposes the same name the bulk
              dialog would. Pattern and rename are remembered per project (and per sub-project) under
              <i>Settings → Flowable Atlas → Generation</i>.</li>
          <li><b>Class names without the model key</b> — Design model names usually carry their key
              (<code>DEMO-D009 Pod Member</code>), which the derived class name repeated as noise:
              <code>DEMOD009PodMemberDto</code>. <code>{shortName}</code> drops it — the key itself when the
              name starts with it, otherwise a leading capitals-and-digits run before the first word, so a
              key written differently from the key model (<code>DEMO-D9</code> against
              <code>DEMO-D009</code>) also shortens while an acronym (<code>IBANCheck</code>) is left
              intact. <code>{shortName}{suffix}</code> is the one-token way to
              <code>PodMemberDto</code>.</li>
        </ul>
        <p>Older releases: see <code>CHANGELOG.md</code> in the repository.</p>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>
    <!-- XML PSI for the Liquibase-changelog coverage inspection. -->
    <depends>com.intellij.modules.xml</depends>
    <!-- JSON PSI for form-model {{…}} expression injection and the .form → JSON association. -->
    <depends>com.intellij.modules.json</depends>
    <!-- JCEF (Atlas Hub, the explorer editor, the Inspect sign-in browser). Up to 2026.1 (build 261)
         JCEF sits in the platform core; 2026.2 (262) moved it into the bundled "Web Browser (JCEF)"
         plugin (id com.intellij.modules.jcef). Declared *optional* so one artifact covers both: on
         262 it wires that plugin's classloader in, on 261 the id doesn't exist and the dependency is
         simply skipped. Every JCEF call site is already guarded by JBCefApp.isSupported(), so a
         remote-dev / no-JCEF IDE keeps working without it. -->
    <depends optional="true" config-file="flowable-atlas-jcef.xml">com.intellij.modules.jcef</depends>

    <resource-bundle>messages.FlowableAtlasBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Services (FlowableModelIndexService, ModelConstantsService/Settings) are @Service — no registration needed. -->
        <!-- Settings tree: an app-level root page (core toggles) with three project-level children.
             parentId chaining across levels is supported — the settings tree merges them by ID. -->
        <applicationConfigurable
                parentId="tools"
                id="com.flowable.atlas.settings"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas"
                instance="com.flowable.atlas.settings.FlowableAtlasConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.expressions"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.expressions"
                instance="com.flowable.atlas.settings.ExpressionsConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.generation"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation"
                instance="com.flowable.atlas.settings.GenerationConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings.generation"
                id="com.flowable.atlas.settings.generation.constants"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation.constants"
                instance="com.flowable.atlas.settings.GenerationConstantsConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings.generation"
                id="com.flowable.atlas.settings.generation.liquibase"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation.liquibase"
                instance="com.flowable.atlas.settings.GenerationLiquibaseConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings.generation"
                id="com.flowable.atlas.settings.generation.dto"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation.dto"
                instance="com.flowable.atlas.settings.GenerationDtoConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.environments"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.environments"
                instance="com.flowable.atlas.settings.EnvironmentsConfigurable"/>
        <completion.contributor
                language="JAVA"
                implementationClass="com.flowable.atlas.completion.FlowableJavaCompletionContributor"/>
        <!-- Liquibase changelog: complete <column name="…"> / tableName from the backing service model. -->
        <completion.contributor
                language="XML"
                implementationClass="com.flowable.atlas.liquibase.LiquibaseColumnCompletionContributor"/>
        <!-- BPMN/CMMN model XML: complete model keys at cross-reference attributes
             (calledElement, flowable:formKey, decisionRef, caseRef, processRef, …). -->
        <completion.contributor
                language="XML"
                implementationClass="com.flowable.atlas.completion.FlowableXmlKeyCompletionContributor"/>
        <localInspection
                language="JAVA"
                shortName="FlowableValueField"
                displayName="Invalid Flowable data-object value field"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableValueFieldInspection"/>
        <localInspection
                language="JAVA"
                shortName="FlowableBrokenKey"
                displayName="Unknown Flowable model key"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableBrokenKeyInspection"/>
        <!-- BPMN/CMMN model XML: an unknown model key at a cross-reference attribute (broken deployment). -->
        <localInspection
                language="XML"
                shortName="FlowableXmlBrokenKey"
                displayName="Unknown Flowable model key (model XML)"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableXmlBrokenKeyInspection"/>
        <!-- Ctrl-click a key literal → jump to the model file; Find Usages works too. -->
        <psi.referenceContributor
                language="JAVA"
                implementation="com.flowable.atlas.navigation.FlowableKeyReferenceContributor"/>
        <!-- Ctrl-click a model key in BPMN/CMMN XML (calledElement, formKey, decisionRef, …) → the model file. -->
        <psi.referenceContributor
                language="XML"
                implementation="com.flowable.atlas.navigation.FlowableXmlKeyReferenceContributor"/>
        <!-- Hover / Ctrl-Q on a key literal → model type, name and file. -->
        <lang.documentationProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.navigation.FlowableKeyDocumentationProvider"/>
        <!-- Search Everywhere / Go to Symbol: every model key (→ model file) and every bot key
             (→ the BotService class and the actions that invoke it). -->
        <gotoSymbolContributor
                implementation="com.flowable.atlas.navigation.FlowableKeyGotoSymbolContributor"/>
        <!-- A "Flowable Model" tab of its own in Search Everywhere: model keys and the file paths
             inside a .bar/.zip (invisible to the Files tab), plus a live full-text search over model
             content (invisible to Find in Files). The contributor above stays — it feeds the
             platform's Symbols tab, this feeds its own. -->
        <searchEverywhereContributor
                implementation="com.flowable.atlas.navigation.se.FlowableModelSeContributor$Factory"/>
        <!-- Liquibase changelog column not mapped in the backing Flowable service/data-object model. -->
        <localInspection
                language="XML"
                shortName="FlowableLiquibaseCoverage"
                displayName="Liquibase column not defined in Flowable model"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.liquibase.LiquibaseCoverageInspection"/>
        <!-- Alt-Enter on a data-object definitionKey → generate a typed Java bean from its fields. -->
        <intentionAction>
            <language>JAVA</language>
            <className>com.flowable.atlas.intention.GenerateDataObjectBeanIntention</className>
            <category>Flowable</category>
        </intentionAction>
        <!-- Don't report Java referenced from models (delegates, ${bean.method()}, listeners) as unused. -->
        <implicitUsageProvider implementation="com.flowable.atlas.usage.FlowableImplicitUsageProvider"/>
        <!-- Find Usages / Ctrl-B on a delegate class or ${bean.method()} shows the model files that use it. -->
        <customUsageSearcher implementation="com.flowable.atlas.usage.FlowableModelUsageSearcher"/>
        <!-- Gutter icon on Java methods/beans referenced from Flowable models → navigate to the model usages. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableModelReferenceLineMarkerProvider"/>
        <!-- Gutter icon on a BotService class → navigate to the .action models that invoke the bot. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableBotActionLineMarkerProvider"/>
        <!-- Gutter icon on a @GetMapping/@PostMapping/… handler → navigate to the models calling its endpoint. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableEndpointUsageLineMarkerProvider"/>
        <!-- Gutter icon on a model-key expression — a literal or a constant/local reference at a key
             call site — whose model has a diagram (bundled Design .svg, or rendered from its DI layout)
             → open it in the IDE's built-in image/SVG viewer. No diagram (e.g. a form BAR) → no marker. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableDiagramLineMarkerProvider"/>
        <!-- Inline hint: the physical table name after a string literal that is a data-object key. -->
        <codeInsight.declarativeInlayProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.hint.FlowableDataObjectTableInlayProvider"
                isEnabledByDefault="true"
                group="VALUES_GROUP"
                providerId="com.flowable.atlas.dataObjectTable"
                bundle="messages.FlowableAtlasBundle"
                nameKey="inlay.dataObjectTable.name"
                descriptionKey="inlay.dataObjectTable.description"/>
        <!-- Inline hint: the action model's name after a string literal that is an .action key. -->
        <codeInsight.declarativeInlayProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.hint.FlowableActionNameInlayProvider"
                isEnabledByDefault="true"
                group="VALUES_GROUP"
                providerId="com.flowable.atlas.actionName"
                bundle="messages.FlowableAtlasBundle"
                nameKey="inlay.actionName.name"
                descriptionKey="inlay.actionName.description"/>
        <!-- Warn when renaming a Java symbol referenced by Flowable model expressions (text, not auto-updated). -->
        <refactoring.elementListenerProvider
                implementation="com.flowable.atlas.usage.FlowableRenameWarningProvider"/>
        <!-- Balloon shown after generating the Atlas explorer (open in browser / open in IDE). -->
        <notificationGroup id="Flowable Atlas" displayType="BALLOON"/>
        <!-- Render a *.explorer.html as an embedded (JCEF) "Atlas Explorer" tab in the center editor
             area (like a DB table); hides the raw-HTML "Text" tab for it. -->
        <fileEditorProvider implementation="com.flowable.atlas.explorer.AtlasFileEditorProvider"/>
        <!-- Second tab on the same *.explorer.html: the Flowable Expressions playground, so the map and
             the playground share one window (no separate tool window needed). Placed after Atlas Explorer. -->
        <fileEditorProvider implementation="com.flowable.atlas.explorer.AtlasExpressionEditorProvider"/>

        <!-- ==================== Flowable Expression support ==================== -->
        <!-- Two dialects of one expression language: backend ${…}/#{…} (JUEL) and frontend {{…}} (flw.*). -->
        <fileType name="Flowable Backend Expression"
                  language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableBackendExprFileType"
                  fieldName="INSTANCE"
                  extensions="flowable-be"/>
        <fileType name="Flowable Frontend Expression"
                  language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableFrontendExprFileType"
                  fieldName="INSTANCE"
                  extensions="flowable-fe"/>
        <lang.parserDefinition language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableBackendExprParserDefinition"/>
        <lang.parserDefinition language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableFrontendExprParserDefinition"/>
        <!-- Token coloring (parentheses, brackets, strings, operators, …) for both dialects. -->
        <lang.syntaxHighlighterFactory language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprSyntaxHighlighterFactory"/>
        <lang.syntaxHighlighterFactory language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprSyntaxHighlighterFactory"/>
        <!-- Match () and [] — caret on a brace highlights its partner, unmatched braces are flagged. -->
        <lang.braceMatcher language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprBraceMatcher"/>
        <lang.braceMatcher language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprBraceMatcher"/>
        <!-- Structural syntax squiggles for both dialects (playground + injected fragments). -->
        <annotator language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.annotator.FlowableExpressionAnnotator"/>
        <annotator language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.annotator.FlowableExpressionAnnotator"/>
        <!-- Semantic expression findings as real inspections: severity/enablement per profile, project
             allowlist via quick fix. No language attribute — the two dialects share no registered base
             language; the implementations bail out immediately on any non-expression file. -->
        <localInspection
                shortName="FlowableExprUnknownFunction"
                displayName="Unknown Flowable expression function or namespace"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.expr.inspection.FlowableExprUnknownFunctionInspection"/>
        <localInspection
                shortName="FlowableExprGrounding"
                displayName="Expression root is not a known variable, bean, or root object"
                groupName="Flowable"
                enabledByDefault="false"
                level="WEAK WARNING"
                implementationClass="com.flowable.atlas.expr.inspection.FlowableExprGroundingInspection"/>
        <!-- Rainbow parentheses are done in the syntax highlighter (FlowableExprSyntaxHighlighter):
             the highlighting lexer tags each round paren with its nesting level and each level gets
             a distinct colour. This paints in every surface, including the playground's embedded
             editor field where an annotator would not. Per-theme default colours + the Color Scheme
             page that makes them customizable: -->
        <additionalTextAttributes scheme="Default" file="colorSchemes/FlowableExprDefault.xml"/>
        <additionalTextAttributes scheme="Darcula" file="colorSchemes/FlowableExprDarcula.xml"/>
        <colorSettingsPage implementation="com.flowable.atlas.expr.lang.FlowableExprColorSettingsPage"/>
        <!-- Alt-Enter on an injected ${…}/{{…}} fragment → rework it in the playground, pre-filled.
             Registered once (no <language> filter): the same class must not be registered twice or the
             platform rejects it as a duplicate. isAvailable() self-gates on FlowableExprFile, which
             covers both the backend and frontend dialects. -->
        <intentionAction>
            <className>com.flowable.atlas.expr.intention.OpenInExpressionPlaygroundIntention</className>
            <category>Flowable</category>
        </intentionAction>
        <!-- Ctrl-Q / hover on a catalog function or root inside an expression → call head + doc line. -->
        <lang.documentationProvider language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.documentation.FlowableExprDocumentationProvider"/>
        <lang.documentationProvider language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.documentation.FlowableExprDocumentationProvider"/>
        <!-- Completion of functions, root objects and the project's variables/form-fields. -->
        <completion.contributor language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.completion.FlowableExpressionCompletionContributor"/>
        <completion.contributor language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.completion.FlowableExpressionCompletionContributor"/>
        <!-- Inject the expression languages inline wherever expressions live in the repo. -->
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableXmlExpressionInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableJsonExpressionInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableJavaExpressionInjector"/>
        <!-- Inject the IDE's real Groovy / JavaScript languages into Flowable script bodies (BPMN
             scriptTask, CMMN script tasks, execution/task listener scripts, action-bot scriptInfo):
             compiler-grade syntax errors right in the model file. Languages are resolved by ID at
             runtime (Language.findLanguageByID), so there is NO hard or optional plugin dependency —
             no Groovy/JavaScript plugin simply means no injection, never an error. -->
        <multiHostInjector implementation="com.flowable.atlas.script.inject.FlowableXmlScriptInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.script.inject.FlowableJsonScriptInjector"/>
        <!-- Completion for the Flowable script bindings (execution.…, task.…, flw.time.…): the real
             language completion knows nothing about these dynamic bindings, so the bindings catalog
             offers them — in the Script Playground and inside injected script bodies. language="any"
             because the host language varies (Groovy/JS/plain-text fallback) and this plugin has no
             dependency on the language plugins; the contributor gates itself cheaply. -->
        <completion.contributor language="any"
                  implementationClass="com.flowable.atlas.script.completion.ScriptBindingsCompletionContributor"/>
        <!-- Silence the Groovy/JS "unresolved" noise on the engine bindings (execution, flw, …)
             inside Flowable script bodies — they are dynamic bindings the language PSI can never
             resolve. Scoped to the playground + injected script fragments; real syntax errors and
             the bindings-catalog validation are unaffected. Registered per language (the suppressor
             lookup does not honor "any"); an entry for an absent language plugin is simply dormant,
             so this keeps the no-Groovy/JS-dependency rule intact. -->
        <lang.inspectionSuppressor language="Groovy"
                  implementationClass="com.flowable.atlas.script.inspection.FlowableScriptInspectionSuppressor"/>
        <lang.inspectionSuppressor language="JavaScript"
                  implementationClass="com.flowable.atlas.script.inspection.FlowableScriptInspectionSuppressor"/>
        <!-- Hover / Ctrl-Q on a binding shows catalog documentation instead of the platform's
             "No candidates found for method call" fallback for unresolvable dynamic bindings. -->
        <lang.documentationProvider language="Groovy"
                  implementationClass="com.flowable.atlas.script.documentation.ScriptBindingsDocumentationProvider"/>
        <lang.documentationProvider language="JavaScript"
                  implementationClass="com.flowable.atlas.script.documentation.ScriptBindingsDocumentationProvider"/>
        <lang.documentationProvider language="TEXT"
                  implementationClass="com.flowable.atlas.script.documentation.ScriptBindingsDocumentationProvider"/>
        <!-- The Script Playground's scratch file resolves imports against the whole project —
             without a module, `import org.slf4j.LoggerFactory` would never resolve there. -->
        <resolveScopeProvider implementation="com.flowable.atlas.script.toolwindow.ScriptPlaygroundResolveScopeProvider"/>
        <!-- Treat Flowable form models (.form) as JSON so their {{…}} strings become injection hosts,
             and action models (.action) so their scriptInfo.script strings can host Groovy/JS. The
             remaining JSON-serialized model extensions are here so a model OPENS with its content:
             an unregistered extension resolves to UnknownFileType, which is binary, so the editor
             shows the "file type not associated" panel instead — most visibly for an entry opened
             out of a .bar/.zip. A <fileType> with no implementationClass only adds matchers to the
             existing type (the same idiom the bundled javaFX and Maven plugins use). The list mirrors
             ModelType.EXTENSIONS in :core. -->
        <fileType name="JSON"
                  extensions="form;action;page;data;dictionary;query;sequence;sla;event;channel;agent;service;tpl;policy;extractor;knowledgebase;masterdata;dashboardcomponent;document;palette;app"/>
        <!-- The XML-serialized models. .bpmn20.xml / .cmmn.xml / .dmn.xml already end in .xml. -->
        <fileType name="XML" extensions="bpmn;cmmn;dmn"/>
        <!-- The Expression Playground: type an expression, get validation + completion. -->
        <toolWindow id="Flowable Expressions"
                    anchor="bottom"
                    secondary="true"
                    icon="/META-INF/expr-toolwindow.svg"
                    factoryClass="com.flowable.atlas.expr.toolwindow.FlowableExpressionToolWindowFactory"/>
        <!-- The Atlas Hub: the plugin's control center (model-index status, generated explorers,
             Flowable Design sync). Right anchor — the home of project control-center panels
             (Gradle, Maven, Database). -->
        <toolWindow id="Atlas Hub"
                    anchor="right"
                    icon="/META-INF/atlas-hub.svg"
                    factoryClass="com.flowable.atlas.hub.AtlasHubToolWindowFactory"/>
    </extensions>

    <projectListeners>
        <!-- Regenerates the model-constants class when models are added/removed/edited. -->
        <listener class="com.flowable.atlas.generate.ModelConstantsAutoRefresher"
                  topic="com.intellij.openapi.vfs.newvfs.BulkFileListener"/>
    </projectListeners>

    <!-- Action/group texts and descriptions come from messages/FlowableAtlasBundle.properties
         (action.<id>.text / .description keys). Ellipsis convention: only actions that always need
         further input before they can complete carry one. -->
    <actions resource-bundle="messages.FlowableAtlasBundle">
        <group id="Flowable.Menu" popup="true">
            <add-to-group group-id="ToolsMenu" anchor="last"/>

            <!-- Open -->
            <action id="Flowable.OpenAtlasHub"
                    class="com.flowable.atlas.action.OpenAtlasHubAction"/>
            <!-- Platform icons (AllIcons) on the actions the Atlas Hub toolbar reuses; no entry in
                 the main toolbar — the hub is the plugin's visible surface, everything else stays
                 in the menu. -->
            <action id="Flowable.OpenAtlasExplorer"
                    class="com.flowable.atlas.action.OpenAtlasExplorerAction"
                    icon="AllIcons.Nodes.PpWeb"/>
            <action id="Flowable.OpenExpressionPlayground"
                    class="com.flowable.atlas.action.OpenExpressionPlaygroundAction"
                    icon="/META-INF/expr-toolwindow.svg"/>
            <!-- Opens Search Everywhere on the "Flowable Model" tab — otherwise reachable only by
                 pressing Shift twice and tabbing across to it, which nobody discovers unaided. -->
            <action id="Flowable.SearchModels"
                    class="com.flowable.atlas.action.SearchFlowableModelsAction"
                    icon="AllIcons.Actions.Search"/>
            <separator/>

            <!-- Generate (submenu, with a nested Liquibase submenu) -->
            <group id="Flowable.Generate" popup="true">
                <action id="Flowable.GenerateAtlasExplorer"
                        class="com.flowable.atlas.action.GenerateAtlasExplorerAction"
                        icon="AllIcons.Actions.Compile"/>
                <action id="Flowable.GenerateModelConstants"
                        class="com.flowable.atlas.action.GenerateModelConstantsAction"/>
                <group id="Flowable.GenerateLiquibase" popup="true">
                    <action id="Flowable.GenerateLiquibaseFromDataObject"
                            class="com.flowable.atlas.action.GenerateLiquibaseFromDataObjectAction"/>
                    <action id="Flowable.GenerateLiquibaseFromApps"
                            class="com.flowable.atlas.action.GenerateLiquibaseFromAppsAction"/>
                </group>
                <group id="Flowable.GenerateDto" popup="true">
                    <action id="Flowable.GenerateDtoFromDataObject"
                            class="com.flowable.atlas.action.GenerateDtoFromDataObjectAction"/>
                    <action id="Flowable.GenerateDtoFromApps"
                            class="com.flowable.atlas.action.GenerateDtoFromAppsAction"/>
                </group>
            </group>
            <separator/>

            <!-- Flowable Design -->
            <action id="Flowable.SwitchDesignEnvironment"
                    class="com.flowable.atlas.action.SwitchDesignEnvironmentAction"/>
            <action id="Flowable.PullFromDesign"
                    class="com.flowable.atlas.action.PullFromDesignAction"
                    icon="AllIcons.Actions.Download"/>
            <action id="Flowable.SwitchWorkEnvironment"
                    class="com.flowable.atlas.action.SwitchWorkEnvironmentAction"/>
            <action id="Flowable.ManageEnvironments"
                    class="com.flowable.atlas.action.ManageEnvironmentsAction"/>
            <separator/>

            <!-- Maintenance -->
            <action id="Flowable.RebuildModelIndex"
                    class="com.flowable.atlas.action.RebuildModelIndexAction"/>
            <!-- Debug helper; visible only with idea.is.internal=true. -->
            <action id="Flowable.DumpKeyIndex" internal="true"
                    class="com.flowable.atlas.action.DumpFlowableIndexAction"/>
        </group>
    </actions>
</idea-plugin>
