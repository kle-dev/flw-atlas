<!-- require-restart: the plugin registers languages / file types / parser definitions (the two
     expression dialects) which cannot be loaded dynamically, so force IntelliJ to prompt for a
     restart on install / update / uninstall instead of silently half-loading. -->
<idea-plugin require-restart="true">
    <id>com.flowable.atlas</id>
    <name>Flowable Atlas</name>
    <!-- No <version> here on purpose: patchPluginXml injects the Gradle project version at build
         time (root build.gradle.kts → allprojects { version }). A literal here is dead text that
         drifts — this one had been reading 0.10.4 since three releases ago. -->
    <vendor email="kevin.klever@flowable.com" url="https://www.flowable.com">Flowable</vendor>

    <description><![CDATA[
        <p><b>The complete Flowable companion for IntelliJ IDEA.</b> Flowable Atlas turns the model keys
        and expressions scattered across your Java code and Flowable models into first-class, IDE-aware
        references — completed, validated and navigable — and maps your whole project into a single
        interactive explorer. Everything is resolved against the models that <i>actually live in your
        repository</i>, so a wrong key or a broken expression is caught in the editor, long before
        deployment.</p>

        <p>Zero configuration: open a project that contains the Flowable models you exported from Design
        (the app <code>.zip</code> / deployment <code>-bar</code>, holding
        <code>.bpmn / .cmmn / .dmn / .form / .data / .service / …</code>) or the Flowable Design
        <code>*-models</code> workspace, and start typing. The plugin indexes every model and puts that
        knowledge to work across three areas.</p>

        <h3>1 &middot; Model-key intelligence</h3>
        <ul>
          <li><b>Context-aware key completion</b> at every Flowable API position that takes a model key —
              process, case, decision, form, event, channel, data object, master data, service, action,
              agent, knowledge base, template, security policy, query, variable extractor, sequence, SLA,
              dashboard component, data dictionary, page and unified work definitions. Both
              <code>org.flowable.*</code> and <code>com.flowable.*</code>. Search by <b>key, by name, or by
              any fragment</b> — type <code>0061</code> to get <code>DEMO-DO-0061</code>.</li>
          <li><b>Cascade completion</b> for fluent chains — <code>operation("…")</code> completes the
              operations of the data object / service resolved from the sibling
              <code>definitionKey(…)</code> / <code>serviceKey(…)</code> (constant references are
              resolved), and <code>value("…", …)</code> completes that operation's input fields. DMN
              <code>variable("…", …)</code> offers the decision's input/output variables.</li>
          <li><b>More than keys</b> — completion for message &amp; signal names, process variables,
              task-definition keys and activity IDs, <b>scoped</b> to the
              <code>processDefinitionKey</code> / <code>caseDefinitionKey</code> in the same query chain
              when present, so you see only that model's ids.</li>
          <li><b>Model &rarr; model references in BPMN/CMMN XML</b> — the keys that live inside the models
              themselves (<code>callActivity calledElement</code>, <code>flowable:formKey</code>,
              <code>decisionRef</code>, CMMN <code>caseRef</code> / <code>processRef</code>) get the same
              completion, Ctrl/Cmd-click navigation and broken-key inspection as Java call sites — so a
              <code>calledElement</code> pointing at a missing process is caught before deployment.</li>
          <li><b>Broken-key inspection</b> — an unknown key (in Java or model XML) is flagged inline with a
              <i>"did you mean …?"</i> quick-fix to the closest real key.</li>
          <li><b>Navigation &amp; Find Usages</b> — Ctrl/Cmd-click a key literal to jump straight to the
              model file; hover for its type, name and location. Find Usages on a delegate class or an
              <code>${bean.method()}</code> expression lists the model files that reference it.</li>
          <li><b>Code generation</b> — generate a typed Java constants class
              (<code>FlowableModelKeys.Process.ORDER_FULFILMENT</code>, model name in Javadoc) that stays
              in sync as models are added, renamed or removed; or, with Alt/Option-Enter on a data-object
              <code>definitionKey("…")</code>, a typed Java bean built from the model's field mappings.</li>
          <li><b>Liquibase aware</b> — inside a changelog it completes <code>&lt;column&gt;</code> names
              and <code>tableName</code> from the backing service model, and flags columns that no
              backing Flowable model defines.</li>
        </ul>

        <h3>2 &middot; Flowable expression language support</h3>
        <ul>
          <li><b>Both dialects, first-class</b> — the backend JUEL dialect (<code>${…}</code> /
              <code>#{…}</code>) and the frontend form dialect (<code>{{…}}</code>) each get syntax
              highlighting, rainbow parentheses and brace matching.</li>
          <li><b>Live validation</b> — structural syntax errors, and unknown functions, namespaces or
              <code>flw.*</code> members, are flagged against a catalog of the real, verified Flowable
              function set.</li>
          <li><b>Expression completion</b> — functions, engine root objects, and your project's own
              process/case variables and form fields.</li>
          <li><b>Recognised inline, everywhere expressions live</b> — BPMN/CMMN/DMN XML,
              <code>.form</code> / <code>.page</code> JSON, and (optionally) Java string literals — so the
              highlighting, validation and completion follow you into the models you edit.</li>
          <li><b>Real Groovy &amp; JavaScript in script bodies</b> — BPMN/CMMN script tasks, listener
              scripts and action-bot scripts get the IDE's own language support injected inline:
              compiler-grade syntax errors, highlighting and fragment editing (Alt-Enter). Groovy is
              bundled in every IDEA; JavaScript lights up on Ultimate. JUEL or unknown script formats
              keep the plain expression injection. Atlas additionally runs its own dependency-free
              structural script check (unterminated strings/comments, unbalanced brackets,
              <code>scriptFormat</code> typos) in the CLI and the explorer's Checks tab.</li>
          <li><b>Flowable environments</b> — define your DEV/QA/UAT/PROD servers once for the whole IDE
              instead of once per repository, and pick them from a list. Each environment holds a Flowable
              Design connection, a Flowable Work connection, or just one of the two; the Design pull and the
              Expression Playground point independently, so running against QA while models still come from
              DEV1 is an ordinary way to work. Mark an environment <i>Protected</i> and Atlas asks before
              pulling from it or evaluating against it. Passwords and tokens stay in the IDE password
              safe.</li>
          <li><b>Expression Playground</b> (a tool window) — type an
              expression, pick backend or frontend, and get instant validation and completion; scope
              completion to one model; evaluate a frontend expression live against a pasted JSON payload;
              or evaluate a backend expression against a running app through the Flowable Inspect REST API.
              Which app it runs against is one of your <b>environments</b>, and the fastest way to switch is
              to paste a Work URL: the environment, the scope and the instance id all fill in from the
              link.</li>
          <li><b>Script Playground</b> (the <i>Scripts</i> tab of the same tool window) — paste or
              write a Groovy/JavaScript/Python script: the IDE's real language editing (completion,
              coloring) where the language plugin is available, live structural validation with
              squiggles and clickable problem rows (the same checks the CLI and the explorer's Checks
              tab run), the scope variables the script touches as chips, a picker that loads any
              script task / listener / action-bot script straight from the project's models, and a
              library of worked examples — a complete, commented script for every context and
              language, to start from rather than from an empty editor.</li>
        </ul>

        <h3>3 &middot; Atlas explorer &amp; hub</h3>
        <ul>
          <li><b>Atlas Hub</b> — a tool window (right stripe) that is the plugin's control center:
              which Flowable project Atlas is about, how many models it knows and how fresh that is, one
              attention line when something needs a hand, and three task blocks — the generated explorer,
              the Flowable Design pull, the playground's runtime — each with its actions beside its state,
              kept up to date live.</li>
          <li><b>Map your whole project</b> — Tools → Flowable Atlas → <i>Generate Atlas Explorer…</i>
              turns your models + Java + Liquibase into a single self-contained, offline, interactive
              HTML page. Save it into the project and open it in your browser or right inside the IDE
              (embedded viewer), then click through every relationship in both directions. In
              Settings → Generation you pick exactly which artifacts are written — summary &amp;
              overview Markdown, the graph JSON, the explorer HTML, a CLAUDE.md primer for AI agents.
              Generation runs entirely in-process — no external tools required — and honours the
              project's expression allowlist.</li>
          <li><b>IDE-native viewing</b> — the embedded explorer follows the IDE light/dark theme
              (switching restyles live) and carries a toolbar with <i>Regenerate Atlas Explorer</i>,
              <i>Reload</i>, <i>Open Expression Playground</i> and <i>Open in Browser</i>.</li>
          <li><b>Reopen without regenerating</b> — <i>Open Atlas Explorer</i> (Atlas Hub, or Tools →
              Flowable Atlas) opens an already-generated <code>*.explorer.html</code> — including
              one written by the standalone <code>atlas</code> CLI into the configured output folder — as a
              center editor tab (like a DB table), so you never have to leave the IDE for a browser.</li>
        </ul>

        <h3>How it feels</h3>
        <ul>
          <li><code>runtimeService.startProcessInstanceByKey("&lt;caret&gt;")</code> → your process keys.</li>
          <li><code>cmmnRuntimeService.createCaseInstanceBuilder().caseDefinitionKey("&lt;caret&gt;")</code> → your case keys.</li>
          <li><code>dataObjectRuntimeService.createDataObjectInstanceQuery().definitionKey(KEY).operation("&lt;caret&gt;")</code>
              → that data object's operations; <code>.value("&lt;caret&gt;", …)</code> → the operation's fields.</li>
          <li>Press completion twice to list <b>all</b> model keys of every type, regardless of the call site.</li>
        </ul>

        <p>Completion fires inside string literals and at bare arguments alike (inserting a quoted key),
        with Flowable keys ranked to the top — no setup beyond opening a project that contains your
        Flowable models.</p>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.27.0</h3>
        <ul>
          <li><b>Detail pages ask whether a model fits.</b> Every gap table is drawn one way, the schema
              coverage table first: a row per thing that should line up, tinted by how badly it does not, a
              pill per kind of gap and <i>only gaps</i> to hide the rows that are fine. A cell says ✓ it
              fits, ✗ it is missing, ⚠ it looks wrong, or ? Atlas cannot tell — and then why, in its
              tooltip.</li>
          <li><b>Relations take a row per relation.</b> Under the drawing, a relation lists its neighbours
              as chips on one row — <i>App contains</i> five models is one row, not five — and unfolds only
              where a neighbour has more to say: a caller's parameter mappings, every REST call with its
              verb. An operation's page is related to its service, and a variable's page lists the models
              that write, read or merely mention it, where both used to say they had no relationships.</li>
          <li><b>Operations Java calls through the service registry are used.</b> A service-registry
              invocation — <code>.serviceKey(…).operationKey(…)</code> — was invisible, so an operation only
              Java called was reported unused and nothing linked it to the code. The call is rarely one
              statement — a helper sets the service key and a lambda elsewhere names the operation, or the
              class hands its own <code>SERVICE_KEY</code> constant to a shared client — and each shape is
              followed now. On one real project twelve operations show the Java class that calls them and
              six <i>unused operation</i> findings are gone. In the IDE, <code>.serviceData("name",
              v)</code> is checked against the operation's input parameters, as a data object's
              <code>.value(…)</code> already was.</li>
          <li><b>A Spring property a model reads links to where it is set.</b>
              <code>environment.getProperty('crm.base-url')</code> and
              <code>propertyConfigurationService.getProperty("…")</code> in a model are edges to a property
              node whose page lists the file and line of every <code>application*.properties</code> and
              <code>application*.yml</code> that sets it, in Spring's relaxed spelling. Ctrl/⌘-click on the
              key in the expression opens those lines. A property set nowhere in the repository is not a
              finding: the value may come from the environment.</li>
          <li><b>User definitions and tenant setups join the graph.</b> A <code>.user.json</code> user
              definition links to the forms that create, show and edit such a user and to the groups it
              joins; a tenant setup to the groups it defines and the user definitions its users are of. A
              tenant setup contributes how many users of each kind it creates, never their logins or
              passwords. A form the platform ships is not reported missing.</li>
          <li><b>More of what Java hands the engine is seen.</b> A map passed to
              <code>startProcessInstanceByKey</code>, <code>complete</code>, <code>.variables(…)</code> or
              <code>setVariables</code> writes every name put into it. A model that renders a template
              against its whole variable container reads every variable it holds, so none of them is
              reported unread. <code>mainContentTemplate(…)</code> and <code>userDefinitionKey(…)</code>
              name a model like the other key-taking calls.</li>
          <li><b>A model's page names the tests that deploy it</b>, from <code>@Deployment(resources =
              …)</code> and its CMMN, DMN and app siblings; a master-data definition lists the files that
              load its rows.</li>
          <li><b>Explorer pages for endpoints and groups.</b> An endpoint lists who calls it, with the verb,
              the URL as the model spells it and the button that does; a group lists what its members may do
              per model. A form's REST call or a service operation whose URL lands on a project endpoint
              links to it. A table past a hundred rows, or a relation past sixty neighbours, shows the first
              ones and <i>show all</i>. On a touch screen a vertical swipe over a diagram scrolls the page
              and two fingers zoom.</li>
          <li><b>One vocabulary for findings.</b> A defect is labelled by its severity, <i>error</i> or
              <i>warning</i>; an advice finding is labelled <i>advice</i> and drawn grey — in the health
              rows, the finding pills, the diagram badges, the Checks filter (which gains an <i>advice</i>
              chip) and the Atlas Findings tool window, where advice wears the information icon. Every
              advice row used to read WARNING under the <i>Advice</i> heading. <code>graph.json</code>, the
              summary and <code>--fail-on</code> keep the severity; a saved
              <code>#/checks&amp;c=warning</code> link now keeps a defect's warnings only, and the sidebar's
              <i>Checks</i> badge is grey while only advice is open.</li>
          <li><b>A View menu in the explorer's top bar.</b> The two switches that sat there as bare glyphs —
              <code>≈</code> for uncertain links and a flag for the finding badges on diagrams — are
              labelled items of one <i>View</i> menu, each with a line saying what it shows, and the button
              carries a dot while something is hidden. It is keyboard-driven like a menu: <code>↓</code>
              opens it, <code>Space</code> flips a switch, <code>Escape</code> closes it.</li>
          <li><b>The explorer fits an editor tab.</b> A window of 1100px or less — the IDE editor tab the
              page is mostly read in — gets a compact, labelled sidebar instead of the rail of 27 unlabelled
              icons; the rail is still there by dragging the sidebar's edge below 140px, and a double-click
              on the edge goes back to the automatic layout instead of pinning 240px. A tab strip holding
              more than it can show fades the edge that hides tabs, scrolls with the wheel, and lists the
              hidden tabs behind a <b>+N</b> button. The browse list is narrower there and folds away from a
              button in its head — or by dragging its edge shut — to give the page the whole width; a long
              name no longer pushes its findings pill out of sight.</li>
          <li><b>A node's relations in one section.</b> A page told its relations three and four times — a
              neighbourhood drawing, <i>Uses / references</i>, <i>Used by / referenced from</i>, and a
              type's own copies (<i>Called by</i>, <i>Access</i>, <i>Subforms</i>, <i>Tools</i>, <i>Used
              by</i>, <i>Called with</i>). <i>Relations</i> is one section under the diagram: the drawing,
              then a table per direction with a row per relation and neighbour, carrying what the old
              sections added — the element that makes a reference, a REST call's verb and URL, a tool's
              operation, and a caller's parameter mappings in its expanded row — with chips for <i>uses</i>,
              <i>used by</i>, <i>uncertain</i> and <i>with mappings</i>. An expression, a binding, a
              function and a service operation get the same section from their own lists. The section
              listing the variables and expressions a model touches is called <i>Variables &amp;
              expressions</i> now; as <i>Uses</i> it collided with the drawing's USES column.</li>
          <li><b>A process's elements in one section.</b> User tasks, service tasks, script tasks, call
              activities, events, gateways, sequence flows, lanes, listeners and documentation — for a case
              its plan model, sentries and event listeners — were a section each, and a big process's
              navigator ran to seventeen chips in three rows. They are the groups of one <i>Elements</i>
              section now, each keeping its own table, with a chip per kind that keeps only that kind and
              one filter over all of them; the navigator fits one line.</li>
          <li><b>The overview summarises instead of repeating.</b> Its health block was the Checks page's
              list and its inventory the sidebar's entries as ungrouped chips. Health is now two numbers —
              the open defects and advice, each split by tier (broken, runtime risk, unfinished, noise) —
              the five checks with the most to say and one line for the rest, every part a way into the
              Checks page; the inventory lists the sidebar's entries grouped as the sidebar groups them. The
              two-column layout follows the overview's own width, so an editor tab stacks it where the room
              runs out.</li>
          <li><b>A category opens as a table.</b> Picking a category no longer shows its list beside an
              empty page: the page is a sortable table of the category — name, key, file, references in and
              out, open findings, and a column the type is worth more with (a model's app, a service's
              table, a Java class's package, a review list's finding). A header sorts, a second click
              reverses; the filter is the search engine's; marking, the keyboard and middle-click work as in
              the list, which steps aside and comes back beside the node a row opens. The sort travels in
              the link, now also by key, out-degree and findings.</li>
          <li><b>Every shortcut behind <code>?</code>.</b> <code>?</code> — or the new <b>?</b> button in
              the top bar — opens a sheet of every keyboard shortcut, grouped by where it works and
              generated from one table. It replaces the sentence in the empty detail pane, which only
              someone with nothing selected ever saw, and which said tabs switch with Alt+←→ (that is
              history; tabs are Alt+[ and Alt+]).</li>
          <li><b>The schema coverage table on every link of the chain.</b> A data object's page and a
              Liquibase changelog's page show the same <i>Schema coverage</i> table the service page has —
              every column from the changelog through the service mapping to the data object field — for
              each service whose coverage names them, so the table no longer has to be looked for one hop
              away. Its <code>⇄ crossed</code> marker wraps instead of being cut off in a narrow
              column.</li>
          <li><b>Smaller things in the explorer.</b> A list row's findings pill carries its tone's icon and
              says what it counts, the reference count beside it carries a link icon and says so too, and a
              key that only repeats the name is shown once. What accepting a finding does is said on the
              <i>accept…</i> button and in its form, instead of in a paragraph above every model's findings.
              Empty states draw their icon instead of a glyph the embedded font did not have, the Checks
              page's subtitle states its numbers and leaves the explanation to its two headings, and an
              accept button's focus ring is no longer clipped by its cell. A check's block opens with one
              line — what it means, then its buttons and <i>why · what to do</i> — where it had four, and no
              longer repeats its rows' severities as a row of chips.</li>
          <li><b>The findings in a tool window of their own.</b> <i>Atlas Findings</i> lists every defect —
              and, when asked, the advice and the accepted findings — from the same analysis the explorer is
              built from, grouped by check. A double-click opens the finding's file at its line, and
              <i>Accept…</i> writes the selected findings, with a reason, to the <code>waivers.json</code>
              the explorer's Save writes, so the page, the CLI gate and this list agree. It is Swing, so it
              works under Remote Development and in an IDE without JCEF, where the explorer was the only
              place to see or accept a finding.</li>
          <li><b>The model preview is interactive.</b> A click on a process, case or decision element puts
              the caret on its declaration beside it, Ctrl/⌘ + wheel zooms about the pointer, a drag pans,
              and Ctrl/⌘ + <code>=</code> <code>-</code> <code>0</code> zoom in, out and fit. The picture
              follows the editor's unsaved text, and a renderer that fails says so instead of claiming the
              model has no layout.</li>
          <li><b>Smaller things in the IDE.</b> The Hub's <i>archives could not be read</i> line shows which
              archive and why when one is picked; Recent Models can drop one entry or be cleared. Success
              balloons have their own notification group, <i>Flowable Atlas Results</i>, so they can be
              muted without muting warnings, and the restart-the-IDE notices are balloons instead of
              dialogs. The playground tool window is called <i>Expression Playground</i>, like the action
              that opens it. The model outline sorts by name, and an archive packed inside an archive says
              it is not expanded.</li>
        </ul>
        <h3>0.26.1</h3>
        <ul>
          <li><b>A condition written on its own line is read.</b> Design indents a sequence flow's
              condition, a script and a decision entry onto their own line inside a CDATA section, and only
              the first piece of text after the tag was read, which was the indentation. The flow lost its
              condition, the gateway was reported as an implicit parallel split instead of a missing
              default, and a script body came back empty.</li>
          <li><b>The model index follows whole folders, and only this project's.</b> A folder created,
              deleted or moved (a checkout adding <code>process-models/</code>, an unzipped export) arrives
              as one file event for the folder, and the index did not notice it until a Rebuild. A zip
              landing in <code>~/Downloads</code> or a model saved in another open project, on the other
              hand, dropped the index and rescanned every archive. A project that itself lives under a
              folder named <code>build</code>, <code>out</code>, <code>bin</code> or <code>target</code> (a
              CI agent's workspace, a dev container) had every file excluded and an empty index.</li>
          <li><b>Forms inside a Design export are models without a setting.</b> An app export keeps its
              forms, pages, actions and data objects only as <code>form-models/X.json</code> and the like.
              The command line always read them; the IDE did only with <i>Also index raw Flowable Design
              workspace sources</i> on, so with the default setting those forms had no key, no wireframe, no
              outline and no icon, and code naming them was flagged as broken.</li>
          <li><b>Generation does what it says.</b> It saves open edits first. <i>Cancel</i> stops it, and
              nothing is overwritten, where the run used to finish and write regardless. The written files
              are refreshed in one go off the UI thread, not one by one on it. <i>Regenerate</i> re-analyses
              the folder each page was made from, once per report, where it analysed whichever sub-project
              was active for every page it found, all at once.</li>
          <li><b>The "keys removed by this pull" warning fires.</b> The pull took its before-snapshot after
              the refresh that had already dropped the index, so the snapshot was always empty.</li>
          <li><b>No more blank explorer.</b> A control character in a model string (a vertical tab pasted
              from Word into a description) or a commented-out <code>&lt;script&gt;</code> tag in an HTML
              component broke the page's data island, and the page showed nothing. <code>graph.json</code>
              was invalid JSON for the same reason.</li>
          <li><b>Java behind a <code>"/api/**"</code> or <code>"http://…"</code> string is read.</b> The
              <code>/*</code> inside a mapping or a URL opened a comment that swallowed the endpoints, beans
              and key literals after it.</li>
          <li><b>The explorer opens Java where it is.</b> A class opens at its declaration's line, a method
              page opens the method (it had no file at all), and the dashboard's "+ N more" entry points
              expand in place.</li>
          <li><b>Fewer freezes.</b> A gutter click on a Java symbol or an endpoint, and the rename warning,
              held the read lock for the whole model scan, so typing stopped until it finished. Project
              custom functions were read inside the highlighting pass; they are now read in the background,
              honour the custom-function settings and are read again when a
              <code>.js</code>/<code>.ts</code> source changes, so a new function is known without a
              restart. A call argument in Java is resolved only when its method name is a Flowable API, Go
              to Symbol no longer repeats its searches for every matching name, the Hub no longer walks the
              project for explorer pages on every refresh, and opening a result reads the model off the UI
              thread.</li>
          <li><b>Large files and odd input no longer take the run down.</b> A multi-megabyte form, a
              database dump in the repository or an image inside a nested archive was read whole before its
              size was checked, and one of them could exhaust memory. A symlink back to a parent folder
              looped the walk. An entity past U+10FFFF dropped its model, and a broken <code>\u</code>
              escape or JSON nested thousands deep failed with an error nothing caught.</li>
          <li><b>Smaller fixes.</b> A qualified <code>flowable:class</code> that matched only a project
              class of the same simple name is marked uncertain. A waiver whose <code>until</code> date
              cannot be read is reported instead of never expiring. Two models whose keys clash (or differ
              only in case) each keep their diagram file. A decision rule's annotation is the rule's own. A
              form's <code>"visible": "true"</code> is the literal it says. A model in a folder whose name
              holds <code>!</code> is found. The recent-models list is safe to read during a tab switch, a
              failed Rebuild says so in a balloon instead of an IDE error, and a page's <code>{{…}}</code>
              bindings get the expression support a form's have.</li>
        </ul>
        <h3>0.26.0</h3>
        <ul>
          <li><b>A <code>.zip</code> or <code>.bar</code> in the Project view opens up.</b> A Design export
              in the repository could be searched but not browsed: the platform expands an archive only when
              it is a library, and for one inside a content root it mounts the archive and then drops every
              entry. A <code>.bar</code> was not even an archive to it, so a double-click asked which file
              type it was. Both now expand into their folders and entries, and a model opens straight from
              the tree, read-only.</li>
          <li><b>A model opens beside its picture.</b> A process, case, decision, form or page, loose or out
              of an archive, opens as text and picture side by side: the diagram, the decision table, or a
              form's wireframe. The picture is painted in Swing on the IDE host with the SVG library the
              platform draws its icons with, not in the IDE's SVG viewer, which is a browser panel — slow
              under Remote Development, and one that opens on the markup until its layout toggle is clicked.
              It fits the width, zooms from its toolbar and redraws when the file changes on disk. The
              diagram gutter icon opens this editor with the picture showing.</li>
          <li><b>Forms and pages have a picture at last.</b> A wireframe of Design's twelve-column grid at
              its real proportions: each component as a placeholder of its kind with its caption, a star
              when it is required, and its id underneath — the name a <code>{{…}}</code> or a script reaches
              it by. Panels, modals, tabs and accordions keep their own grids, a data table shows its column
              headers, a subform names the form it embeds. A <code>visible</code> or <code>enabled</code>
              that depends on an expression is spelled out beside the id; one that is plainly off greys the
              component out. It is a developer's map of the form, not a preview of the Work UI. The gutter
              icon on a form key — in Java, on a user task's <code>flowable:formKey</code>, on a subform
              reference — opens it like any other diagram. All 153 forms and pages of two real app corpora
              render.</li>
          <li><b>The Structure tool window outlines a model.</b> A form's or page's components as Design
              nests them, by caption with the id beside it; a process's or case's elements by name, without
              the connectors, the plan items and the diagram interchange. A click goes to the component or
              the element, in an archive entry as much as in a loose file. Any other JSON or XML keeps its
              usual outline.</li>
          <li><b>Any file opened out of an archive is safe from the formatting-layer crash.</b> Since the
              Project view expands archives, a <code>manifest.json</code> or a model under a name Atlas does
              not recognise opens just as easily, and is just as read-only and minified. The platform's
              visual formatting layer throws on such a file (<code>Wrong line: 1. Available lines count:
              1</code>); the guard that kept it off model files now keeps it off every file inside an
              archive.</li>
        </ul>
        <h3>0.25.1</h3>
        <ul>
          <li><b>A pull that cannot run no longer traps the IDE.</b> <i>Pull from Flowable Design</i>
              answers missing configuration by opening the environment editor and running again once that
              closes, and the check in front of the second attempt asked whether a workspace and some apps
              were selected — which a missing password does not change. So a connection with nothing signed
              in reopened the page every single time it was closed, with nothing to break the loop but
              closing the project. Two ordinary routes led straight into it: the action opens the editor
              itself when no environment exists yet and pulls the moment one does, and a connection created
              in that dialog has no secret stored yet; and <i>Sign out &amp; retry</i> on a failed pull
              clears the secret before doing exactly the same thing. The editor is now offered once per
              pull, and the attempt that follows it reports instead of reopening anything — <code>Not signed
              in to &lt;server&gt;</code>, <code>No Flowable Design environment is selected</code>,
              <code>This project has no folder on disk to pull into</code>, each on the usual balloon with
              its <i>Configure…</i> action for a second try that the reader asks for.</li>
        </ul>
        <h3>0.25.0</h3>
        <ul>
          <li><b>A model in the project, compared against the model in the app.</b> Models are increasingly
              generated rather than modelled — a form written by an LLM, say — and the file lands in the
              project folder, not in the app export. The question that follows is "what does this change
              against the app we have?", and that is a diff the IDE can draw, except that one of its two
              sides lives inside <code>&lt;App&gt;.zip</code> and nothing offered that pairing: the route
              was to unzip by hand into a temp folder. <i>Compare Model with Archive</i> is that pairing, in
              the platform's own diff viewer, from either end — a file in the project is matched against the
              model entries of every <code>.bar</code>/<code>.zip</code> in scope, an entry inside an
              archive against the project's own model files. The counterpart is found by file name, by the
              name behind the <code>&lt;kind&gt;-</code> prefix a deployment archive gives its entries, and
              failing both by the model key inside the file, because a generator rarely names a file the way
              Design does. One match opens straight away; several, or none, offer the archive's model
              entries in a list that filters as you type. It is in the Project view's context menu — which
              now also comes up on a <code>.json</code>, the one file the comparison is most wanted on and
              the one it was hidden from — and in the editor's, which is how an entry opened by <i>Go to
              Model</i> is reached.</li>
          <li><b>Both sides are laid out before they are compared.</b> A Design export is minified: one long
              line per model, and the same goes for every <code>.form</code> in a deployment archive. Held
              against a file that some generator or formatter wrote out over hundreds of lines,
              <i>everything</i> differs and the viewer has nothing to say — so without this the comparison
              would have been an answer nobody can read. Both sides are re-indented first, whitespace only
              and never a value, so a number still reads the way its own file spells it; the pair is then
              read-only, because neither side is the file on disk any more. <i>Show Raw Files</i> in the
              diff toolbar gives the two files themselves, where the project side stays editable. Processes,
              cases and decisions are exported formatted already and are always shown as they are.</li>
        </ul>
        <h3>0.24.3</h3>
        <ul>
          <li><b><code>CLAUDE.md</code> says less, and none of it is wrong.</b> Gloaguen et al. (ETH Zürich,
              <i>Evaluating AGENTS.md</i>, 2026) measured what a repository-level context file does for a
              coding agent: its instructions are followed, its repository overview adds nothing the agent
              would not read anyway, and the file costs about a fifth more per task. Held against that, a
              third of the generated file was overview — the scale line, the app list, the model inventory,
              the Java-by-role tally, the directory counts and the per-check health tallies, every one a
              copy of the summary that §0 has the agent read whole — and §5 restated four of its own six
              rules in a "pitfalls" paragraph. All of that is gone. What stays is what the agent cannot get
              from the repository: the procedure, the Design-vs-repo convention, the wiring examples to
              mirror, the known issues not to copy, and the catalogs. §1 lost its ASCII diagram and the
              <code>ACT_*</code> table paragraph on the same grounds.</li>
          <li><b>The catalog is complete.</b> §6 cut every list at a fixed length — <code>flw.</code> showed
              34 of its 59 members, <code>bpmn:</code> and <code>cmmn:</code> ended in "(+4 more)" — under a
              sentence that calls anything outside the list a hallucination. That sentence was declaring
              real functions hallucinations. The lists are whole now, and the section opens by saying when
              it is needed at all.</li>
          <li><b>The file works from where it is read.</b> It is meant to be the project's
              <code>CLAUDE.md</code>, read from the repository root, but it named its siblings
              (<code>&lt;project&gt;.summary.md</code>, <code>.graph.json</code>, …) as bare file names
              relative to wherever Atlas had written them — which, with the launcher's default, is a folder
              next to the Atlas checkout. From the root not one of them resolved. The renderer now knows the
              output directory: inside the project the paths are spelled from the root
              (<code>atlas-output/&lt;project&gt;/&lt;project&gt;.summary.md</code>) and the header offers
              the <code>@</code>-import line for a hand-kept <code>CLAUDE.md</code>; outside it, one line
              says where the files are and how to regenerate them. <code>--claude</code> on its own, which
              writes nothing but this file, no longer sends the agent to read a summary that does not exist
              — its first step is to generate the artifacts. And the <code>--slice</code> recipe is written
              in the form that runs: through the launcher it never did, because the launcher adds
              <code>--all</code>, which <code>--slice</code> refuses.</li>
          <li><b>What not to touch is stated, not hedged.</b> §3 said "unless this project's convention is
              to edit the model files directly (check existing commits)". Atlas knows whether the models
              came out of Design export archives or sit in the repository as loose files, so §4 now says
              which — "Design exports packed in <code>apps/demo.zip</code> — never edit the exports", or
              "unpacked files under <code>processes/</code> — check <code>git log</code> before you decide"
              — next to where the custom Java goes. The "Flowable version: not auto-detected" line, a
              sentence about the absence of a fact, is dropped when nothing is found; so is the generic
              <i>Run &amp; verify</i> fallback that fitted every Flowable project and told this one nothing.
              The house-rules reminder is a real HTML comment now, which Claude Code strips before loading,
              instead of the same text in backticks that every session paid for.</li>
        </ul>
        <h3>0.24.2</h3>
        <ul>
          <li><b>Go to Model does something under Remote Development.</b> It still did nothing there after
              0.24.1, and the reason was one layer below the one that release fixed: on a remote IDE the
              <b>Flowable Model</b> tab is not in Search Everywhere at all. The popup renders in the thin
              client, a tab contributed by a plugin running on the host does not reach it, so there was
              nothing for the action to select. Routing the action to the client — what 0.24.1 did — is
              worse rather than better: the plugin is not loaded there, so the action has no implementation
              to run. On a remote host <i>Go to Model…</i> now asks for a pattern and opens the result list,
              which is the same search reaching the same index; a dialog and a tool window are ordinary UI
              that Remote Development mirrors. Locally nothing changes. Putting the tab itself in the remote
              popup means republishing the contributor through the platform's newer provider API, which is
              its own piece of work.</li>
        </ul>
        <h3>0.24.1</h3>
        <ul>
          <li><b>The model search works under Remote Development.</b> <i>Search Models…</i> did nothing at
              all on a remote host: the Search Everywhere popup is a frontend component, an ordinary action
              runs on the backend, and the call reached no UI — it failed silently, which is the worst way
              for a button to fail. It is built on the platform's own
              <code>SearchEverywhereBaseAction</code> now, the same base every <i>Go to Class / File /
              Symbol</i> uses, whose whole purpose is to route the action to the thin client.</li>
          <li><b>One menu, in two places.</b> <i>Tools → Flowable Atlas</i> and the Atlas Hub's <b>⋮</b>
              were two hand-kept lists over the same actions, and they had drifted into different contents
              <i>and</i> a different order — so the same plugin had two navigations to learn, and an entry
              was reliably in the one you were not looking at. The ⋮ now renders the Tools group itself: an
              action added to the descriptor appears in both, in the same place, or in neither. <i>Open
              Environment in Browser</i>, which only the Hub had, is a registered group now and is in
              both.</li>
          <li><b>The two searches say which is which.</b> <i>Search Models…</i> and <i>Find in Models…</i>
              differed by one word, and nothing in the pair said that the first takes you to one place and
              closes while the second leaves a list. They are <b>Go to Model…</b> and <b>Find in Models…</b>
              now, after the platform's own <i>Go to File</i> / <i>Find in Files</i> — and they no longer
              share a magnifier: the list carries the Find tool window's own icon, which is the window it
              opens. The keyboard shortcut is unchanged.</li>
        </ul>
        <h3>0.24.0</h3>
        <ul>
          <li><b>Findings can be accepted, in a file you commit.</b> Some findings are correct and still not
              worth acting on, and until now the only answer was to drop the check for everyone or stop
              running <code>--fail-on</code> at all. A project can now carry <code>waivers.json</code> next
              to its artifacts, beside a generated <code>.gitignore</code> that ignores everything in the
              folder <i>except</i> that file — the analysis is regenerated and may hold client data, the
              decisions are yours and belong in review. A waiver names check + node, narrowed by element and
              subject, so it survives the message rewording; an accepted finding stays in the report, in a
              section of its own, and leaves the counts and the gate. A rule that matched nothing, expired,
              or gives no reason is reported on every surface, and matching is counted per run, so a second
              rule covering the same finding is not "matched nothing". The explorer and
              <code>Waivers.serialize</code> write the same bytes — <code>WaiverWriterParityTest</code> runs
              one set through both — and a save from the IDE merges into the file rather than over it: a
              rule a colleague committed, or the CLI added, after the page was generated survives. New
              flags: <code>--waivers</code> (the folder the artifact lands in), <code>--no-waivers</code>,
              <code>--fail-on-stale-waivers</code>, <code>--waiver-author</code> to prefill the
              <code>by</code> of a rule accepted from a CLI-generated page, and <code>any</code> as the
              honest spelling of what <code>--fail-on warning</code> has always meant.</li>
          <li><b>Accept a finding where you read it.</b> Every finding row — on the Checks page, under
              <i>Findings on this model</i> (right under the diagram), on a diagram element's card — carries
              <b>accept…</b>: a labelled reason that is refused in words when empty, an optional
              <i>until</i>, <i>by</i> prefilled with the project's git identity in the IDE, and, for a
              finding that names an element or a subject, a choice between this finding only (the default)
              and every finding of the check on the model. Accepted rows stay in place with <i>edit</i> and
              <i>restore</i>; a bar under the top bar says on every view how many decisions are unsaved,
              with <i>Save to waivers.json</i> in the IDE or <i>Export</i> elsewhere and a two-step
              <i>discard</i>. After a save the IDE confirms, regenerates the page — on the block or node you
              were reading, not back on the dashboard — and the diff reconciles itself against the file. The
              IDE reads <code>waivers.json</code> on both generate paths, so a page generated from the IDE
              no longer shows every accepted finding as open. The <i>Deliberately accepted</i> table shows
              every decision as it stands — scope, reason, author, expiry, how many findings it covers —
              with its troubles as marks on the row and <i>restore</i> beside it; notes get a table of their
              own.</li>
          <li><b>A model with findings looks like one.</b> A count pill on tree rows and list items,
              coloured by the worst open finding; a marker on every diagram element with a finding, whose
              card lists them and hands <i>accept…</i> to the finding's row. A review list that mirrors a
              check carries the check's open count — so accepting the three unused forms empties the list
              instead of leaving <i>Unused forms 3</i> in the sidebar — and a model whose every finding was
              accepted wears a small ✓ in lists and in the tree.</li>
          <li><b>Findings explain themselves.</b> Every check is described once, in
              <code>CheckCatalog</code>: what it is, its severity, why a finding matters, what to do about
              it — accepting included — and where the docs are. The summary, the overview, the agent primer,
              the CLI's <code>--fail-on</code> vocabulary and the explorer all read it, where until now a
              check was a string id in one place, a label in a second, a second label in a third and a
              severity stated only on the documentation page. A renamed docs heading is now a red build
              rather than a dead link.</li>
          <li><b>Every check is a defect or an advice, and every surface leads with the split.</b> A list
              that put "task without a boundary event" beside "expression does not parse" taught a reader to
              skim both. Each of the 23 checks now has a <i>kind</i>: a <b>defect</b> is wrong now — a file
              that does not parse, a key nothing answers, a gateway the engine cannot leave, a column two
              names disagree about — and an <b>advice</b> is a pattern worth a look while nothing is broken
              — a call with no error path, a form nothing references. The Checks page, the summary, the
              overview, the generated <code>CLAUDE.md</code> and the CLI status line say <code>3 defects ·
              41 advice</code> instead of <code>44 findings</code>; <code>graph.json</code> carries
              <code>stats.defects</code> and <code>stats.advice</code>; the catalog reads defects first,
              then advice; and <code>--fail-on defects</code> makes a pipeline red on what is wrong and
              green on what could be better.</li>
        </ul>
        <p>Older releases: see <code>CHANGELOG.md</code> in the repository.</p>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>
    <!-- XML PSI for the Liquibase-changelog coverage inspection. -->
    <depends>com.intellij.modules.xml</depends>
    <!-- JSON PSI for form-model {{…}} expression injection and the .form → JSON association. -->
    <depends>com.intellij.modules.json</depends>
    <!-- JCEF (Atlas Hub, the explorer editor, the Inspect sign-in browser). Up to 2026.1 JCEF sat in the
         platform core; 2026.2 (build 262, the floor since the 2026.2 move) has it in the bundled
         "Web Browser (JCEF)" plugin (id com.intellij.modules.jcef), which is also why :idea-plugin has to
         put that plugin on its compile classpath. Still declared *optional*: someone who disables the
         bundled plugin, or an IDE without JCEF at all (remote dev), should lose the browser panels rather
         than the whole plugin — every call site is guarded by JBCefApp.isSupported() for exactly that. -->
    <depends optional="true" config-file="flowable-atlas-jcef.xml">com.intellij.modules.jcef</depends>

    <resource-bundle>messages.FlowableAtlasBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Services (FlowableModelIndexService, ModelConstantsService/Settings) are @Service — no registration needed. -->
        <!-- Settings tree: an app-level root page (core toggles) with project-level children.
             parentId chaining across levels is supported — the settings tree merges them by ID. -->
        <applicationConfigurable
                parentId="tools"
                id="com.flowable.atlas.settings"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas"
                instance="com.flowable.atlas.settings.FlowableAtlasConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.expressions"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.expressions"
                instance="com.flowable.atlas.settings.ExpressionsConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.generation"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation"
                instance="com.flowable.atlas.settings.GenerationConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings.generation"
                id="com.flowable.atlas.settings.generation.constants"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation.constants"
                instance="com.flowable.atlas.settings.GenerationConstantsConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings.generation"
                id="com.flowable.atlas.settings.generation.liquibase"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation.liquibase"
                instance="com.flowable.atlas.settings.GenerationLiquibaseConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings.generation"
                id="com.flowable.atlas.settings.generation.dto"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.generation.dto"
                instance="com.flowable.atlas.settings.GenerationDtoConfigurable"/>
        <projectConfigurable
                parentId="com.flowable.atlas.settings"
                id="com.flowable.atlas.settings.environments"
                bundle="messages.FlowableAtlasBundle"
                key="configurable.atlas.environments"
                instance="com.flowable.atlas.settings.EnvironmentsConfigurable"/>
        <completion.contributor
                language="JAVA"
                implementationClass="com.flowable.atlas.completion.FlowableJavaCompletionContributor"/>
        <!-- Liquibase changelog: complete <column name="…"> / tableName from the backing service model. -->
        <completion.contributor
                language="XML"
                implementationClass="com.flowable.atlas.liquibase.LiquibaseColumnCompletionContributor"/>
        <!-- BPMN/CMMN model XML: complete model keys at cross-reference attributes
             (calledElement, flowable:formKey, decisionRef, caseRef, processRef, …). -->
        <completion.contributor
                language="XML"
                implementationClass="com.flowable.atlas.completion.FlowableXmlKeyCompletionContributor"/>
        <localInspection
                language="JAVA"
                shortName="FlowableValueField"
                displayName="Invalid Flowable data-object value field"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableValueFieldInspection"/>
        <localInspection
                language="JAVA"
                shortName="FlowableBrokenKey"
                displayName="Unknown Flowable model key"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableBrokenKeyInspection"/>
        <!-- BPMN/CMMN model XML: an unknown model key at a cross-reference attribute (broken deployment). -->
        <localInspection
                language="XML"
                shortName="FlowableXmlBrokenKey"
                displayName="Unknown Flowable model key (model XML)"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.inspection.FlowableXmlBrokenKeyInspection"/>
        <!-- Ctrl-click a key literal → jump to the model file; Find Usages works too. -->
        <psi.referenceContributor
                language="JAVA"
                implementation="com.flowable.atlas.navigation.FlowableKeyReferenceContributor"/>
        <!-- Ctrl-click a model key in BPMN/CMMN XML (calledElement, formKey, decisionRef, …) → the model file. -->
        <psi.referenceContributor
                language="XML"
                implementation="com.flowable.atlas.navigation.FlowableXmlKeyReferenceContributor"/>
        <!-- Ctrl-click a model key in a JSON model (a data object's backing service, a form component's subform,
             data object, service or action, a document's forms, an app's models, …) → the model file. The sites are
             :core's JsonKeySites, shared with the parsers. -->
        <psi.referenceContributor
                language="JSON"
                implementation="com.flowable.atlas.navigation.FlowableJsonKeyReferenceContributor"/>
        <!-- Hover / Ctrl-Q on a key literal → model type, name and file. -->
        <lang.documentationProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.navigation.FlowableKeyDocumentationProvider"/>
        <!-- …and on a key inside a model file: a cross-reference or the file's own key. -->
        <lang.documentationProvider
                language="XML"
                implementationClass="com.flowable.atlas.navigation.FlowableKeyDocumentationProvider"/>
        <lang.documentationProvider
                language="JSON"
                implementationClass="com.flowable.atlas.navigation.FlowableKeyDocumentationProvider"/>
        <!-- Search Everywhere / Go to Symbol: every model key (→ model file) and every bot key
             (→ the BotService class and the actions that invoke it). -->
        <gotoSymbolContributor
                implementation="com.flowable.atlas.navigation.FlowableKeyGotoSymbolContributor"/>
        <!-- A "Flowable Model" tab of its own in Search Everywhere: model keys and the file paths
             inside a .bar/.zip (invisible to the Files tab), plus a live full-text search over model
             content (invisible to Find in Files). The contributor above stays — it feeds the
             platform's Symbols tab, this feeds its own. -->
        <searchEverywhereContributor
                implementation="com.flowable.atlas.navigation.se.FlowableModelSeContributor$Factory"/>
        <!-- Liquibase changelog column not mapped in the backing Flowable service/data-object model. -->
        <localInspection
                language="XML"
                shortName="FlowableLiquibaseCoverage"
                displayName="Liquibase column not defined in Flowable model"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.liquibase.LiquibaseCoverageInspection"/>
        <!-- Alt-Enter on a data-object definitionKey → generate a typed Java bean from its fields. -->
        <intentionAction>
            <language>JAVA</language>
            <className>com.flowable.atlas.intention.GenerateDataObjectBeanIntention</className>
            <category>Flowable</category>
        </intentionAction>
        <!-- Alt-Enter on a model key — in Java, or inside a model file — → the model's page in the Atlas
             Explorer (the reverse of the page's "open in IDE" button). No language filter: the intention
             itself answers only in Java and model files. -->
        <intentionAction>
            <className>com.flowable.atlas.intention.OpenInAtlasExplorerIntention</className>
            <category>Flowable</category>
        </intentionAction>
        <!-- Find Usages on a model's key, in its own file → every model and every Java call site that names it. -->
        <findUsagesHandlerFactory implementation="com.flowable.atlas.usage.ModelKeyFindUsagesHandlerFactory"/>
        <!-- Don't report Java referenced from models (delegates, ${bean.method()}, listeners) as unused. -->
        <implicitUsageProvider implementation="com.flowable.atlas.usage.FlowableImplicitUsageProvider"/>
        <!-- Find Usages / Ctrl-B on a delegate class or ${bean.method()} shows the model files that use it. -->
        <customUsageSearcher implementation="com.flowable.atlas.usage.FlowableModelUsageSearcher"/>
        <!-- Gutter icon on Java methods/beans referenced from Flowable models → navigate to the model usages. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableModelReferenceLineMarkerProvider"/>
        <!-- Gutter icon on a BotService class → navigate to the .action models that invoke the bot. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableBotActionLineMarkerProvider"/>
        <!-- Gutter icon on a @GetMapping/@PostMapping/… handler → navigate to the models calling its endpoint. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableEndpointUsageLineMarkerProvider"/>
        <!-- Gutter icon on a model-key expression — a literal or a constant/local reference at a key
             call site — whose model has a diagram (bundled Design .svg, rendered from its DI layout, or a
             form/page drawn as its grid) → open it in the IDE's built-in image/SVG viewer. No diagram
             (e.g. an action) → no marker. -->
        <codeInsight.lineMarkerProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.usage.FlowableDiagramLineMarkerProvider"/>
        <!-- …and inside a model file: on the file's own key and on every process/case/decision/form reference. -->
        <codeInsight.lineMarkerProvider
                language="XML"
                implementationClass="com.flowable.atlas.usage.FlowableDiagramLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider
                language="JSON"
                implementationClass="com.flowable.atlas.usage.FlowableDiagramLineMarkerProvider"/>
        <!-- Inline hint: the physical table name after a string literal that is a data-object key. -->
        <codeInsight.declarativeInlayProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.hint.FlowableDataObjectTableInlayProvider"
                isEnabledByDefault="true"
                group="VALUES_GROUP"
                providerId="com.flowable.atlas.dataObjectTable"
                bundle="messages.FlowableAtlasBundle"
                nameKey="inlay.dataObjectTable.name"
                descriptionKey="inlay.dataObjectTable.description"/>
        <!-- Inline hint: the action model's name after a string literal that is an .action key. -->
        <codeInsight.declarativeInlayProvider
                language="JAVA"
                implementationClass="com.flowable.atlas.hint.FlowableActionNameInlayProvider"
                isEnabledByDefault="true"
                group="VALUES_GROUP"
                providerId="com.flowable.atlas.actionName"
                bundle="messages.FlowableAtlasBundle"
                nameKey="inlay.actionName.name"
                descriptionKey="inlay.actionName.description"/>
        <!-- Inline hint inside a model file: the referenced model's name after the key that names it. -->
        <codeInsight.declarativeInlayProvider
                language="XML"
                implementationClass="com.flowable.atlas.hint.FlowableModelNameInlayProvider"
                isEnabledByDefault="true"
                group="VALUES_GROUP"
                providerId="com.flowable.atlas.modelName.xml"
                bundle="messages.FlowableAtlasBundle"
                nameKey="inlay.modelName.name"
                descriptionKey="inlay.modelName.description"/>
        <codeInsight.declarativeInlayProvider
                language="JSON"
                implementationClass="com.flowable.atlas.hint.FlowableModelNameInlayProvider"
                isEnabledByDefault="true"
                group="VALUES_GROUP"
                providerId="com.flowable.atlas.modelName.json"
                bundle="messages.FlowableAtlasBundle"
                nameKey="inlay.modelName.name"
                descriptionKey="inlay.modelName.description"/>
        <!-- Warn when renaming a Java symbol referenced by Flowable model expressions (text, not auto-updated). -->
        <refactoring.elementListenerProvider
                implementation="com.flowable.atlas.usage.FlowableRenameWarningProvider"/>
        <!-- Balloon shown after generating the Atlas explorer (open in browser / open in IDE). -->
        <notificationGroup id="Flowable Atlas" displayType="BALLOON"/>
        <!-- "It worked": the index was rebuilt, artifacts were generated, waivers were saved — its own
             group, so it can be silenced in Settings → Notifications without silencing the warnings. -->
        <notificationGroup id="Flowable Atlas Results" displayType="BALLOON"/>
        <!-- The embedded "Atlas Explorer" editor tab is registered in flowable-atlas-jcef.xml: it needs
             the bundled JCEF plugin, and a provider named here would be asked on every file open
             whether or not that plugin is loaded. -->
        <!-- "Models changed since this explorer was generated" banner above the page. -->
        <editorNotificationProvider implementation="com.flowable.atlas.explorer.AtlasExplorerStaleNotificationProvider"/>

        <!-- ==================== Flowable Expression support ==================== -->
        <!-- Two dialects of one expression language: backend ${…}/#{…} (JUEL) and frontend {{…}} (flw.*). -->
        <fileType name="Flowable Backend Expression"
                  language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableBackendExprFileType"
                  fieldName="INSTANCE"
                  extensions="flowable-be"/>
        <fileType name="Flowable Frontend Expression"
                  language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableFrontendExprFileType"
                  fieldName="INSTANCE"
                  extensions="flowable-fe"/>
        <lang.parserDefinition language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableBackendExprParserDefinition"/>
        <lang.parserDefinition language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableFrontendExprParserDefinition"/>
        <!-- Token coloring (parentheses, brackets, strings, operators, …) for both dialects. -->
        <lang.syntaxHighlighterFactory language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprSyntaxHighlighterFactory"/>
        <lang.syntaxHighlighterFactory language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprSyntaxHighlighterFactory"/>
        <!-- Match () and [] — caret on a brace highlights its partner, unmatched braces are flagged. -->
        <lang.braceMatcher language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprBraceMatcher"/>
        <lang.braceMatcher language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.lang.FlowableExprBraceMatcher"/>
        <!-- Structural syntax squiggles for both dialects (playground + injected fragments). -->
        <annotator language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.annotator.FlowableExpressionAnnotator"/>
        <annotator language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.annotator.FlowableExpressionAnnotator"/>
        <!-- Semantic expression findings as real inspections: severity/enablement per profile, project
             allowlist via quick fix. No language attribute — the two dialects share no registered base
             language; the implementations bail out immediately on any non-expression file. -->
        <localInspection
                shortName="FlowableExprUnknownFunction"
                displayName="Unknown Flowable expression function or namespace"
                groupName="Flowable"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.flowable.atlas.expr.inspection.FlowableExprUnknownFunctionInspection"/>
        <localInspection
                shortName="FlowableExprGrounding"
                displayName="Expression root is not a known variable, bean, or root object"
                groupName="Flowable"
                enabledByDefault="false"
                level="WEAK WARNING"
                implementationClass="com.flowable.atlas.expr.inspection.FlowableExprGroundingInspection"/>
        <!-- Rainbow parentheses are done in the syntax highlighter (FlowableExprSyntaxHighlighter):
             the highlighting lexer tags each round paren with its nesting level and each level gets
             a distinct colour. This paints in every surface, including the playground's embedded
             editor field where an annotator would not. Per-theme default colours + the Color Scheme
             page that makes them customizable: -->
        <additionalTextAttributes scheme="Default" file="colorSchemes/FlowableExprDefault.xml"/>
        <additionalTextAttributes scheme="Darcula" file="colorSchemes/FlowableExprDarcula.xml"/>
        <colorSettingsPage implementation="com.flowable.atlas.expr.lang.FlowableExprColorSettingsPage"/>
        <!-- Alt-Enter on an injected ${…}/{{…}} fragment → rework it in the playground, pre-filled.
             Registered once (no <language> filter): the same class must not be registered twice or the
             platform rejects it as a duplicate. isAvailable() self-gates on FlowableExprFile, which
             covers both the backend and frontend dialects. -->
        <intentionAction>
            <className>com.flowable.atlas.expr.intention.OpenInExpressionPlaygroundIntention</className>
            <category>Flowable</category>
        </intentionAction>
        <!-- Ctrl-Q / hover on a catalog function or root inside an expression → call head + doc line. -->
        <!-- Ctrl-click on a backend expression's root (`${orderService.…}`) → the Spring bean's class, by its
             default bean name; Ctrl-Q then shows that class's documentation. -->
        <psi.referenceContributor language="FlowableBackendExpr"
                  implementation="com.flowable.atlas.expr.navigation.FlowableExprBeanReferenceContributor"/>
        <!-- environment.getProperty('k') → the line of every application*.properties|yml that sets k. -->
        <psi.referenceContributor language="FlowableBackendExpr"
                  implementation="com.flowable.atlas.expr.navigation.FlowableExprPropertyReferenceContributor"/>
        <lang.documentationProvider language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.documentation.FlowableExprDocumentationProvider"/>
        <lang.documentationProvider language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.documentation.FlowableExprDocumentationProvider"/>
        <!-- Completion of functions, root objects and the project's variables/form-fields. -->
        <completion.contributor language="FlowableBackendExpr"
                  implementationClass="com.flowable.atlas.expr.completion.FlowableExpressionCompletionContributor"/>
        <completion.contributor language="FlowableFrontendExpr"
                  implementationClass="com.flowable.atlas.expr.completion.FlowableExpressionCompletionContributor"/>
        <!-- Inject the expression languages inline wherever expressions live in the repo. -->
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableXmlExpressionInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableJsonExpressionInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.expr.inject.FlowableJavaExpressionInjector"/>
        <!-- Inject the IDE's real Groovy / JavaScript languages into Flowable script bodies (BPMN
             scriptTask, CMMN script tasks, execution/task listener scripts, action-bot scriptInfo):
             compiler-grade syntax errors right in the model file. Languages are resolved by ID at
             runtime (Language.findLanguageByID), so there is NO hard or optional plugin dependency —
             no Groovy/JavaScript plugin simply means no injection, never an error. -->
        <multiHostInjector implementation="com.flowable.atlas.script.inject.FlowableXmlScriptInjector"/>
        <multiHostInjector implementation="com.flowable.atlas.script.inject.FlowableJsonScriptInjector"/>
        <!-- Completion for the Flowable script bindings (execution.…, task.…, flw.time.…): the real
             language completion knows nothing about these dynamic bindings, so the bindings catalog
             offers them — in the Script Playground and inside injected script bodies. language="any"
             because the host language varies (Groovy/JS/plain-text fallback) and this plugin has no
             dependency on the language plugins; the contributor gates itself cheaply. -->
        <completion.contributor language="any"
                  implementationClass="com.flowable.atlas.script.completion.ScriptBindingsCompletionContributor"/>
        <!-- Silence the Groovy/JS "unresolved" noise on the engine bindings (execution, flw, …)
             inside Flowable script bodies — they are dynamic bindings the language PSI can never
             resolve. Scoped to the playground + injected script fragments; real syntax errors and
             the bindings-catalog validation are unaffected. Registered per language (the suppressor
             lookup does not honor "any"); an entry for an absent language plugin is simply dormant,
             so this keeps the no-Groovy/JS-dependency rule intact. -->
        <lang.inspectionSuppressor language="Groovy"
                  implementationClass="com.flowable.atlas.script.inspection.FlowableScriptInspectionSuppressor"/>
        <lang.inspectionSuppressor language="JavaScript"
                  implementationClass="com.flowable.atlas.script.inspection.FlowableScriptInspectionSuppressor"/>
        <!-- Hover / Ctrl-Q on a binding shows catalog documentation instead of the platform's
             "No candidates found for method call" fallback for unresolvable dynamic bindings. -->
        <lang.documentationProvider language="Groovy"
                  implementationClass="com.flowable.atlas.script.documentation.ScriptBindingsDocumentationProvider"/>
        <lang.documentationProvider language="JavaScript"
                  implementationClass="com.flowable.atlas.script.documentation.ScriptBindingsDocumentationProvider"/>
        <lang.documentationProvider language="TEXT"
                  implementationClass="com.flowable.atlas.script.documentation.ScriptBindingsDocumentationProvider"/>
        <!-- The Script Playground's scratch file resolves imports against the whole project —
             without a module, `import org.slf4j.LoggerFactory` would never resolve there. -->
        <resolveScopeProvider implementation="com.flowable.atlas.script.toolwindow.ScriptPlaygroundResolveScopeProvider"/>
        <!-- Treat Flowable form models (.form) as JSON so their {{…}} strings become injection hosts,
             and action models (.action) so their scriptInfo.script strings can host Groovy/JS. The
             remaining JSON-serialized model extensions are here so a model OPENS with its content:
             an unregistered extension resolves to UnknownFileType, which is binary, so the editor
             shows the "file type not associated" panel instead — most visibly for an entry opened
             out of a .bar/.zip. A <fileType> with no implementationClass only adds matchers to the
             existing type (the same idiom the bundled javaFX and Maven plugins use). The list mirrors
             ModelType.EXTENSIONS in :core. -->
        <fileType name="JSON"
                  extensions="form;action;page;data;dictionary;query;sequence;sla;event;channel;agent;service;tpl;policy;extractor;knowledgebase;masterdata;dashboardcomponent;document;palette;app"/>
        <!-- The XML-serialized models. .bpmn20.xml / .cmmn.xml / .dmn.xml already end in .xml. -->
        <fileType name="XML" extensions="bpmn;cmmn;dmn"/>
        <!-- A .bar is a zip under another name. As the platform's ARCHIVE a double-click expands it (see
             ArchiveTreeStructureProvider) instead of asking which file type it is. -->
        <fileType name="ARCHIVE" extensions="bar"/>

        <!-- Schemas for that model XML. Without them a .bpmn / .cmmn / .dmn opens with an unresolved
             namespace and no tag or attribute completion at all: the IDE ships no BPMN, CMMN or DMN
             schemas of its own. The namespace-to-file table, what is deliberately left unvalidated, and
             why this is a provider rather than the declarative standardResource bean: FlowableSchemas. -->
        <standardResourceProvider implementation="com.flowable.atlas.schema.FlowableSchemas"/>
        <!-- A model file wears its type's icon — the explorer's glyph and colour — in the Project view
             and the editor tabs; a .bar the archive icon. Decided from the name alone. -->
        <fileIconProvider implementation="com.flowable.atlas.icons.ModelFileIconProvider"/>
        <!-- .zip / .bar nodes in the Project view list their entries — the platform drops them for an
             archive inside a content root. -->
        <treeStructureProvider implementation="com.flowable.atlas.model.ArchiveTreeStructureProvider"/>
        <!-- A process, case, decision, form or page opens as text and picture side by side — loose or out
             of an archive. The picture is painted in Swing, not JCEF, so it stays fast under Remote Dev. -->
        <fileEditorProvider implementation="com.flowable.atlas.preview.FlowableModelPreviewEditorProvider"/>
        <!-- The Structure tool window for a form/page (its components as nested in Design) and a
             process/case (its elements by name). Asked before the language's builder; any other
             JSON or XML file gets null and keeps its usual outline. -->
        <structureViewBuilder key="JSON" factoryClass="com.flowable.atlas.structure.ModelStructureViewProvider"/>
        <structureViewBuilder key="XML" factoryClass="com.flowable.atlas.structure.ModelStructureViewProvider"/>
        <!-- Reader Mode's visual formatting layer crashes on minified single-line JSON, which is what a
             Design model export is; order="last" is load-bearing, because the platform enables the layer
             from this same extension point. See FlowableModelVisualFormattingGuard. -->
        <readerModeProvider implementation="com.flowable.atlas.model.FlowableModelVisualFormattingGuard"
                            order="last"/>
        <!-- The Expression Playground: type an expression, get validation + completion. -->
        <toolWindow id="Expression Playground"
                    anchor="bottom"
                    secondary="true"
                    icon="/icons/atlas/playground.svg"
                    factoryClass="com.flowable.atlas.expr.toolwindow.FlowableExpressionToolWindowFactory"/>
        <!-- The Atlas Hub: the plugin's control center (model-index status, generated explorers,
             Flowable Design sync). Right anchor — the home of project control-center panels
             (Gradle, Maven, Database). -->
        <!-- The explorer's findings in Swing: works under Remote Development and without JCEF. -->
        <toolWindow id="Atlas Findings"
                    anchor="bottom"
                    icon="AllIcons.Toolwindows.ToolWindowProblems"
                    factoryClass="com.flowable.atlas.findings.AtlasFindingsToolWindowFactory"/>
        <toolWindow id="Atlas Hub"
                    anchor="right"
                    icon="/icons/atlas/hub.svg"
                    factoryClass="com.flowable.atlas.hub.AtlasHubToolWindowFactory"/>
        <!-- "Report Flowable Atlas Problem…" in the IDE's error dialog: builds the report, copies it to
             the clipboard and opens the issue tracker — nothing is transmitted by the plugin itself. -->
        <errorHandler implementation="com.flowable.atlas.AtlasErrorReportSubmitter"/>
        <!-- Starts the model-index build when a project opens, so the Hub, the markers and the
             inspections do not wait for the first completion or Rebuild click to ask for one. -->
        <postStartupActivity implementation="com.flowable.atlas.index.FlowableIndexWarmUp"/>
    </extensions>

    <projectListeners>
        <!-- Regenerates the model-constants class when the model index lands (a VFS listener fired on the
             change that had just dropped the index and found nothing to generate from). -->
        <listener class="com.flowable.atlas.generate.ModelConstantsAutoRefresher"
                  topic="com.flowable.atlas.events.AtlasEventsListener"/>
        <!-- Every way to a model ends in an editor tab; the Hub's Recent Models list listens there. -->
        <listener class="com.flowable.atlas.hub.RecentModelsService$Listener"
                  topic="com.intellij.openapi.fileEditor.FileEditorManagerListener"/>
    </projectListeners>

    <applicationListeners>
        <!-- The Hub's list of generated explorer pages is remembered until a page appears or goes. -->
        <listener class="com.flowable.atlas.explorer.AtlasExplorerFiles$Listener"
                  topic="com.intellij.openapi.vfs.newvfs.BulkFileListener"/>
    </applicationListeners>

    <!-- Action/group texts and descriptions come from messages/FlowableAtlasBundle.properties
         (action.<id>.text / .description keys). Ellipsis convention: only actions that always need
         further input before they can complete carry one. -->
    <actions resource-bundle="messages.FlowableAtlasBundle">
        <group id="Flowable.Menu" popup="true" icon="/icons/atlas/model.svg">
            <add-to-group group-id="ToolsMenu" anchor="last"/>

            <!-- Open -->
            <action id="Flowable.OpenAtlasHub"
                    class="com.flowable.atlas.action.OpenAtlasHubAction"
                    icon="/icons/atlas/hub.svg"/>
            <!-- Icons: AtlasIcons (/icons/atlas) where the thing has a meaning of its own, platform icons
                 for platform verbs (search, download). No entry in the main toolbar — the hub is the
                 plugin's visible surface, everything else stays in the menu. -->
            <action id="Flowable.OpenAtlasFindings"
                    class="com.flowable.atlas.action.OpenAtlasFindingsAction"
                    icon="AllIcons.Toolwindows.ToolWindowProblems"/>
            <action id="Flowable.OpenAtlasExplorer"
                    class="com.flowable.atlas.action.OpenAtlasExplorerAction"
                    icon="/icons/atlas/explorer.svg"/>
            <action id="Flowable.OpenExpressionPlayground"
                    class="com.flowable.atlas.action.OpenExpressionPlaygroundAction"
                    icon="/icons/atlas/playground.svg"/>
            <!-- Opens Search Everywhere on the "Flowable Model" tab — otherwise reachable only by
                 pressing Shift twice and tabbing across to it, which nobody discovers unaided. -->
            <action id="Flowable.GoToModel"
                    class="com.flowable.atlas.action.GoToModelAction"
                    icon="AllIcons.Actions.Search">
                <!-- The one action that competes with Shift-Shift for the hand: worth a chord. -->
                <keyboard-shortcut keymap="$default" first-keystroke="control alt shift M"/>
            </action>
            <!-- The same hits as the tab above, in a list that stays open: the popup is for reaching one
                 place, this is for working through all of them. No second chord — it is reached from the
                 popup with Shift+Enter, and from here.
                 The icon is the Find tool window's own, not a second magnifier: Actions.Search and
                 Actions.Find are near-identical glasses, and two adjacent entries that differ only in
                 wording are exactly what made this pair hard to tell apart. -->
            <action id="Flowable.FindInModels"
                    class="com.flowable.atlas.action.FindInModelsAction"
                    icon="AllIcons.Toolwindows.ToolWindowFind"/>
            <!-- The key under the caret — a literal or constant at a Flowable API site in Java, a reference or
                 the file's own key in a model file — onto the clipboard. Also in the editor's context menu. -->
            <action id="Flowable.CopyModelKey"
                    class="com.flowable.atlas.action.CopyModelKeyAction"
                    icon="AllIcons.Actions.Copy">
                <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            </action>
            <!-- The same model in two places: the entry inside an app export .bar/.zip and the file
                 sitting in the project. Both directions, whichever end is selected. Also in the editor's
                 context menu, which is how an entry opened by Go to Model is reached. -->
            <action id="Flowable.CompareModelWithArchive"
                    class="com.flowable.atlas.action.CompareModelWithArchiveAction"
                    icon="AllIcons.Actions.Diff">
                <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            </action>
            <separator/>

            <!-- Generate (submenu, with a nested Liquibase submenu) -->
            <group id="Flowable.Generate" popup="true">
                <action id="Flowable.GenerateAtlasExplorer"
                        class="com.flowable.atlas.action.GenerateAtlasExplorerAction"
                        icon="/icons/atlas/explorer.svg"/>
                <!-- No ellipsis: it re-runs the existing page(s) with the settings as they are. Its text is
                     what the Hub, the explorer banner and the post-pull balloon show (FlowableActionIds.text). -->
                <action id="Flowable.RegenerateAtlasExplorer"
                        class="com.flowable.atlas.action.RegenerateAtlasExplorerAction"/>
                <action id="Flowable.GenerateModelConstants"
                        class="com.flowable.atlas.action.GenerateModelConstantsAction"/>
                <group id="Flowable.GenerateLiquibase" popup="true">
                    <action id="Flowable.GenerateLiquibaseFromDataObject"
                            class="com.flowable.atlas.action.GenerateLiquibaseFromDataObjectAction"/>
                    <action id="Flowable.GenerateLiquibaseFromApps"
                            class="com.flowable.atlas.action.GenerateLiquibaseFromAppsAction"/>
                </group>
                <group id="Flowable.GenerateDto" popup="true">
                    <action id="Flowable.GenerateDtoFromDataObject"
                            class="com.flowable.atlas.action.GenerateDtoFromDataObjectAction"/>
                    <action id="Flowable.GenerateDtoFromApps"
                            class="com.flowable.atlas.action.GenerateDtoFromAppsAction"/>
                </group>
            </group>
            <separator/>

            <!-- Flowable Design -->
            <action id="Flowable.SwitchDesignEnvironment"
                    class="com.flowable.atlas.action.SwitchDesignEnvironmentAction"/>
            <action id="Flowable.PullFromDesign"
                    class="com.flowable.atlas.action.PullFromDesignAction"
                    icon="AllIcons.Actions.Download"/>
            <action id="Flowable.SwitchWorkEnvironment"
                    class="com.flowable.atlas.action.SwitchWorkEnvironmentAction"/>
            <!-- Every address in the catalog — Design, the app, Control, Hub — grouped by stage. It lives
                 in the descriptor rather than in the Hub because the Hub's ⋮ renders this very group:
                 an entry built only there is what made the two menus disagree. -->
            <group id="Flowable.OpenEnvironmentInBrowser"
                   class="com.flowable.atlas.action.OpenEnvironmentInBrowserGroup"
                   popup="true"/>
            <action id="Flowable.ManageEnvironments"
                    class="com.flowable.atlas.action.ManageEnvironmentsAction"/>
            <separator/>

            <!-- Maintenance -->
            <action id="Flowable.RebuildModelIndex"
                    class="com.flowable.atlas.action.RebuildModelIndexAction"/>
            <!-- Debug helper; visible only with idea.is.internal=true. -->
            <action id="Flowable.DumpKeyIndex" internal="true"
                    class="com.flowable.atlas.action.DumpFlowableIndexAction"/>
        </group>

        <!-- Right-click on a folder, a model file, an archive or a .json in the Project view: the
             actions that are about the thing under the cursor. Hidden elsewhere (FlowableProjectViewGroup). -->
        <group id="Flowable.ProjectViewPopup" class="com.flowable.atlas.action.FlowableProjectViewGroup" popup="false">
            <separator/>
            <reference ref="Flowable.GenerateAtlasExplorer"/>
            <reference ref="Flowable.GoToModel"/>
            <reference ref="Flowable.CompareModelWithArchive"/>
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
        </group>
    </actions>
</idea-plugin>
