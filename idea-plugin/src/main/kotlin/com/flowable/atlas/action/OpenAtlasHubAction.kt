package com.flowable.atlas.action

import com.flowable.atlas.hub.AtlasHubToolWindowFactory

/**
 * Tools → Flowable Atlas → *Open Atlas Hub*: the plugin's control centre — the model index, the generated
 * explorers, the Flowable Design pull.
 */
class OpenAtlasHubAction : OpenAtlasToolWindowAction(AtlasHubToolWindowFactory.ID)
